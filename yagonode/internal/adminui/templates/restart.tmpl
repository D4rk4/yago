{{define "content"}}
<nav class="cds-breadcrumb" aria-label="Breadcrumb"><a href="/admin/overview">Admin</a> / Restart node</nav>
<h1 class="cds-h1">Restart node</h1>
<div class="cds-tile">
  <p>Restarting stops the node gracefully and lets the supervisor bring it back
  up with the persisted settings applied. In-flight crawls and searches are
  interrupted for a few seconds; durable queues resume where they left off.</p>
  <form method="post" action="/admin/restart" hx-boost="false">
    <input type="hidden" name="csrf_token" value="{{.CSRF}}">
    <button class="cds-btn cds-btn--danger" type="submit">Restart node now</button>
    <a class="cds-btn cds-btn--ghost" href="/admin/overview">Cancel</a>
  </form>
</div>
{{end}}

{{define "restarting-node"}}<!doctype html>
<html lang="en">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<meta name="referrer" content="no-referrer">
<meta http-equiv="refresh" content="10;url=/admin/overview">
<title>Restarting — {{.AppName}} admin</title>
<style>
  body { font-family: 'Helvetica Neue', Helvetica, 'Nimbus Sans', Arial, sans-serif; background: #6f829c; color: #17150e; margin: 0; }
  .card { max-width: 26rem; margin: 14vh auto; background: #c9c6c0; border: 1px solid #46443d;
    box-shadow: inset 1px 1px 0 #fbfaf6, inset -1px -1px 0 #5c5a52, inset 2px 2px 0 #ece9e3, inset -2px -2px 0 #8b887f; }
  .card__bar { background: linear-gradient(180deg, #7ba2e8 0%, #5c8bdf 60%, #3f6fc4 100%);
    color: #fff; font-size: 0.8125rem; font-weight: 700; padding: 0.25rem 0.75rem;
    border-bottom: 1px solid #2f5296; box-shadow: inset 0 1px 0 rgba(255,255,255,0.45); }
  .card__bar b { color: #fa4d56; }
  .card__body { padding: 1.25rem; }
  h1 { font-size: 1.1875rem; font-weight: 700; margin: 0 0 0.75rem; }
  p { color: #46443c; font-size: 0.8125rem; }
</style>
</head>
<body>
<div class="card">
  <div class="card__bar"><b>ya</b>go admin</div>
  <div class="card__body">
    <h1>Restarting the node</h1>
    <p role="status">The node is shutting down gracefully and will be brought
    back up by its supervisor. This page returns to the console in about ten
    seconds; if it does not load yet, give the node a moment and reload.</p>
  </div>
</div>
</body>
</html>{{end}}
