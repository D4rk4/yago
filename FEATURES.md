# Feature Catalog

This catalog tracks project capabilities in Markdown. Update it when behavior, public surfaces, status, files, or tests change.

Status values: implemented, partial, planned, not applicable.

| Feature ID | Area | Feature | `yacynode` | `yacycrawler` | `yacycrawlcontract` | `yacymodel` / `yacyproto` | Relevant files and tests | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| FTR-001 | YaCy protocol | YaCy value and wire model | Uses the model and protocol packages at P2P edges and keeps golden YaCy wire fixtures for P0 peer endpoints. | Uses crawl contract values that reference YaCy metadata. | Defines crawl order and ingest batch JSON shapes. | Implemented value codecs, endpoint names, protocol DTOs, and fixture-backed parser/encoder coverage for `hello.html`, `query.html`, `transferRWI.html`, `transferURL.html`, and `search.html`. | `yacymodel/*_test.go`, `yacyproto/*_test.go`, `yacycrawlcontract/*_test.go`, `yacynode/test/fixtures/yacywire/*` | Protocol vocabulary stays at transport edges. |
| FTR-002 | Peer identity | Senior peer advertisement and liveness | Implemented for the current RWI node scope through YaCy peer endpoints; `/yacy/hello.html` rejects self-pings and callers using the local peer hash, `/yacy/query.html` accepts YaCy-compatible capacity checks with `youare` and optional `iam`, reports `rwicount`, per-word `rwiurlcount`, and `lurlcount`, returns zero for upstream `wanted*` probes, and the runtime can self-test that endpoint before DHT participation. | Not applicable. | Not applicable. | Supports seed, peer, and query protocol values. | `yacynode/internal/peeradmission/*_test.go`, `yacynode/internal/nodestatus/*_test.go`, `yacynode/cmd/yacy-rwi-node/*_test.go`, `yacyproto/query_test.go` | Operators configure the advertised peer hash, name, host, port, and optional public self-test base URL. |
| FTR-003 | DHT ingest | RWI posting intake | Implemented through `/yacy/transferRWI.html` with YaCy preflight result strings for wrong network units and missing required fields, plus Prometheus counters for received postings, rejected postings, unknown URL requests, and ingest duration. | Emits postings in ingest batches. | Carries crawler-produced postings. | Defines RWI posting wire values and transferRWI preflight results. | `yacynode/internal/rwi/*_test.go`, `yacynode/internal/metrics/*_test.go`, `yacymodel/rwi_*_test.go`, `yacyproto/transfer_rwi_test.go` | Accepted records must be durable before acknowledgement. |
| FTR-004 | DHT ingest | URL metadata intake | Implemented through `/yacy/transferURL.html` with YaCy network-auth preflight behavior and Prometheus counters for received, rejected, and RWI-reference-reconciled URL metadata rows. | Emits URL metadata in ingest batches. | Carries crawler-produced URL metadata. | Defines URL metadata, transfer URL protocol values, and empty auth-failure responses. | `yacynode/internal/urlmeta/*_test.go`, `yacynode/internal/urlreferences/*_test.go`, `yacynode/internal/metrics/*_test.go`, `yacymodel/url_metadata*_test.go`, `yacyproto/transfer_url_test.go` | The node stores metadata referenced by postings and does not store full document bodies. |
| FTR-005 | Search | Remote RWI search responder | Implemented for YaCy remote search scope, including requested `indexabstract.<wordhash>` and `indexcount.<wordhash>` rows. | Not applicable. | Not applicable. | Defines search protocol values, YaCy index abstract encode/decode behavior, and emits the YaCy `count` field for `/yacy/search.html` responses while accepting legacy `linkcount` on parse. | `yacynode/internal/documentsearch/*_test.go`, `yacymodel/search_index_abstract*_test.go`, `yacyproto/search_test.go` | Local search behavior is bounded by stored RWI and URL metadata. |
| FTR-006 | Peer exchange | Seedlist bootstrap and peer announcement | Implemented through bootstrap, roster, announcement packages, and `/yacy/seedlist.html`, `/yacy/seedlist.json`, and `/yacy/seedlist.xml` seed-list serving, including YaCy `maxcount`, `minversion`, `node`, `me`, `my`, `id`, `name`, `peername`, `address`, and `callback` filters, with Prometheus metrics for known peers, active peers, peer probe failures, and successful seedlist imports. | Not applicable. | Not applicable. | Supports seed encoding, clear-text seed-list variants, seed-list request filters, UTC offset and timestamp wire forms, and seed wire forms. | `yacynode/internal/bootstrap/*_test.go`, `yacynode/internal/peerannouncement/*_test.go`, `yacynode/internal/peerroster/*_test.go`, `yacynode/internal/seedlist/*_test.go`, `yacynode/internal/metrics/*_test.go`, `yacynode/cmd/yacy-rwi-node/*_test.go`, `yacyproto/seedlist_test.go` | The node does not redistribute unconfirmed inbound peers. |
| FTR-007 | Storage | Embedded durable storage and quota pressure | Implemented with replaceable storage boundaries, RWI and URL metadata collections, concurrent transaction safety, and eviction. | Not applicable. | Not applicable. | Supplies stored value forms. | `yacynode/internal/boltvault/*_test.go`, `yacynode/internal/memvault/*_test.go`, `yacynode/internal/vault/*_test.go`, `yacynode/internal/eviction/*_test.go` | Storage engines remain behind narrow interfaces. |
| FTR-008 | Operations | Health, metrics, and compatibility status | Implemented through the ops address and metrics packages, including HTTP endpoint, storage, eviction, peer discovery, inbound DHT, outbound DHT series, DHT gate status JSON, and machine-readable compatibility status JSON for implemented, partial, planned, and unsupported surfaces. | Not applicable. | Not applicable. | Not applicable. | `yacynode/internal/metrics/*_test.go`, `yacynode/internal/compatibility/*_test.go`, `yacynode/cmd/yacy-rwi-node/*_test.go`, `yacynode/doc/compatibility.md` | Operational behavior and compatibility claims are machine-readable. |
| FTR-009 | Crawling | Node-crawler message contract | Publishes crawl orders and consumes ingest batches when crawling is configured. | Consumes crawl orders and publishes ingest batches. | Implemented shared contract. | Reuses YaCy model values. | `yacycrawlcontract/*_test.go`, `yacynode/internal/crawlbroker/*_test.go`, `yacycrawler/internal/crawlorder/*_test.go`, `yacycrawler/internal/ingest/*_test.go` | `NATS_URL` enables crawl fleet integration. |
| FTR-010 | Crawling | Optional crawler worker | Not applicable except through node-side orchestration. | Partial production scope: fetches pages, parses content, builds references, and publishes ingest batches. | Carries orders and results. | Reuses URL metadata and RWI values. | `yacycrawler/internal/pagefetch/*`, `yacycrawler/internal/pageparse/*_test.go`, `yacycrawler/internal/pageindex/*_test.go`, `yacycrawler/internal/pipeline/*_test.go` | The crawler is optional and disposable. |
| FTR-011 | Crawling | Operator crawl dispatch and receipt | Partial node-side crawl dispatch exists; remote crawl receipts are accepted at the YaCy wire edge, target-checked, and rejected with YaCy retry delay while the remote crawl policy is disabled. | Executes accepted local orders. | Carries order provenance and ingest attribution. | Supports crawl receipt protocol values. | `yacynode/internal/crawldispatch/*_test.go`, `yacynode/internal/crawling/*_test.go`, `yacyproto/crawl_receipt_test.go`, `yacynode/doc/remote-crawl-policy.md` | Remote crawl execution remains disabled by default until trusted-peer and destination allowlist controls exist. |
| FTR-012 | Packaging | Container and compose examples | Implemented Dockerfile and compose example for the node. | Implemented Dockerfile for the crawler; the runtime image copies the pinned headless-shell runtime into a scratch non-root image. | Not applicable. | Not applicable. | `yacynode/Dockerfile`, `yacycrawler/Dockerfile`, `docker-compose.yml.example`, `.github/workflows/container-image.yml`, `AGENTS.md` | Feature closure includes Dockerized Semgrep and Trivy scans; runtime docs must stay aligned with image and compose behavior. |
| FTR-013 | YaCy protocol | Auxiliary P2P compatibility endpoints | Partial: `/yacy/list.html` checks the YaCy network unit and exports shared blacklist entries from files named in `YACY_DATA_DIR/SETTINGS/yacy.conf` `BlackLists.Shared`, `/yacy/profile.html` exports peer profile properties from `YACY_DATA_DIR/SETTINGS/profile.txt`, `/yacy/idx.json?object=host` exports a bounded incoming host-link index inferred from stored URL metadata referrers, `/yacy/message.html` accepts permission checks while ignoring post-only body fields and stores peer messages into a durable inbox, and `/yacy/urls.xml` serves URL-hash metadata feeds plus empty remote-crawl feeds while remote crawl execution is disabled. | Not applicable. | Not applicable. | Defines list, profile, index, message, and crawl URL endpoint paths and request DTOs, including list network-unit filtering. | `yacynode/internal/sharedblacklist/*_test.go`, `yacynode/cmd/yacy-rwi-node/node_shared_blacklist_test.go`, `yacynode/internal/peerprofile/*_test.go`, `yacynode/cmd/yacy-rwi-node/node_profile_test.go`, `yacynode/internal/hostlinks/*_test.go`, `yacynode/internal/urlmeta/*_test.go`, `yacynode/cmd/yacy-rwi-node/host_link_source_test.go`, `yacynode/internal/peermessage/*_test.go`, `yacynode/internal/crawlurls/*_test.go`, `yacyproto/list_test.go`, `yacyproto/profile_test.go`, `yacyproto/index_test.go`, `yacyproto/message_test.go`, `yacyproto/crawl_urls_test.go`, `yacynode/doc/remote-crawl-policy.md` | Additional original YaCy P2P endpoints remain to be implemented. |
| FTR-014 | DHT outbound | YaCy index transfer handoff | Partial: the node can evaluate YaCy-compatible sender-side DHT gates from runtime state and environment flags, expose those gates as JSON on the ops listener, require a YaCy-compatible public endpoint self-test before DHT distribution, feed an empty outbound queue from bounded stored RWI selections only after sender gates are open, durably journal selected postings before deleting them for queue handoff, recover pending selected postings during node storage startup, restore transferable rows when enqueue cannot keep them, clear recovery records after accepted handoffs, probe a peer's `/yacy/query.html?object=rwicount` capacity, choose YaCy-compatible DHT transfer targets from reachable peers, filter peers that do not advertise remote-index intake, apply the upstream age gate, clear a clashing rejected target's remote-index flag, use YaCy freeworld senior redundancy and vertical partition defaults unless overridden, build bounded outbound exchange chunks by target, run retry-aware outbound scheduler cycles with failed chunks returned to the queue, derive retry and quarantine decisions for failed outbound cycles, confirm successful targets in the peer roster, quarantine repeatedly failing targets out of the peer roster, observe outbound DHT Prometheus counters from scheduler receipts, and perform the two-phase handoff for a known peer: post RWI rows to `/yacy/transferRWI.html`, read `unknownURL`, load matching local URL metadata, and post those rows to `/yacy/transferURL.html`. | Not applicable. | Not applicable. | Reuses YaCy DHT position values, shared DHT target selection, vertical URL partitioning, DHT gate vocabulary, query response values, transfer RWI/URL request/response DTOs, and outbound DHT metric names. | `yacynode/cmd/yacy-rwi-node/*_test.go`, `yacynode/internal/dhttarget/*_test.go`, `yacynode/internal/dhtexchange/*_test.go`, `yacynode/internal/indextransfer/*_test.go`, `yacynode/internal/metrics/*_test.go`, `yacynode/internal/peerroster/*_test.go`, `yacynode/internal/rwi/*_test.go`, `yacymodel/dht_position_test.go`, `yacymodel/seed_test.go`, `yacyproto/query_test.go`, `yacyproto/transfer_rwi_test.go`, `yacyproto/transfer_url_test.go` | A two-local-Go-node integration test covers RWI and URL metadata transfer through the real YaCy endpoints. End-to-end Java YaCy interop distribution tests remain separate compatibility work. |
| FTR-015 | Search | YaCy public search surfaces | Partial: `/yacysearch.json`, `/yacysearch.rss`, and `/yacysearch.html` accept plain query text, local/global resource selection, pagination, content domain, language, site, in-URL, TLD, filetype, URL mask, preference mask, verify, and navigation parameters. JSON serves an upstream-like `channels[0].items` shape. RSS serves OpenSearch-flavored RSS 2.0 items. HTML serves a simple search form and result list. `/opensearchdescription.xml` advertises HTML, RSS, JSON suggestion, and XML suggestion URLs for the current public base URL. `/suggest.json` returns OpenSearch suggestion JSON from bounded in-memory recent queries. `/suggest.xml` returns YaCy-compatible `SearchSuggestion` XML from the same source. `resource=local` searches local RWI-backed metadata only. `resource=global` merges local results with bounded YaCy `/yacy/search.html` fanout to DHT-selected reachable peers for query term hashes using the configured DHT redundancy and partition exponent, uses YaCy `indexabstract` negotiation for multi-term remote result conjunctions before secondary `urls=` retrieval, filters remote targets by advertised non-empty RWI inventory, randomly samples redundant eligible DHT candidates, deduplicates by URL identity, and returns partial-failure metadata for unavailable, malformed, or missing DHT target peer responses. | Not applicable. | Not applicable. | Defines the public search paths, parameter vocabulary, OpenSearch description surface, suggestion shapes, remote search response values, and shared DHT target selection. | `yacynode/internal/searchcore/*_test.go`, `yacynode/internal/searchremote/*_test.go`, `yacynode/internal/dhttarget/*_test.go`, `yacynode/internal/documentsearch/*_test.go`, `yacynode/internal/yacysearch/*_test.go`, `yacynode/cmd/yacy-rwi-node/*_test.go` | Persistent/high-frequency suggestions and richer YaCy HTML parity remain separate compatibility work. |
