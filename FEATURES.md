# Feature Catalog

This catalog tracks project capabilities in Markdown. Update it when behavior, public surfaces, status, files, or tests change.

Status values: implemented, partial, planned, not applicable.

| Feature ID | Area | Feature | `yacynode` | `yacycrawler` | `yacycrawlcontract` | `yacymodel` / `yacyproto` | Relevant files and tests | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| FTR-001 | YaCy protocol | YaCy value and wire model | Uses the model and protocol packages at P2P edges. | Uses crawl contract values that reference YaCy metadata. | Defines crawl order and ingest batch JSON shapes. | Implemented value codecs, endpoint names, and protocol DTOs. | `yacymodel/*_test.go`, `yacyproto/*_test.go`, `yacycrawlcontract/*_test.go` | Protocol vocabulary stays at transport edges. |
| FTR-002 | Peer identity | Senior peer advertisement and liveness | Implemented for the current RWI node scope through YaCy peer endpoints. | Not applicable. | Not applicable. | Supports seed and peer protocol values. | `yacynode/internal/peeradmission/*_test.go`, `yacynode/internal/nodestatus/*_test.go`, `yacynode/cmd/yacy-rwi-node/*_test.go` | Operators configure the advertised peer hash, name, host, and port. |
| FTR-003 | DHT ingest | RWI posting intake | Implemented through `/yacy/transferRWI.html`. | Emits postings in ingest batches. | Carries crawler-produced postings. | Defines RWI posting wire values. | `yacynode/internal/rwi/*_test.go`, `yacymodel/rwi_*_test.go`, `yacyproto/transfer_rwi_test.go` | Accepted records must be durable before acknowledgement. |
| FTR-004 | DHT ingest | URL metadata intake | Implemented through `/yacy/transferURL.html`. | Emits URL metadata in ingest batches. | Carries crawler-produced URL metadata. | Defines URL metadata and transfer URL protocol values. | `yacynode/internal/urlmeta/*_test.go`, `yacymodel/url_metadata*_test.go`, `yacyproto/transfer_url_test.go` | The node stores metadata referenced by postings and does not store full document bodies. |
| FTR-005 | Search | Remote RWI search responder | Implemented for YaCy remote search scope. | Not applicable. | Not applicable. | Defines search protocol values. | `yacynode/internal/documentsearch/*_test.go`, `yacyproto/search_test.go` | Local search behavior is bounded by stored RWI and URL metadata. |
| FTR-006 | Peer exchange | Seedlist bootstrap and peer announcement | Implemented through bootstrap, roster, announcement packages, and `/yacy/seedlist.html`, `/yacy/seedlist.json`, and `/yacy/seedlist.xml` seed-list serving. | Not applicable. | Not applicable. | Supports seed encoding, clear-text seed-list variants, seed-list request filters, and wire forms. | `yacynode/internal/bootstrap/*_test.go`, `yacynode/internal/peerannouncement/*_test.go`, `yacynode/internal/peerroster/*_test.go`, `yacynode/internal/seedlist/*_test.go`, `yacyproto/seedlist_test.go` | The node does not redistribute unconfirmed inbound peers. |
| FTR-007 | Storage | Embedded durable storage and quota pressure | Implemented with replaceable storage boundaries, RWI and URL metadata collections, and eviction. | Not applicable. | Not applicable. | Supplies stored value forms. | `yacynode/internal/boltvault/*_test.go`, `yacynode/internal/vault/*_test.go`, `yacynode/internal/eviction/*_test.go` | Storage engines remain behind narrow interfaces. |
| FTR-008 | Operations | Health and metrics | Implemented through the ops address and metrics packages. | Not applicable. | Not applicable. | Not applicable. | `yacynode/internal/metrics/*_test.go`, `yacynode/cmd/yacy-rwi-node/response_metrics_middleware_test.go` | Operational behavior is machine-readable. |
| FTR-009 | Crawling | Node-crawler message contract | Publishes crawl orders and consumes ingest batches when crawling is configured. | Consumes crawl orders and publishes ingest batches. | Implemented shared contract. | Reuses YaCy model values. | `yacycrawlcontract/*_test.go`, `yacynode/internal/crawlbroker/*_test.go`, `yacycrawler/internal/crawlorder/*_test.go`, `yacycrawler/internal/ingest/*_test.go` | `NATS_URL` enables crawl fleet integration. |
| FTR-010 | Crawling | Optional crawler worker | Not applicable except through node-side orchestration. | Partial production scope: fetches pages, parses content, builds references, and publishes ingest batches. | Carries orders and results. | Reuses URL metadata and RWI values. | `yacycrawler/internal/pagefetch/*`, `yacycrawler/internal/pageparse/*_test.go`, `yacycrawler/internal/pageindex/*_test.go`, `yacycrawler/internal/pipeline/*_test.go` | The crawler is optional and disposable. |
| FTR-011 | Crawling | Operator crawl dispatch and receipt | Partial node-side crawl dispatch and receipt surfaces exist. | Executes accepted orders. | Carries order provenance and ingest attribution. | Supports crawl receipt protocol values. | `yacynode/internal/crawldispatch/*_test.go`, `yacynode/internal/crawling/*_test.go`, `yacyproto/crawl_receipt_test.go` | Remote crawl work remains security-sensitive. |
| FTR-012 | Packaging | Container and compose examples | Implemented Dockerfile and compose example for the node. | Implemented Dockerfile for the crawler; the runtime image copies the pinned headless-shell runtime into a scratch non-root image. | Not applicable. | Not applicable. | `yacynode/Dockerfile`, `yacycrawler/Dockerfile`, `docker-compose.yml.example`, `.github/workflows/container-image.yml`, `AGENTS.md` | Feature closure includes Dockerized Semgrep and Trivy scans; runtime docs must stay aligned with image and compose behavior. |
| FTR-013 | YaCy protocol | Auxiliary P2P compatibility endpoints | Partial: `/yacy/list.html` exports shared blacklist entries, `/yacy/profile.html` exports peer profile properties, `/yacy/idx.json?object=host` exports the host-link index, `/yacy/message.html` accepts peer messages into a durable inbox, and `/yacy/urls.xml` serves remote-crawl URL and URL-hash metadata feeds; list/profile/host-link/remote-crawl runtime sources are empty. | Not applicable. | Not applicable. | Defines list, profile, index, message, and crawl URL endpoint paths and request DTOs. | `yacynode/internal/sharedblacklist/*_test.go`, `yacynode/internal/peerprofile/*_test.go`, `yacynode/internal/hostlinks/*_test.go`, `yacynode/internal/peermessage/*_test.go`, `yacynode/internal/crawlurls/*_test.go`, `yacyproto/list_test.go`, `yacyproto/profile_test.go`, `yacyproto/index_test.go`, `yacyproto/message_test.go`, `yacyproto/crawl_urls_test.go` | Additional original YaCy P2P endpoints remain to be implemented. |
| FTR-014 | DHT outbound | YaCy index transfer handoff | Partial: runtime distribution is not enabled, but the node can evaluate YaCy-compatible sender-side DHT gates, choose YaCy-compatible DHT transfer targets from reachable peers, filter peers that do not advertise remote-index intake, apply the upstream age gate, build bounded outbound exchange chunks by target, and perform the two-phase handoff for a known peer: post RWI rows to `/yacy/transferRWI.html`, read `unknownURL`, load matching local URL metadata, and post those rows to `/yacy/transferURL.html`. | Not applicable. | Not applicable. | Reuses YaCy DHT position values, vertical URL partitioning, DHT gate vocabulary, and transfer RWI/URL request/response DTOs. | `yacynode/internal/dhtexchange/*_test.go`, `yacynode/internal/indextransfer/*_test.go`, `yacymodel/dht_position_test.go`, `yacymodel/seed_test.go`, `yacyproto/transfer_rwi_test.go`, `yacyproto/transfer_url_test.go` | Runtime scheduling, retry, local deletion after successful DHT handoff, metrics, and automatic distribution remain separate compatibility work. |
