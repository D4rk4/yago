# Feature Catalog

This catalog tracks project capabilities in Markdown. Update it when behavior, public surfaces, status, files, or tests change.

Status values: implemented, partial, planned, not applicable.

| Feature ID | Area | Feature | `yacynode` | `yacycrawler` | `yacycrawlcontract` | `yacymodel` / `yacyproto` | Relevant files and tests | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| FTR-001 | YaCy protocol | YaCy value and wire model | Uses the model and protocol packages at P2P edges and keeps golden YaCy wire fixtures for P0 peer endpoints. | Uses crawl contract values that reference YaCy metadata. | Defines crawl order and ingest batch JSON shapes. | Implemented value codecs, endpoint names, protocol DTOs, and fixture-backed parser/encoder coverage for `hello.html`, `query.html`, `transferRWI.html`, `transferURL.html`, and `search.html`. | `yacymodel/*_test.go`, `yacyproto/*_test.go`, `yacycrawlcontract/*_test.go`, `yacynode/test/fixtures/yacywire/*` | Protocol vocabulary stays at transport edges. |
| FTR-002 | Peer identity | Senior peer advertisement and liveness | Implemented for the current `yago-node` compatibility scope through YaCy peer endpoints; `/yacy/hello.html` rejects self-pings and callers using the local peer hash, `/yacy/query.html` accepts YaCy-compatible capacity checks with `youare` and optional `iam`, reports `rwicount`, per-word `rwiurlcount`, and `lurlcount`, returns zero for upstream `wanted*` probes, and the runtime can self-test that endpoint before DHT participation. The advertised seed carries the YaCy statistics DNA (`NCount`, `RCount`, `SCount`, `CCount`, `ISpeed`, `RSpeed`, `USpeed`, `sI`, `rI`, `sU`, `rU`) with honest current values: the known-seed count from the peer roster, persistent cumulative DHT transfer totals for words and URLs sent and received, and a `BDate` birth date persisted across restarts. | Not applicable. | Not applicable. | Supports seed, peer, query, and seed statistics protocol values. | `yacynode/internal/peeradmission/*_test.go`, `yacynode/internal/nodestatus/*_test.go`, `yacynode/internal/peerbirth/*_test.go`, `yacynode/internal/transfertally/*_test.go`, `yacynode/internal/yagonode/*_test.go`, `yacymodel/seed_statistics_test.go`, `yacyproto/query_test.go` | Operators configure the advertised peer hash, name, host, port, and optional public self-test base URL. Upstream also counts remote-search result intake into `rI`/`rU`; this node counts DHT transfer intake and handoffs. |
| FTR-003 | DHT ingest | RWI posting intake | Implemented through `/yacy/transferRWI.html` with YaCy preflight result strings for wrong network units and missing required fields, plus Prometheus counters for received postings, rejected postings, unknown URL requests, and ingest duration. | Emits postings in ingest batches. | Carries crawler-produced postings. | Defines RWI posting wire values and transferRWI preflight results. | `yacynode/internal/rwi/*_test.go`, `yacynode/internal/metrics/*_test.go`, `yacymodel/rwi_*_test.go`, `yacyproto/transfer_rwi_test.go` | Accepted records must be durable before acknowledgement. |
| FTR-004 | DHT ingest | URL metadata intake | Implemented through `/yacy/transferURL.html` with YaCy network-auth preflight behavior and Prometheus counters for received, rejected, and RWI-reference-reconciled URL metadata rows. | Emits URL metadata in ingest batches. | Carries crawler-produced URL metadata. | Defines URL metadata, transfer URL protocol values, and empty auth-failure responses. | `yacynode/internal/urlmeta/*_test.go`, `yacynode/internal/urlreferences/*_test.go`, `yacynode/internal/metrics/*_test.go`, `yacymodel/url_metadata*_test.go`, `yacyproto/transfer_url_test.go` | URL metadata remains the YaCy exchange metadata layer; document content metadata is stored separately for local search. |
| FTR-005 | Search | Remote RWI search responder | Implemented for YaCy remote search scope, including requested `indexabstract.<wordhash>` and `indexcount.<wordhash>` rows. | Not applicable. | Not applicable. | Defines search protocol values, YaCy index abstract encode/decode behavior, and emits the YaCy `count` field for `/yacy/search.html` responses while accepting legacy `linkcount` on parse. | `yacynode/internal/documentsearch/*_test.go`, `yacymodel/search_index_abstract*_test.go`, `yacyproto/search_test.go` | RWI remains the YaCy P2P exchange/search responder layer; local public search now uses the `SearchIndex` path. |
| FTR-006 | Peer exchange | Seedlist bootstrap and peer announcement | Implemented through bootstrap, roster, announcement packages, and `/yacy/seedlist.html`, `/yacy/seedlist.json`, and `/yacy/seedlist.xml` seed-list serving, including YaCy `maxcount`, `minversion`, `node`, `me`, `my`, `id`, `name`, `peername`, `address`, and `callback` filters, with Prometheus metrics for known peers, active peers, peer probe failures, and successful seedlist imports. | Not applicable. | Not applicable. | Supports seed encoding, clear-text seed-list variants, seed-list request filters, UTC offset and timestamp wire forms, and seed wire forms. | `yacynode/internal/bootstrap/*_test.go`, `yacynode/internal/peerannouncement/*_test.go`, `yacynode/internal/peerroster/*_test.go`, `yacynode/internal/seedlist/*_test.go`, `yacynode/internal/metrics/*_test.go`, `yacynode/internal/yagonode/*_test.go`, `yacyproto/seedlist_test.go` | The node does not redistribute unconfirmed inbound peers. |
| FTR-007 | Storage | Embedded durable storage and quota pressure | Implemented with replaceable storage boundaries, document, RWI, and URL metadata collections, concurrent transaction safety, eviction, `yago-node.db` for new data dirs, and fallback opening of existing `yacy-rwi.db` files. | Not applicable. | Supplies `DocumentIngest` as the crawler-to-node document payload. | Supplies stored value forms. | `yacynode/internal/documentstore/*_test.go`, `yacynode/internal/boltvault/*_test.go`, `yacynode/internal/memvault/*_test.go`, `yacynode/internal/vault/*_test.go`, `yacynode/internal/eviction/*_test.go`, `yacynode/internal/yagonode/node_config_test.go` | Document storage is bounded extracted text and metadata, not unbounded raw crawl bodies. |
| FTR-008 | Operations | Health, readiness, metrics, and compatibility status | Implemented through the ops address and metrics packages, including HTTP endpoint, readiness JSON, storage, eviction, peer discovery, inbound DHT, outbound DHT series, DHT gate status JSON, search index stats JSON, and machine-readable compatibility status JSON for implemented, partial, planned, and unsupported surfaces. | Not applicable. | Not applicable. | Not applicable. | `yacynode/internal/metrics/*_test.go`, `yacynode/internal/compatibility/*_test.go`, `yacynode/internal/yagonode/*_test.go`, `yacynode/doc/compatibility.md` | Operational behavior and compatibility claims are machine-readable. |
| FTR-009 | Crawling | Node-crawler message contract | Publishes crawl orders and consumes ingest batches when crawling is configured; ingest absorption stores document payloads, updates the local full-text index, then stores URL metadata and postings. | Consumes crawl orders, expands sitemap and sitelist start modes, and publishes ingest batches. | Implemented shared contract for crawl request modes, sitemap `LastModified` hints, `DocumentIngest`, bounded image metadata, RWI postings, URL metadata, and attribution. | Reuses YaCy model values. | `yacycrawlcontract/*_test.go`, `yacynode/internal/crawlbroker/*_test.go`, `yacynode/internal/crawlresults/*_test.go`, `yacycrawler/internal/crawlorder/*_test.go`, `yacycrawler/internal/crawlseed/*_test.go`, `yacycrawler/internal/ingest/*_test.go` | `YACY_CRAWL_RPC_ADDR` enables crawl fleet integration. |
| FTR-010 | Crawling | Optional crawler worker | Not applicable except through node-side orchestration. | Partial production scope: rejects non-public crawl destinations before robots/fetch, checks final URLs against the same public-web policy, expands XML sitemap URL sets/indexes and plain text sitelists into bounded URL roots before frontier admission, fetches ordinary HTML pages through a bounded HTTP fast path with explicit redirect-hop and timeout budgets, falls back to the browser only when the fast path rejects the page, parses content/headings/canonical links/meta descriptions/nofollow link relations/image URL and alt metadata, excludes nofollow links from frontier and outlink evidence unless the crawl profile opts in, builds document ingest payloads plus references, and publishes ingest batches. | Carries orders and results. | Reuses URL metadata and RWI values. | `yacycrawler/cmd/yacycrawler/*_test.go`, `yacycrawler/internal/crawlseed/*_test.go`, `yacycrawler/internal/sitemap/*_test.go`, `yacycrawler/internal/httpfetch/*_test.go`, `yacycrawler/internal/publicweb/*_test.go`, `yacycrawler/internal/pagefetch/*_test.go`, `yacycrawler/internal/pageparse/*_test.go`, `yacycrawler/internal/pageindex/*_test.go`, `yacycrawler/internal/pipeline/*_test.go` | The crawler is optional and disposable. Robots.txt sitemap discovery, recrawl scheduling, persistent frontier, and browser request interception for redirect blocking remain separate hardening work. |
| FTR-011 | Crawling | Operator crawl dispatch and receipt | Partial node-side crawl dispatch exists at `/crawl` and accepts local `url`, `sitemap`, and `sitelist` start modes; it validates the crawl profile before publishing, rejecting an impossible must-match or must-not-match URL regex, a negative or unbounded crawl depth, a non-positive pages-per-host cap, and negative recrawl or delay durations with a `400` instead of admitting a job that would only fail deep in the worker; remote crawl receipts are accepted at the YaCy wire edge, network-auth failures produce no delay field, same-network malformed or wrong target hashes get YaCy retry delay, and addressed receipts are rejected with the same delay while the remote crawl policy is disabled. | Executes accepted local orders. | Carries crawl request mode, order provenance, and ingest attribution. | Supports crawl receipt protocol values. | `yacynode/internal/crawldispatch/*_test.go`, `yacynode/internal/crawling/*_test.go`, `yacyproto/crawl_receipt_test.go`, `yacynode/doc/remote-crawl-policy.md` | Remote crawl execution remains disabled by default until trusted-peer and destination allowlist controls exist. |
| FTR-012 | Packaging | Container and compose examples | Implemented `yago-node` Dockerfile and compose service; the node image ships only the `yago-node` entrypoint. | Implemented Dockerfile for the crawler; the runtime image copies the pinned headless-shell runtime into a scratch non-root image; the crawler identifies as `yago-crawler` by default. | Not applicable. | Not applicable. | `yacynode/Dockerfile`, `yacynode/cmd/yago-node`, `yacynode/internal/yagonode`, `yacycrawler/Dockerfile`, `yacycrawler/cmd/yacycrawler/crawler_config.go`, `docker-compose.yml.example`, `.github/workflows/container-image.yml`, `AGENTS.md` | Feature closure includes Dockerized Semgrep and Trivy scans; runtime docs must stay aligned with image and compose behavior. |
| FTR-013 | YaCy protocol | Auxiliary P2P compatibility endpoints | Partial: `/yacy/list.html` checks the YaCy network unit and exports shared blacklist entries from files named in `YACY_DATA_DIR/SETTINGS/yacy.conf` `BlackLists.Shared`, `/yacy/profile.html` exports peer profile properties from `YACY_DATA_DIR/SETTINGS/profile.txt`, `/yacy/idx.json?object=host` exports a bounded incoming host-link index counted from stored document outlinks per source host, `/yacy/message.html` accepts permission checks without requiring `iam` while ignoring post-only body fields and stores peer messages into a durable inbox, and `/yacy/urls.xml` serves URL-hash metadata feeds plus empty remote-crawl feeds while remote crawl execution is disabled. | Not applicable. | Not applicable. | Defines list, profile, index, message, and crawl URL endpoint paths and request DTOs, including list network-unit filtering. | `yacynode/internal/sharedblacklist/*_test.go`, `yacynode/internal/yagonode/node_shared_blacklist_test.go`, `yacynode/internal/peerprofile/*_test.go`, `yacynode/internal/yagonode/node_profile_test.go`, `yacynode/internal/hostlinks/*_test.go`, `yacynode/internal/urlmeta/*_test.go`, `yacynode/internal/yagonode/host_link_source_test.go`, `yacynode/internal/peermessage/*_test.go`, `yacynode/internal/crawlurls/*_test.go`, `yacyproto/list_test.go`, `yacyproto/profile_test.go`, `yacyproto/index_test.go`, `yacyproto/message_test.go`, `yacyproto/crawl_urls_test.go`, `yacynode/doc/remote-crawl-policy.md` | Additional original YaCy P2P endpoints remain to be implemented. |
| FTR-014 | DHT outbound | YaCy index transfer handoff | Partial: the node can evaluate YaCy-compatible sender-side DHT gates from runtime state and environment flags, expose those gates as JSON on the ops listener, require a YaCy-compatible public endpoint self-test before DHT distribution, feed an empty outbound queue from bounded stored RWI selections only after sender gates are open, durably journal selected postings before deleting them for queue handoff, recover pending selected postings during node storage startup, restore transferable rows when enqueue cannot keep them, clear recovery records after accepted handoffs, probe a peer's `/yacy/query.html?object=rwicount` capacity, choose YaCy-compatible DHT transfer targets from reachable peers, filter peers that do not advertise remote-index intake, apply the upstream age gate, clear a clashing rejected target's remote-index flag, use YaCy freeworld senior redundancy and vertical partition defaults unless overridden, build bounded outbound exchange chunks by target, run retry-aware outbound scheduler cycles with failed chunks returned to the queue, derive retry and quarantine decisions for failed outbound cycles, confirm successful targets in the peer roster, quarantine repeatedly failing targets out of the peer roster, observe outbound DHT Prometheus counters from scheduler receipts, and perform the two-phase handoff for a known peer: post RWI rows to `/yacy/transferRWI.html`, read `unknownURL`, load matching local URL metadata, and post those rows to `/yacy/transferURL.html`. | Not applicable. | Not applicable. | Reuses YaCy DHT position values, shared DHT target selection, vertical URL partitioning, DHT gate vocabulary, query response values, transfer RWI/URL request/response DTOs, and outbound DHT metric names. | `yacynode/internal/yagonode/*_test.go`, `yacynode/internal/dhttarget/*_test.go`, `yacynode/internal/dhtexchange/*_test.go`, `yacynode/internal/indextransfer/*_test.go`, `yacynode/internal/metrics/*_test.go`, `yacynode/internal/peerroster/*_test.go`, `yacynode/internal/rwi/*_test.go`, `yacymodel/dht_position_test.go`, `yacymodel/seed_test.go`, `yacyproto/query_test.go`, `yacyproto/transfer_rwi_test.go`, `yacyproto/transfer_url_test.go` | A two-local-Go-node integration test covers RWI and URL metadata transfer through the real YaCy endpoints, and the live Java interop matrix in `yacynode/test/e2e` proves outbound distribution into a real `yacy/yacy_search_server` peer and inbound Java-initiated distribution from it. |
| FTR-015 | Search | YaCy public search surfaces | Partial: `/yacysearch.json`, `/yacysearch.rss`, and `/yacysearch.html` accept plain query text, local/global resource selection, pagination, content domain, language, site, in-URL, TLD, filetype, URL mask, preference mask, verify, and navigation parameters. JSON serves an upstream-like `channels[0].items` shape. RSS serves OpenSearch-flavored RSS 2.0 items. HTML serves a simple search form and result list. `/opensearchdescription.xml` advertises HTML, RSS, JSON suggestion, and XML suggestion URLs for the current public base URL. `/suggest.json` returns OpenSearch suggestion JSON from bounded in-memory recent queries. `/suggest.xml` returns YaCy-compatible `SearchSuggestion` XML from the same source. `resource=local` searches the local persistent `SearchIndex` full-text path over stored documents. `resource=global` merges local full-text results with bounded YaCy `/yacy/search.html` fanout to DHT-selected reachable peers for query term hashes using the configured DHT redundancy and partition exponent, uses YaCy `indexabstract` negotiation for multi-term remote result conjunctions before secondary `urls=` retrieval, filters remote targets by advertised non-empty RWI inventory, randomly samples redundant eligible DHT candidates, deduplicates by URL identity, and returns partial-failure metadata for unavailable, malformed, or missing DHT target peer responses. | Not applicable. | Not applicable. | Defines the public search paths, parameter vocabulary, OpenSearch description surface, suggestion shapes, remote search response values, and shared DHT target selection. | `yacynode/internal/searchcore/*_test.go`, `yacynode/internal/searchlocal/*_test.go`, `yacynode/internal/searchremote/*_test.go`, `yacynode/internal/dhttarget/*_test.go`, `yacynode/internal/searchindex/*_test.go`, `yacynode/internal/documentsearch/*_test.go`, `yacynode/internal/yacysearch/*_test.go`, `yacynode/internal/yagonode/*_test.go` | Persistent/high-frequency suggestions and richer YaCy HTML parity remain separate work. The live Java interop matrix proves global search returns documents indexed only by a real `yacy/yacy_search_server` peer. |
| FTR-016 | Search | Local search backend boundary | Partial: defines the node-side `SearchIndex` abstraction over document indexing, deletion, search, and stats; opens an embedded persistent Bleve fallback under `YACY_DATA_DIR/search.bleve`; rebuilds that index from the document store only when missing or unusable; crawler ingest updates it before URL metadata and RWI postings are acknowledged; `searchlocal` adapts it to the shared public search core. | Provides extracted document payloads that the node indexes. | Carries document fields required by snippets, filters, raw-content references, image metadata, and Tavily-compatible responses. | Not applicable. | `yacynode/internal/searchindex/*_test.go`, `yacynode/internal/searchlocal/*_test.go`, `yacynode/internal/documentstore/*_test.go`, `yacynode/internal/crawlresults/*_test.go`, `yacycrawlcontract/*_test.go`, `yacycrawler/internal/pageindex/*_test.go`, `yacynode/doc/adr/0012-use-bleve-for-embedded-full-text-fallback.md` | Tantivy sidecar remains the future production backend choice; RWI remains a compatibility exchange layer. |
| FTR-017 | Agent API | Tavily-compatible search surface | Partial: mounts `POST /search` over the shared search core; accepts current Tavily-like search contract fields including `query`, `search_depth`, `chunks_per_source`, `max_results`, `topic`, `time_range`, `start_date`, `end_date`, `include_answer`, `include_raw_content`, image/favicon flags, domain filters, `country`, `auto_parameters`, `exact_match`, `include_usage`, and `safe_search`; ignores unknown fields for forward compatibility; optionally requires `Authorization: Bearer <YAGO_SEARCH_API_KEY>` when that local key is configured; returns Tavily-like `query`, optional empty `answer`, stored page images when `include_images` is requested, `results`, `response_time`, optional `auto_parameters`, local synthetic `usage`, `request_id`, and stable JSON error envelopes. It also mounts `POST /extract`, returning Tavily-like extract results (`results` and `failed_results`) for URLs already in the document store, accepting `urls` as a string or array plus `extract_depth`, `format`, `include_images`, and `include_favicon` under the same bearer auth and error envelopes; fetch-on-extract is disabled, so uncached URLs become controlled `failed_results` with no private-network fetch. | Not applicable. | Supplies extracted document content and image metadata that can satisfy `raw_content` and image response fields after ingest. | Not applicable. | `yacynode/internal/tavilyapi/*_test.go`, `yacynode/internal/yagonode/node_public_search_test.go`, `yacynode/internal/compatibility/*_test.go` | Basic, fast, and ultra-fast depths use local search; advanced depth includes DHT-selected reachable-peer search. Local answer generation, image ranking/search, real usage accounting, key scopes, hashed API key storage, per-key rate limits, optional upstream Tavily, and fetch-on-extract for uncached URLs remain planned. |
| FTR-017 | Peer news | YaCy news pool and seed attachment | Partial: a vault-backed peer news pool keeps the YaCy incoming, processed, outgoing, and published queues; own publications carry the upstream record wire form (`{key=value,...}` with `ori`, `cat`, `cre`, `rec`, `dis`), the id offset attribute `#`, and duplicate suppression; each publication is offered thirty times and then rests in the published queue; incoming records are validated against the upstream category whitelist. Each announcement cycle rotates the next publication into the advertised seed `news` attribute in the YaCy `b\|` wire form, and news attachments arriving on same-network hello callers and on seeds returned by greeted peers are decoded and enqueued. | Not applicable. | Not applicable. | Provides the seed `news` attribute and the base64 wire form codec. | `yacynode/internal/peernews/*_test.go`, `yacynode/internal/peeradmission/hello_news_test.go`, `yacynode/internal/peerannouncement/announce_news_test.go`, `yacynode/internal/nodestatus/node_report_test.go` | Automatic processing of incoming news categories (profile updates, crawl starts) into local state remains planned. |
| FTR-018 | Security | Admin authentication | Implemented: gates every operations endpoint except `/health` and `/ready` behind an admin session. The single administrator is provisioned by `YAGO_ADMIN_USER`/`YAGO_ADMIN_PASSWORD` at start or by `POST /api/admin/v1/auth/setup` on first run (allowed only while no admin exists), and the password is stored as an Argon2id hash (ADR-0015). `POST /api/admin/v1/auth/login` verifies the password with a constant-time compare and timing equalized against a missing account, then returns an `HttpOnly` `SameSite=Strict` session cookie plus a CSRF token; login is rate limited per client. `logout` invalidates the durable server-side session, `GET /api/admin/v1/auth/session` returns the current administrator, and unsafe methods require the `X-CSRF-Token` header to match the session. There is no default password and a failed login does not disclose whether the account exists. | Not applicable. | Not applicable. | Not applicable. | `yacynode/internal/adminauth/*_test.go`, `yacynode/internal/yagonode/node_admin_auth_test.go`, `yacynode/internal/compatibility/*_test.go`, `yacynode/doc/adr/0015-argon2id-admin-password-hashing.md` | CORS controls (SEC-03), password change and rotation, and the Carbon login UI remain planned. |
| FTR-019 | Security | Admin API keys and scopes | Implemented: an `Authorization: Bearer` API key authenticates operations requests as an alternative to the session cookie. `POST /api/admin/v1/auth/api-keys` mints a high-entropy key with an explicit scope set, returns the secret exactly once, and stores only its SHA-256 hash beside a public identifier; `GET` lists key metadata and last-used time without the secret; `DELETE /api/admin/v1/auth/api-keys/{id}` revokes by identifier. The guard resolves the scope a path needs (`admin:read` for safe methods, `admin:write` for unsafe methods, `crawl:write` for `POST /crawl`), grants every scope to a cookie session, checks the required scope for a key, rate limits requests per key, and requires no CSRF token on the Bearer path. Scopes are `admin:read`, `admin:write`, `crawl:write`, and the reserved `search:read` and `search:raw`. | Not applicable. | Not applicable. | Not applicable. | `yacynode/internal/adminauth/api_key_store_test.go`, `yacynode/internal/adminauth/api_key_endpoints_test.go`, `yacynode/internal/adminauth/guard_apikey_test.go`, `yacynode/internal/compatibility/*_test.go` | Per-key scope enforcement on the public `POST /search` surface (TAVILY-03) and CORS controls (SEC-03) remain planned. |
