# Feature Catalog

This catalog tracks project capabilities in Markdown. Update it when behavior, public surfaces, status, files, or tests change.

Status values: implemented, partial, planned, not applicable.

| Feature ID | Area | Feature | `yagonode` | `yagocrawler` | `yagocrawlcontract` | `yagomodel` / `yagoproto` | Relevant files and tests | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| FTR-001 | YaCy protocol | YaCy value and wire model | Uses the model and protocol packages at P2P edges and keeps golden YaCy wire fixtures for P0 peer endpoints. | Uses crawl contract values that reference YaCy metadata. | Defines crawl order and ingest batch JSON shapes. | Implemented value codecs, endpoint names, protocol DTOs, and fixture-backed parser/encoder coverage for `hello.html`, `query.html`, `transferRWI.html`, `transferURL.html`, and `search.html`. | `yagomodel/*_test.go`, `yagoproto/*_test.go`, `yagocrawlcontract/*_test.go`, `yagonode/test/fixtures/yacywire/*` | Protocol vocabulary stays at transport edges. |
| FTR-002 | Peer identity | Senior peer advertisement and liveness | Implemented for the current `yago-node` compatibility scope through YaCy peer endpoints; `/yacy/hello.html` rejects self-pings and callers using the local peer hash, `/yacy/query.html` accepts YaCy-compatible capacity checks with `youare` and optional `iam`, reports `rwicount`, per-word `rwiurlcount`, and `lurlcount`, returns zero for upstream `wanted*` probes, and the runtime can self-test that endpoint before DHT participation. The advertised seed carries the YaCy statistics DNA (`NCount`, `RCount`, `SCount`, `CCount`, `ISpeed`, `RSpeed`, `USpeed`, `sI`, `rI`, `sU`, `rU`) with honest current values: the known-seed count from the peer roster, persistent cumulative DHT transfer totals for words and URLs sent and received, and a `BDate` birth date persisted across restarts. The seed's capability flags — the YaCy `Seed.java` bitfield (direct-connect bit 0, accept-remote-crawl 1, accept-remote-index 2, root-node 3, SSL-available 4, encoded 5 bits per space-offset ASCII atom) — are operator-configurable in Configuration → Network & peers (`peer.advertise.direct_connect`/`remote_index`/`root_node`/`ssl`, defaults direct+remote-index on) and applied on restart when the seed identity is rebuilt — the remote-index flag is honored, not just advertised: with it off both DHT-in transfer endpoints refuse with YaCy's not-granted answers (transferRWI `not_granted`, transferURL `error_not_granted`), matching `allowReceiveIndex` semantics; accept-remote-crawl stays clear because remote crawl execution is disabled for SSRF safety, so the node never advertises a capability its `crawlReceipt` endpoint rejects. The seed's `Version` stays a plain numeric YaCy-compatible value (peers parse it as a float and gate features on it) aligned to the current YaCy release so peers treat this node as a current participant; separately, the node brands its outbound peer and HTTP requests with a `yago/<build>` User-Agent (filled only when a caller set none, so the extract and web-search agents keep theirs) and shows its human-facing YagoSeek build version on the landing page. The peer hash and name are generated and persisted to the data directory when the environment does not supply them, so a bare node bootstraps without a mandatory identity yet keeps a stable one (and its standing on the network) across restarts. | Not applicable. | Not applicable. | Supports seed, peer, query, and seed statistics protocol values. | `yagonode/internal/peeradmission/*_test.go`, `yagonode/internal/nodestatus/*_test.go`, `yagonode/internal/peerbirth/*_test.go`, `yagonode/internal/peeridentity/*_test.go`, `yagonode/internal/transfertally/*_test.go`, `yagonode/internal/yagonode/*_test.go`, `yagomodel/seed_statistics_test.go`, `yagoproto/query_test.go` | Operators may pin the advertised peer hash, name, host, port, and optional public self-test base URL, or leave the hash and name unset to auto-generate and persist a stable identity on first start. When the node announces to the network without a pinned `YAGO_ADVERTISE_HOST`, it auto-detects its first non-loopback IPv4 address rather than refusing to start; operators behind NAT or Docker bridge networking still pin the host explicitly. Upstream also counts remote-search result intake into `rI`/`rU`; this node counts DHT transfer intake and handoffs. |
| FTR-003 | DHT ingest | RWI posting intake | Implemented through `/yacy/transferRWI.html` with YaCy preflight result strings for wrong network units and missing required fields; with the accept-remote-index capability off the transfer is refused with YaCy's `not_granted` (allowReceiveIndex parity, `error_not_granted` on transferURL); intake is shed with YaCy's exact "too high load" answer when all admission slots are busy (a shared concurrency gate across both DHT-in transfer endpoints, the Go stand-in for YaCy's system-load check); an inbound transfer carrying more postings than a batch may hold is answered busy with a pause at the transfer endpoint (a swarm-transfer admission limit), and storage-capacity pressure answers busy with a pause from the shared intake. The batch-size limit guards only the swarm-facing endpoint — the same intake serves local crawl ingest, so a crawled page whose posting count exceeds the transfer cap is absorbed in full rather than deferred forever. Prometheus counters for received postings, rejected postings, unknown URL requests, and ingest duration. | Emits postings in ingest batches. | Carries crawler-produced postings. | Defines RWI posting wire values and transferRWI preflight results. | `yagonode/internal/rwi/*_test.go`, `yagonode/internal/metrics/*_test.go`, `yagomodel/rwi_*_test.go`, `yagoproto/transfer_rwi_test.go` | Accepted records must be durable before acknowledgement. |
| FTR-004 | DHT ingest | URL metadata intake | Implemented through `/yacy/transferURL.html` with YaCy network-auth preflight behavior; intake is shed with the endpoint's not-granted answer when all admission slots are busy (shared with the RWI transfer gate). Prometheus counters for received, rejected, and RWI-reference-reconciled URL metadata rows. | Emits URL metadata in ingest batches. | Carries crawler-produced URL metadata. | Defines URL metadata, transfer URL protocol values, and empty auth-failure responses. | `yagonode/internal/urlmeta/*_test.go`, `yagonode/internal/urlreferences/*_test.go`, `yagonode/internal/metrics/*_test.go`, `yagomodel/url_metadata*_test.go`, `yagoproto/transfer_url_test.go` | URL metadata remains the YaCy exchange metadata layer; document content metadata is stored separately for local search. |
| FTR-005 | Search | Remote RWI search responder | Implemented for YaCy remote search scope, including requested `indexabstract.<wordhash>` and `indexcount.<wordhash>` rows; requested count and time are clamped like YaCy, and concurrent floods beyond the admission slots are shed with empty-but-valid responses (YaCy 1.0 distributed-search DoS protection). The answer is built from the RWI posting index and URL-metadata store, never Solr (the intentional no-Solr swarm-interop divergence, ADR-0012): a current YaCy release's Solr-only request fields are accepted and logged but do not steer the search, so an interoperating peer's request never fails on them. Every outbound search supplies the node's current compact seed in `myseed`, which current YaCy peers require before executing the query. A wire-conformance test drives the real `/yacy/search.html` route with a multi-term query and parses the raw body back through the same peer-response parser the outbound path uses, proving the response is consumable by a YaCy-compatible peer; the outbound direction stays covered by the opt-in live `yacy/yacy_search_server` interop matrix. | Not applicable. | Not applicable. | Defines search protocol values, YaCy index abstract encode/decode behavior, and emits the YaCy `count` field for `/yacy/search.html` responses while accepting legacy `linkcount` on parse. | `yagonode/internal/documentsearch/*_test.go`, `yagomodel/search_index_abstract*_test.go`, `yagoproto/search_test.go`, `yagonode/test/e2e/interop_matrix_e2e_test.go` | RWI remains the YaCy P2P exchange/search responder layer; local public search now uses the `SearchIndex` path. Fixed (PORT-01, from upstream `nikitakarpei/yacy-rwi-node` commit b39b88e): the responder read a per-posting `ColWordDistance` ("i") column that the indexer never writes (it writes only `ColTextPosition` "t"), so `Cardinal("i")` failed on every real crawled posting and the appearance was silently discarded — this node returned nothing to a searching peer for its own crawled content, and only unit fixtures injecting "i" masked it. The responder now reads `ColTextPosition` and derives cross-term proximity (term spread = max−min of the query terms' positions) at join time, with the `MaxDist` proximity filter enforced post-join; a regression test drives postings shaped exactly as the indexer writes them (no "i" column) and asserts they are returned and proximity-ranked. The matching set is reduced to the requested top-k with a bounded heap instead of a full sort (result-identical because the ranking is a strict total order), and each per-term posting scan stops at the per-term cap once the exact per-term match total is provably unread downstream — the local searchcore path, and peer requests that ask for no index abstracts — skipping the tail of long posting lists; abstract-bearing peer requests keep the exhaustive scan so the wire `indexcount` stays exact, which a conformance test asserts under a cap far below the true count. Textbook MaxScore/WAND is architecturally inapplicable to this conjunctive, span-ranked, count-reporting path, so it is deliberately not adopted (PERF-CPU-01, ADR-0040). A live inbound test (a real YaCy peer calling this node) is a documented manual opt-in step because CI cannot deterministically force an external peer's DHT to target this node. |
| FTR-006 | Peer exchange | Seedlist bootstrap and peer announcement | Implemented through bootstrap, roster, announcement packages, and `/yacy/seedlist.html`, `/yacy/seedlist.json`, and `/yacy/seedlist.xml` seed-list serving, including YaCy `maxcount`, `minversion`, `node`, `me`, `my` (key presence selects the own seed, matching YaCy's containsKey semantics — even a bare `?my` or `my=false`), `id`, `name`, `peername`, `address`, and `callback` filters, with Prometheus metrics for known peers, active peers, peer probe failures, and successful seedlist imports. With `YAGO_PEER_HTTPS_PREFERRED` (default false, YaCy's `network.unit.protocol.https.preferred` parity) every outbound peer-protocol call — hello greet, back-ping, remote search, transferRWI/transferURL, RWI-count probe — tries a peer's advertised HTTPS endpoint (SSL seed flag + `PortSSL`, version >= 1.5) first and retries the same peer over plain HTTP only on transport failure, YaCy-style, through a dedicated egress-guarded client that tolerates the self-signed certificates YaCy peers serve (authenticity on the wire is protocol-level, not PKI; TLS 1.2 minimum; all non-peer egress keeps strict verification). Every egress-guarded dial (peer protocol and crawler fetch) prefers IPv4: a host that resolves to both families is tried over IPv4 first and only falls back to IPv6, so a dual-stack peer stays reachable on a host with IPv6 disabled, where an IPv6 socket cannot even be created ("address family not supported by protocol"). | Not applicable. | Not applicable. | Supports seed encoding, clear-text seed-list variants, seed-list request filters, UTC offset and timestamp wire forms, and seed wire forms. | `yagonode/internal/bootstrap/*_test.go`, `yagonode/internal/peerannouncement/*_test.go`, `yagonode/internal/peerroster/*_test.go`, `yagonode/internal/seedlist/*_test.go`, `yagonode/internal/metrics/*_test.go`, `yagonode/internal/yagonode/*_test.go`, `yagoegress/*_test.go`, `yagoproto/seedlist_test.go` | The node does not redistribute unconfirmed inbound peers. |
| FTR-007 | Storage | Embedded durable storage and quota pressure | Implemented with replaceable storage boundaries, document, RWI, and URL metadata collections, concurrent transaction safety, eviction, `yago-node.db` for new data dirs, and fallback opening of existing `yacy-rwi.db` files. The vault engine is sharded and compressed (ADR-0025): records route by xxhash64(bucket+key) across the bbolt shard files selected by a linear-hashing state (level, split) recorded in vault/manifest.json — version 2, a legacy version-1 flat count N reading as the equivalent state (log2 N, 0) so an existing vault adopts the scheme with no data migration (ADR-0037); the pool starts at the min-8 concurrency floor (independent of the quota) and while unsplit routes exactly as hash mod 2^level — living in a three-level fanout (vault/aa/bb/cc/aabbcc.vlt), keeping every file far below the 10 GB cap, and a background growth loop grows the pool one shard at a time whenever the mean shard load exceeds ~7 GB (gated by the storage.autosplit setting, YAGO_STORAGE_AUTOSPLIT, default on) by splitting the linear-hashing pointer shard under the engine's exclusive gate — copying the records that rehash into the new shard, flipping the manifest atomically, then reclaiming the moved copies, so a crash leaves either the old layout intact or the new one committed (ADR-0037); shard files open without per-commit freelist sync (bbolt rebuilds the freelist on open; hashmap freelist shape) so a write transaction stops paying freelist pages and their fsync on fsync-bound storage (IO-AGG-03); an operator on hardware with reliable power can additionally opt the shards into deferred fsync (bbolt NoSync, `storage.defer_fsync` / `YAGO_STORAGE_DEFER_FSYNC`, default off and restart-required because it is only crash-safe where a bounded loss window is acceptable — not on the XFS production node): commits then skip the per-commit disk flush entirely and a background maintenance pass flushes the shards on a staggered cadence that bounds the loss window without a synchronized fsync storm, while a clean shutdown flushes every shard so a planned restart or the setting change itself loses nothing (ADR-0038, IO-DEFER-01); writes yield the disk to in-flight reads for up to a configurable budget per update (`YAGO_STORAGE_READ_DEFER`, default 50ms — raise it to prioritize reads harder under sustained crawl load, a negative value disables the yield; IO-PRIO-01, PERF-PRIO-02), so interactive searches and admin pages get the disk ahead of bulk ingest without ever starving the writers, and the cumulative time yielded is exported as the `storage_read_defer_seconds_total` metric so read-vs-ingest contention is observable; values compress with zstd at the fastest level (frame checksums verify on read; small/incompressible values stay raw with a crc32c) while keys stay uncompressed so ordered prefix scans work — reads merge the shards' cursors into one ascending key sequence. A fan-out term read (the reverse word index, whose postings scatter across every shard) consults a per-shard binary-fuse membership filter over that shard's term-key prefixes before seeking it, so a shard that provably holds no posting for the term is skipped instead of sought — the win grows with the shard count and is largest for rare and multi-term queries (PERF-READ-01, ADR-0039, via the `xorfilter` library): each shard keeps an immutable fuse filter rebuilt at open and compaction plus a mutable side-set of keys written since, membership is conservative (a not-yet-built, failed, or hit filter never skips; only a built miss on both sets skips), and a failed build degrades to matching everything, so the filter can only ever cost a wasted seek, never drop a result. The filtered collection and its key width are injected by the assembly layer so the engine stays independent of the RWI layout; without that wiring the filters are off and reads never skip. Each shard is an independent failure domain: losing one file loses 1/N of the keyspace, never the store. Cross-shard atomicity is relaxed by design (per-shard commits; a failed commit rolls back the remaining shards so none stays write-locked). A legacy single-file vault migrates automatically on startup (compressing on the way) and is retired to a .migrated.bak file. The full-text search index shards the same way (ADR-0025): documents route by xxhash64(docID) across 8 scorch shard indexes in the three-level fanout (index root/xx/zz/yy/xxzzyy.idx), each created with a merge policy capping merged segments at 400k documents (zap files stay far below the 1 GB cap; zap segments are already snappy-compressed and this mapping stores no bodies) and three retained snapshots, and biasing merge selection toward the segments carrying the most deleted documents (ReclaimDeletesWeight 2.5, above the 2.0 default but below the ~3.0 the planner warns is too aggressive) so the churn from re-ingest posting purges and document eviction reclaims tombstoned space sooner and a search or merge reads less dead disk (PERF-IO-02, ADR-0041); searches fan in through a bleve IndexAlias so callers still see one index, counts sum across shards, and a legacy single-directory bleve index rebuilds into the sharded layout from the vault on startup (or keeps serving as a single compatibility shard when no rebuild source exists). Loss tolerance is active on both stores: a vault shard that fails to open is quarantined (renamed aside with a .quarantine suffix for offline inspection, logged) and replaced with a fresh empty shard so the store keeps serving the surviving N-1 shards; every stored value is integrity-checked on read (zstd frame checksums / crc32c) with corrupt records surfacing as absent on point reads and as errors on scans; file-lock waits are bounded so a stuck lock fails startup instead of hanging it. Unreadable index shards already recreate alone and re-fill from the vault on the rebuild pass. Storage accounting is truthful (ADR-0036): reported usage counts only live bytes — the pages actually holding data — not a shard file's high-water size, so space freed by deletions stops reading as used; eviction purges each URL's full stored document (bounded text and metadata) alongside its postings and URL metadata rather than only the index rows; and a background compaction pass rewrites over-full shards under the engine's exclusive gate to hand deleted pages back to the OS on a configurable cadence (`YAGO_STORAGE_COMPACTION_INTERVAL`, default daily, `off` to disable, and a live Configuration → Storage admin control), so a churning store no longer thrashes eviction against a file that never shrinks. The disk-budget quota is a live ceiling (ADR-0037): the engine holds it as an atomic value that the capacity check and eviction sweep read every cycle, so raising or lowering it through the Configuration → Storage control (or `YAGO_STORAGE_QUOTA`) takes effect immediately — no restart, no reshard — and the boot path pushes the operator's persisted quota override onto the freshly opened vault before serving, so an admin-set budget is honored on the next start instead of being masked by the environment default (the shard count no longer derives from the quota at all). | Not applicable. | Supplies `DocumentIngest` as the crawler-to-node document payload. | Supplies stored value forms. | `yagonode/internal/documentstore/*_test.go`, `yagonode/internal/boltvault/*_test.go`, `yagonode/internal/memvault/*_test.go`, `yagonode/internal/vault/*_test.go`, `yagonode/internal/shardvault/*_test.go`, `yagonode/internal/eviction/*_test.go`, `yagonode/internal/yagonode/node_config_test.go`, `yagonode/internal/yagonode/shard_deferred_sync_loop_test.go` | Document storage is bounded extracted text and metadata, not unbounded raw crawl bodies. |
| FTR-008 | Operations | Health, readiness, metrics, and compatibility status | Implemented through the ops address and metrics packages, including HTTP endpoint, readiness JSON, storage, eviction, peer discovery, inbound DHT, outbound DHT series, admin authentication counters (login attempts by result and API key authentications by result), DHT gate status JSON, search index stats JSON, admin search ranking explain/model/train/rollback endpoints with held-out promotion reports, content-safety model status/train/rollback endpoints, and machine-readable compatibility status JSON for implemented, partial, planned, and unsupported surfaces. | Not applicable. | Not applicable. | Not applicable. | `yagonode/internal/metrics/*_test.go`, `yagonode/internal/compatibility/*_test.go`, `yagonode/internal/yagonode/*_test.go`, `yagonode/doc/compatibility.md` | Operational behavior and compatibility claims are machine-readable. |
| FTR-009 | Crawling | Node-crawler message contract | Publishes crawl orders and consumes ingest batches when crawling is configured; ingest absorption stores document payloads, updates the local full-text index, then stores URL metadata and postings; a storage fault redelivers the batch and is logged as an error, while capacity backpressure redelivers it quietly (debug, with a reason instead of a null error) so a busy vault cannot flood the operator. | Consumes crawl orders, expands sitemap, sitelist, and robots start modes, and publishes ingest batches. | Implemented shared contract for crawl request modes, sitemap `LastModified` hints, `DocumentIngest`, bounded image metadata, RWI postings, URL metadata, and attribution. | Reuses YaCy model values. | `yagocrawlcontract/*_test.go`, `yagonode/internal/crawlbroker/*_test.go`, `yagonode/internal/crawlresults/*_test.go`, `yagocrawler/internal/crawlorder/*_test.go`, `yagocrawler/internal/crawlseed/*_test.go`, `yagocrawler/internal/ingest/*_test.go` | `YAGO_CRAWL_RPC_ADDR` defaults to `127.0.0.1:9091` (matching yagocrawler's own default), so a package install that enables both services connects the crawl fleet out of the box; set it to `off` for a node that runs no crawler, or to `:9091`/a routable address to accept a crawler on another host. |
| FTR-010 | Crawling | Optional crawler worker | Not applicable except through node-side orchestration. | Partial production scope: rejects non-public crawl destinations before robots/fetch while ignoring bogus unspecified DNS records (a "::" or "0.0.0.0" published alongside real addresses cannot be dialed, so it neither admits nor condemns the host; a private address in the answer still rejects the whole host to keep the DNS-rebinding defense), checks final URLs against the same public-web policy, counts a non-robots rejected fetch as a failed fetch in the run tally and metrics and logs the rejection reason at info (a rejected seed previously finished the run all-zero with no trace), expands XML sitemap URL sets/indexes and plain text sitelists into bounded URL roots before frontier admission, discovers sitemaps from a host's robots.txt `Sitemap:` directives on a robots start and expands them through the same bounded path, fetches ordinary HTML pages through a bounded HTTP fast path with explicit redirect-hop and timeout budgets, falls back to the headless browser only when the fast path rejects a page for a reason the browser could resolve — a status or bot-wall rejection, never an unsupported (non-HTML) media type, which the browser refuses under the same shared MIME policy, so a PDF or image no longer wastes a browser tab — and drives one long-lived headless Firefox over the Marionette protocol (a single browser process reused across every fetch rather than a fresh launch per page), configured deployment-agnostically: `YAGOCRAWLER_BROWSER_PATH` names the binary (empty = discovery on PATH, `firefox-esr` then `firefox`, so the container's bundled `firefox-esr` and a host's `/usr/bin/firefox-esr` both work, and a browser path left pointing at a Chromium/Chrome binary — a stale setting from before the crawler moved to Firefox — is ignored with an actionable startup warning and Firefox is discovered on PATH instead, since the slow path speaks Firefox-only Marionette, BROWSER-05) and `YAGOCRAWLER_BROWSER_SANDBOX` (default off) drops Firefox's content-process sandbox so it starts inside the disposable, egress-guarded container and on hosts that restrict the unprivileged user namespaces the sandbox needs, while a hardened host can opt the sandbox back in; a circuit breaker pauses the browser fallback after `YAGOCRAWLER_BROWSER_FAILURE_THRESHOLD` consecutive browser malfunctions (a launch failure, a dead process, a navigation timeout — default 5, 0 disables) and re-probes after a cooldown, so a crashed or incompatible browser degrades to the HTTP fast path instead of relaunching and failing on every page, while a clean non-HTML rejection proves the browser is alive and never trips it (BROWSER-04) — the same binary runs under Docker, systemd, and a Debian/RPM package (see `deploy/`), parses content/headings/canonical links/meta descriptions/nofollow link relations/image URL and alt metadata, excludes nofollow links from frontier and outlink evidence unless the crawl profile opts in, honors page-level robots directives — `<meta name="robots">` and the `X-Robots-Tag` response header (case-insensitive substring match, `none` = both, YaCy ContentScraper parity; the browser fetch path cannot observe response headers, so header directives ride the HTTP path only): a noindex page is crawled for links but never indexed or emitted, a nofollow page's links are not discovered, the profile's follow-nofollow opt-in overrides page-level nofollow, and the robots-ignoring opt-out waives both (CRAWL-28), optionally treats a rel=canonical resolving to a different URL than the fetched page as noindex-but-follow (`noindexCanonicalMismatch` profile flag, console checkbox and operator-API field, default off because canonical often points paginated pages at page 1; YaCy NOINDEX_WHEN_CANONICAL_UNEQUAL_URL parity, CRAWL-29), checks a redirect's final URL against the run's visited-set and per-host caps so two URLs redirecting to one target index it once per run — the skipped duplicate counts in the run tally's duplicates and the links are not re-discovered (CRAWL-30), skips RWI postings for stopword tokens via a crawler-side mirror of the node's stopword list so the crawler's postings and the node's query-side heuristics agree — non-stop tokens keep their positions from the original token stream and the document/bleve text is untouched (SEARCH-39), admits discovered links against the crawl scope (wide, domain, or subpath) where a domain-scoped crawl treats a bare domain and its `www.` variant as one host so it follows links between `example.com` and `www.example.com` (YaCy 1.4 relaxed site-operator parity; subpath scope stays exact-host), paces every crawl run at a polite default of 30 fetches per minute (`YAGOCRAWLER_RUN_PAGES_PER_MINUTE`; 0 disables the default) layered on top of the per-host crawl delay, so a fresh crawl cannot flatten the box or hammer the web at full speed — the monitor's per-run rate control overrides it, including an explicit zero to deliberately unleash a run, builds document ingest payloads plus references, publishes ingest batches, and exposes `yacy_crawler_*` Prometheus metrics (active jobs, fetches, fetch failures, bytes, robots denials, ingest batches) on an optional `/metrics` listener configured by `YAGOCRAWLER_METRICS_ADDR`, and shuts down gracefully on `SIGINT`/`SIGTERM` by stopping new-job intake while letting in-flight fetches finish within `YAGOCRAWLER_SHUTDOWN_GRACE`, reports each run's progress to the node not only at start and finish but on a periodic interval while it runs, carrying the live outcome tally and live queue depth so the node's crawl monitor shows advancing counters and runtime instead of the frozen start snapshot, and settles each order against the node's durable lease queue — acking a fully-delivered order, naking for redelivery both a cancelled order and one where any page's references failed to deliver (a hard ingest-emit failure such as the node going unreachable mid-run, which previously still acked and silently dropped those references under at-least-once; a deterministic index/parse failure still acks so it does not poison-loop; ported from upstream `nikitakarpei/yacy-rwi-node` commit 0384b476), and heartbeating in-flight leases so a crashed or disconnected worker's orders are reclaimed and retried (at-least-once delivery) — and, because a reconnecting worker's heartbeats would otherwise keep extending its stranded leases past the sweeper's reach, the node returns a worker's still-held leases to the pending queue the moment its last order stream drops, so a crawler that disconnected mid-batch is fed again on reconnect rather than only after a node restart (issue #230); the node extends lease deadlines durably at most once per quarter-TTL — heartbeats arrive every few seconds because they also carry control directives, and rewriting the lease records with an fsync per beat is pure overhead on fsync-bound storage (IO-AGG-02), while a dead worker is still reclaimed within one TTL of its last durable extension. Crawl requests (plain HTTP, robots fetches, and the browser fetch chain) carry a version-branded User-Agent `yago-crawler/<build> (+https://github.com/D4rk4/yago/)` — the build version is baked in as a calendar version and overridable at release time via `-ldflags -X main.version` (the Dockerfile injection now targets a real symbol); `YAGOCRAWLER_USER_AGENT` still overrides the whole string. Page parsing extracts the author (article:author over meta name=author) into the document metadata for the author: search operator and the coming facets. Document parsing goes through a format registry (YaCy TextParser parity): content type and URL extension route to format families, resolving an extension-less document (an arxiv `/pdf/<id>`, a CMS download route) by its content type — the office, misc, and text families additionally resolve their sub-format from the content type or the file's magic bytes when the URL has no extension (an extension-less .docx by its OOXML type, an RTF by application/rtf, a vCard by its BEGIN:VCARD line), and the office routing now carries the real OOXML/OpenDocument/legacy content types (CRAWL-FORMAT-01, verified against real pandoc/LibreOffice .docx/.xlsx/.odt/.rtf and a real arxiv PDF served at extension-less URLs — CRAWL-FORMAT-02) — text (txt/tex/csv as plain text, RTF through a control-word walker that skips font/color/info/pict groups and decodes hex and unicode escapes, Outlook MSG via best-effort readable-run extraction), XML+feeds (RSS 2.0 and Atom parse one document per feed with item titles/summaries as text and item links followable, capped at 200 items; other XML indexes its character data), PDF/PS (an own bounded extractor: content streams decode through their /Filter chains and BT..ET text blocks yield the show-operator strings — parenthesized literals with octal escapes and <hex> strings alike — TJ-array fragments rejoin split words while large kerns keep inter-word gaps, embedded ToUnicode CMaps map subset-font glyph codes back to text with TeX-ligature and Latin-1 byte fallbacks, and the Info title reads literal or UTF-16 hex form (SEARCH-PDF-01); a font with no usable mapping stays honestly unparsed rather than indexing noise — no OCR; PostScript extracts its shown text literals), Office (OOXML word/slide/shared-string parts and OpenDocument/StarOffice content+meta parse as zip+XML text with stdlib only; FreeMind .mm maps index their node tree; legacy binary doc/xls/ppt/Visio extract best-effort readable runs — full POI-grade decoding is a YaCy divergence), images (dimensions via the stdlib decoders plus raw BMP/PSD/WBMP headers, JPEG EXIF camera/date/GPS through an own bounded TIFF-IFD reader, SVG title/description/size; the document carries itself as an image so the image vertical surfaces standalone pictures; no OCR, like YaCy), audio tags (own bounded readers: MP3 ID3v2 text frames and the ID3v1 trailer, Ogg Vorbis comments, WAV/AIFF INFO and annotation chunks, iTunes-style M4A/MP4 metadata atoms, C64 SID headers; wma/ra/rm fall back to readable-run extraction; tags only, no transcription, like YaCy), vCard contacts (RFC 6350 subset: FN/ORG/TEL/EMAIL/ADR/URL/NOTE with line unfolding), BitTorrent metainfo (own bencode decoder: name, comment, tracker, file list, sizes), Android APKs (zip file list plus best-effort manifest strings — full binary-AXML decoding is a documented divergence), and archive containers (zip/jar/epub, tar, gz/tgz, bz2/tbz — inner files dispatch to the registered parsers with entry-count, per-entry, total-size, and text caps; nested archives stop at one level; xz/txz await a decompressor ADR) — each behind an operator toggle stamped into every dispatched crawl profile from the console's shared format settings; HTML/web text stays always-on, unknown types keep the HTML fallback, and a family without its extractor reports the page unparsed instead of indexing garbage. Defaults enable every family except archives (unpacking hostile containers is a security decision). | Carries orders and results. | Reuses URL metadata and RWI values. | `yagocrawler/cmd/yagocrawler/*_test.go`, `yagocrawler/internal/crawladmission/*_test.go`, `yagocrawler/internal/crawlseed/*_test.go`, `yagocrawler/internal/sitemap/*_test.go`, `yagocrawler/internal/weburl/*_test.go`, `yagocrawler/internal/crawlermetrics/*_test.go`, `yagocrawler/internal/httpfetch/*_test.go`, `yagocrawler/internal/publicweb/*_test.go`, `yagocrawler/internal/pagefetch/*_test.go`, `yagocrawler/internal/firefoxfetch/*_test.go`, `yagocrawler/internal/pageparse/*_test.go`, `yagocrawler/internal/pageindex/*_test.go`, `yagocrawler/internal/pipeline/*_test.go`, `yagocrawler/internal/crawlorder/*_test.go`, `yagocrawler/internal/crawlrun/*_test.go`, `yagocrawler/internal/frontier/*_test.go`, `yagocrawler/test/e2e/robots_mode_e2e_test.go`, `yagocrawler/test/e2e/combined_crawl_e2e_test.go` | The crawler is optional and disposable. Recrawl scheduling, persistent frontier, and browser request interception for redirect blocking remain separate hardening work. |
| FTR-011 | Crawling | Operator crawl dispatch and receipt | Partial node-side crawl dispatch exists at `/crawl` and accepts local `url`, `sitemap`, `sitelist`, and `robots` start modes; it validates the crawl profile before publishing, rejecting an impossible must-match or must-not-match URL regex, a negative or unbounded crawl depth, a non-positive pages-per-host cap, and negative recrawl or delay durations with a `400` instead of admitting a job that would only fail deep in the worker; remote crawl receipts are accepted at the YaCy wire edge, network-auth failures produce no delay field, same-network malformed or wrong target hashes get YaCy retry delay, and addressed receipts are rejected with the same delay while the remote crawl policy is disabled. | Executes accepted local orders. | Carries crawl request mode, order provenance, and ingest attribution. | Supports crawl receipt protocol values. | `yagonode/internal/crawldispatch/*_test.go`, `yagonode/internal/crawling/*_test.go`, `yagoproto/crawl_receipt_test.go`, `yagonode/doc/remote-crawl-policy.md` | Remote crawl execution remains disabled by default until trusted-peer and destination allowlist controls exist. |
| FTR-012 | Packaging | Container and compose examples | Implemented `yago-node` Dockerfile and compose service; the node image ships only the `yago-node` entrypoint. Both container images now use the shared /opt/yago layout (OPS-04 tasks 1-2): binaries in /opt/yago/bin, node state in /opt/yago/data (YAGO_DATA_DIR default) with /opt/yago/etc for operator config, both declared volumes; docker-compose.yml.example mounts yago-data/yago-etc named volumes at the new paths and sketches an optional TLS reverse proxy; deploy/README.md carries the migration note for pre-layout deployments (same named volume, new target — data survives; or keep /data via YAGO_DATA_DIR). The crawler image is stateless, so only its binary moved. | Implemented Dockerfile for the crawler; the runtime image uses the pinned Alpine 3.24 Docker Official Image with pinned Firefox ESR, CA, and Liberation font packages, runs as a non-root user, preserves the `nice`/`ionice` entrypoint, and identifies as `yago-crawler` by default. | Not applicable. | Not applicable. | `yagonode/Dockerfile`, `yagonode/cmd/yago-node`, `yagonode/internal/yagonode`, `yagocrawler/Dockerfile`, `yagocrawler/cmd/yagocrawler/crawler_config.go`, `docker-compose.yml.example`, `yagonode/doc/adr/0044-alpine-browser-crawler-runtime.md`, `AGENTS.md` | Feature closure includes Dockerized Semgrep and Trivy scans; runtime docs must stay aligned with image and compose behavior. Container images are built locally from these Dockerfiles (`docker compose build`); there is no CI image-publishing workflow. |
| FTR-013 | YaCy protocol | Auxiliary P2P compatibility endpoints | Partial: `/yacy/list.html` checks the YaCy network unit and exports shared blacklist entries from files named in `YAGO_DATA_DIR/SETTINGS/yacy.conf` `BlackLists.Shared`, `/yacy/profile.html` exports peer profile properties from `YAGO_DATA_DIR/SETTINGS/profile.txt`, `/yacy/idx.json?object=host` exports a bounded incoming host-link index counted from stored document outlinks per source host, `/yacy/message.html` grants permission to a correctly addressing network peer and stores a posted message's wire-decoded subject and body (taken from the sender's `myseed`) into a durable inbox at full parity with YaCy `message.java`, and `/yacy/urls.xml` serves URL-hash metadata feeds plus empty remote-crawl feeds while remote crawl execution is disabled. | Not applicable. | Not applicable. | Defines list, profile, index, message, and crawl URL endpoint paths and request DTOs, including list network-unit filtering. | `yagonode/internal/sharedblacklist/*_test.go`, `yagonode/internal/yagonode/node_shared_blacklist_test.go`, `yagonode/internal/peerprofile/*_test.go`, `yagonode/internal/yagonode/node_profile_test.go`, `yagonode/internal/hostlinks/*_test.go`, `yagonode/internal/urlmeta/*_test.go`, `yagonode/internal/yagonode/host_link_source_test.go`, `yagonode/internal/peermessage/*_test.go`, `yagonode/internal/crawlurls/*_test.go`, `yagoproto/list_test.go`, `yagoproto/profile_test.go`, `yagoproto/index_test.go`, `yagoproto/message_test.go`, `yagoproto/crawl_urls_test.go`, `yagonode/doc/remote-crawl-policy.md` | `/yacy/idx.json`, `/yacy/list.html`, and `/yacy/profile.html` match upstream's only-implemented behavior (`object=host` / `col=black` / file-or-empty), validated against the YaCy servlet sources (`idx.java`, `list.java`, `profile.java`, `WebStructureGraph.java`, 2026-07); `/yacy/message.html` is now verified at full parity against `message.java` (the previously-flagged missing `iam` and attachment storage are declined by upstream itself), leaving `/yacy/urls.xml` (remote-crawl delegation gated, not unfinished) as the sole partial member. |
| FTR-014 | DHT outbound | YaCy index transfer handoff | Partial: outbound transfers and RWI-count probes follow the peer-protocol HTTPS preference (see FTR-006); the node can evaluate YaCy-compatible sender-side DHT gates from runtime state and environment flags, expose those gates as JSON on the ops listener, require a YaCy-compatible public endpoint self-test before DHT distribution, feed an empty outbound queue from bounded stored RWI selections only after sender gates are open, durably journal selected postings before deleting them for queue handoff, recover pending selected postings during node storage startup, restore transferable rows when enqueue cannot keep them, clear recovery records after accepted handoffs, probe a peer's `/yacy/query.html?object=rwicount` capacity, choose YaCy-compatible DHT transfer targets from reachable peers, filter peers that do not advertise remote-index intake, apply the upstream age gate, clear a clashing rejected target's remote-index flag, use YaCy freeworld senior redundancy and vertical partition defaults unless overridden, build bounded outbound exchange chunks by target, run retry-aware outbound scheduler cycles with failed chunks returned to the queue, derive retry and quarantine decisions for failed outbound cycles, confirm successful targets in the peer roster, quarantine repeatedly failing targets out of the peer roster, observe outbound DHT Prometheus counters from scheduler receipts, and perform the two-phase handoff for a known peer: post RWI rows to `/yacy/transferRWI.html`, read `unknownURL`, load matching local URL metadata, and post those rows to `/yacy/transferURL.html`. | Not applicable. | Not applicable. | Reuses YaCy DHT position values, shared DHT target selection, vertical URL partitioning, DHT gate vocabulary, query response values, transfer RWI/URL request/response DTOs, and outbound DHT metric names. | `yagonode/internal/yagonode/*_test.go`, `yagonode/internal/dhttarget/*_test.go`, `yagonode/internal/dhtexchange/*_test.go`, `yagonode/internal/indextransfer/*_test.go`, `yagonode/internal/metrics/*_test.go`, `yagonode/internal/peerroster/*_test.go`, `yagonode/internal/rwi/*_test.go`, `yagomodel/dht_position_test.go`, `yagomodel/seed_test.go`, `yagoproto/query_test.go`, `yagoproto/transfer_rwi_test.go`, `yagoproto/transfer_url_test.go` | A two-local-Go-node integration test covers RWI and URL metadata transfer through the real YaCy endpoints, and the live Java interop matrix in `yagonode/test/e2e` proves outbound distribution into a real `yacy/yacy_search_server` peer and inbound Java-initiated distribution from it. |
| FTR-015 | Search | YaCy public search surfaces | Implemented: `/yacysearch.json`, `/yacysearch.rss`, and `/yacysearch.html` accept plain query text, local/global resource selection, pagination, content domain, language, site, in-URL, TLD, filetype, URL mask, preference mask, verify, and navigation parameters. JSON serves an upstream-like `channels[0].items` shape. RSS serves OpenSearch-flavored RSS 2.0 items with per-item Dublin Core `dc:creator`/`dc:publisher`/`dc:subject` sourced from the crawler's extracted author/publisher/keywords document metadata. HTML serves a simple search form and result list with Previous/Next result-page navigation (YaCy `startRecord`/`maximumRecords` paging); the public portal and admin console search pages carry the same prev/next paging over the core's offset window. Result links open in the same tab by default (NN/G); `YAGO_SEARCH_LINKS_NEW_TAB` switches all three human surfaces to new-tab links with an accessible indicator. When result-click capture is enabled (FTR-118), `/yacysearch.html` additionally carries a `navigator.sendBeacon` click hook that keeps result links direct while reporting clicks for ranking; the JSON and RSS surfaces never carry it. Snippets on all three surfaces highlight query terms with `<mark>` (text is HTML-escaped before markup is added, whole-word prefix matching keeps inflected forms highlighted), and result meta lines carry host/date/size. The portal and /yacysearch.html search boxes offer keyboard-accessible autocomplete (WAI-ARIA combobox/listbox pattern, debounced fetch of `/suggest.json`, progressive enhancement — the plain form works without JavaScript), and both pages carry a "Search operators" help block listing the supported query operators (`"phrase"`, `-word`, `site:`, `inurl:`, `tld:`, `filetype:`, `language:`, `/date`). The `/date` modifier now actually re-orders results newest-first across local, remote, and federated search before the paging window is cut. Result lists are finalized before paging: persistent exact/near-duplicate content clusters consolidate to deterministic representatives, MMR spreads similar unclustered results without deleting them, and hosts holding more than two upper-rank slots have their overflow deferred behind other hosts (YaCy `skipDoubleDom`), except for `site:` queries and `/date` ordering where crowding control would defeat the query's intent. `/opensearchdescription.xml` advertises HTML, RSS, JSON suggestion, and XML suggestion URLs for the current public base URL. `/suggest.json` returns OpenSearch suggestion JSON built first from the titles of matching locally-indexed documents (whole, multi-word page titles, deduplicated case-insensitively and length-bounded) and then filled from bounded in-memory recent queries; the suggestion lookup is local-index-only (denylist applied like served results, and never the swarm fan-out or web-search fallback) so typeahead stays offline and private, and a fresh node with no query history still suggests from indexed content. `/suggest.xml` returns YaCy-compatible `SearchSuggestion` XML from the same source. `resource=local` searches the local persistent `SearchIndex` full-text path over stored documents. `resource=global` merges local full-text results with bounded YaCy `/yacy/search.html` fanout to DHT-selected known senior peers for query term hashes using the configured DHT redundancy and partition exponent (targets are drawn from the known-peer set, not only peers a prior hello confirmed reachable, matching YaCy's `RemoteSearch`/`DHTSelection` — so a node behind NAT that never confirms inbound reachability can still search, with unreachable candidates surfacing as partial failures), uses YaCy `indexabstract` negotiation for multi-term remote result conjunctions before secondary `urls=` retrieval, filters remote targets by advertised non-empty RWI inventory, randomly samples redundant eligible DHT candidates, deduplicates by URL identity, and returns partial-failure metadata for unavailable, malformed, or missing DHT target peer responses. Remote results carry local title/URL and peer-reputation evidence, while the federated merge uses rank-only RRF so incomparable local and peer score scales cannot dominate each other. The public search-serving paths (/yacysearch.*, /suggest.json, portal queries) rate-limit per client IP with YaCy's search.public.max.access tiers — 10 requests per 3 seconds, 60 per minute, 300 per ten minutes — returning 429 with Retry-After when exceeded; callers presenting a valid search API key and requests from the local host ride a tenfold raised budget, and asset routes (favicons, thumbnails, cached pages) stay unthrottled. | Not applicable. | Not applicable. | Defines the public search paths, parameter vocabulary, OpenSearch description surface, suggestion shapes, remote search response values, and shared DHT target selection. | `yagonode/internal/searchcore/*_test.go`, `yagonode/internal/searchlocal/*_test.go`, `yagonode/internal/searchremote/*_test.go`, `yagonode/internal/dhttarget/*_test.go`, `yagonode/internal/searchindex/*_test.go`, `yagonode/internal/documentsearch/*_test.go`, `yagonode/internal/yacysearch/*_test.go`, `yagonode/internal/yagonode/*_test.go` | Cross-restart persistent query suggestions remain a separate enhancement; per-`contentdom` thumbnail/media layouts are a deliberate text-first-node simplification, not a wire gap. The live Java interop matrix proves global search returns documents indexed only by a real `yacy/yacy_search_server` peer. |
| FTR-016 | Search | Local search backend boundary | Partial: defines the node-side `SearchIndex` abstraction over document indexing, deletion, search, and stats; opens an embedded persistent Bleve fallback under `YAGO_DATA_DIR/search.bleve`; rebuilds that index from the document store when missing, unusable, or carrying a pre-trigram persisted mapping (bleve pins the mapping at creation; without the rebuild every query referencing the trigram analyzer failed outright — when no rebuild source is available the index is kept and queries simply omit the trigram clauses); indexes each text field both as whole words and as language-agnostic character trigrams (a Unicode-tokenized, lowercased, NFKC-normalized `ngram` field) so a truncated or morphologically varied query still finds an indexed word in any script with no per-language stemmer (for example `зеленски` matches indexed `Зеленский`) — the trigram clause is restricted to the zero-result recovery path because matching a word's trigrams with AND semantics does not require them to be contiguous, so on the ordinary query path a long same-script document contained every common trigram of a query word scattered across its body and flooded results with unrelated content (Russian `черногория` pulled in unrelated Cisco blogs); precise queries now return whole-word matches only and trigram recall widens the set solely when the precise query found nothing (per-language stemming is the planned replacement, tracked as SEARCH-25); crawler ingest updates it before URL metadata and RWI postings are acknowledged; a swarm search caches the metadata of the results peers return into the store and index (YaCy `addResultsToLocalIndex` parity, gated by `YAGO_INDEX_REMOTE_RESULTS`, default on) — metadata only, off the request path, and never overwriting a document already stored so a locally crawled full page is preserved — so the index grows from use and repeat queries answer locally; `searchlocal` adapts it to the shared public search core. The on-disk scorch shards score with Okapi BM25 rather than bleve's default TF-IDF: term-frequency saturation stops a keyword-stuffed page from outranking a concise relevant one, and document-length normalization keeps a long page from accumulating score merely by being long — the scoring model is set on the index mapping and, because a model change needs no reindex, switched in place on already-persisted shards when they open (the in-memory upside-down fallback keeps TF-IDF until it is unified onto scorch with the multilingual reindex). Documents are routed to a per-language analyzer by content language detection (whatlanggo, with the HTML lang attribute as a prior and the dominant Unicode script as a floor): Russian, German, French, Spanish, Italian, Portuguese, Dutch, Scandinavian, Arabic, Persian, Turkish, Hindi, Croatian/Serbian, and others stem with their own Snowball/bleve analyzer, Chinese/Japanese/Korean segment with the CJK bigram analyzer, and a language with no analyzer (Hebrew and the rest) keeps the no-stemming standard analyzer — routed per document via bleve's TypeField. A query is interpreted with the analyzers that serve its own Unicode script (never by identifying the query language, which is unreliable on short strings), so 'черногория' recalls the inflected 'черногории'/'черногорию' through the Russian stemmer without dragging in unrelated same-script pages. Text fields now carry term vectors so phrase and other positional queries work, and the in-memory fallback runs on scorch for BM25 and analyzer parity; an index built under the previous single-analyzer mapping rebuilds from the document store, or keeps its old query path when no rebuild source is available (ADR-0026). | Provides extracted document payloads that the node indexes. | Carries document fields required by snippets, filters, raw-content references, image metadata, and Tavily-compatible responses. | Not applicable. | `yagonode/internal/searchindex/*_test.go`, `yagonode/internal/searchlocal/*_test.go`, `yagonode/internal/documentstore/*_test.go`, `yagonode/internal/crawlresults/*_test.go`, `yagocrawlcontract/*_test.go`, `yagocrawler/internal/pageindex/*_test.go`, `yagonode/doc/adr/0012-use-bleve-for-embedded-full-text-fallback.md` | Bleve is the committed local search backend, tuned for web search (see `yagonode/doc/adr/0018-commit-to-bleve-web-search-backend.md`); RWI remains a compatibility exchange layer. |
| FTR-017 | Agent API | Tavily-compatible search surface | Partial: mounts `POST /search` over the shared search core; accepts current Tavily-like search contract fields including `query`, `search_depth`, `chunks_per_source`, `max_results`, `topic`, `time_range`, `start_date`, `end_date`, `include_answer`, `include_raw_content`, image/favicon flags, domain filters, `country`, `auto_parameters`, `exact_match`, `include_usage`, and `safe_search`; ignores unknown fields for forward compatibility; supports two auth modes — a legacy static `Authorization: Bearer <YAGO_SEARCH_API_KEY>`, or, when `YAGO_SEARCH_REQUIRE_API_KEY` is on, scoped admin-minted API keys (`search:read`, upgraded to `search:raw` for raw content) sharing the admin service's hashed key store, per-key rate limiter, and audit observer, returning `401`/`403`/`429` envelopes; returns Tavily-like `query`, optional empty `answer`, stored page images when `include_images` is requested, `results`, `response_time`, optional `auto_parameters`, local synthetic `usage`, `request_id`, and stable JSON error envelopes. It also mounts `POST /extract`, returning Tavily-like extract results (`results` and `failed_results`) for URLs already in the document store, accepting `urls` as a string or array plus `extract_depth`, `format`, `include_images`, and `include_favicon` under the same auth (requiring `search:raw` in scoped mode) and error envelopes; fetch-on-extract is off by default (opt-in via `YAGO_EXTRACT_FETCH_ENABLED`), so an uncached URL is a controlled `failed_result` with no outbound request; when enabled it fetches through the shared egress-guarded client (private networks default-denied — no SSRF), size- and time-bounded, and extracts the page title and visible text. Response shape mirrors real Tavily payloads: `answer`, `images`, and `follow_up_questions` are present on every response (null / empty array when not requested), the top-level `images` array holds plain URL strings unless `include_image_descriptions` asks for `{url, description}` objects, and error responses carry Tavily's documented `{"detail": {"error": ...}}` envelope alongside the structured `error` object. Recency controls now filter results: explicit start_date/end_date (whole end day included) win over time_range (day/week/month/year windows), then the legacy days parameter, then topic=news's default 3-day window; the bounds flow into the shared search core (the local index filters by document date, with undated documents excluded while a bound is active) and remote/web results are held to the same bounds post-retrieval (both compact and dashed date encodings accepted). The paging session key includes the bounds so cached windows never leak across differently-bounded queries. include_answer now returns a locally synthesized extractive answer: sentences from the top result snippets that mention the query's terms, stitched in rank order, deduplicated, and bounded (300 runes basic / 600 advanced); an empty result set yields an empty answer — nothing is invented and no model is involved (LLM-based generation would need its own ADR). Option fidelity: include_raw_content 'markdown' renders best-effort markdown from the stored document (title and recorded headings as heading lines, list markers preserved) on both /search and /extract; several include_domains now widen retrieval (no single-host narrowing, fourfold overfetch) so the post-filter can fill max_results across all of them, with the response still capped at max_results; and advanced searches honor chunks_per_source by returning up to N query-relevant sentence chunks per source instead of one leading snippet. POST /crawl and POST /map are mounted: a bounded synchronous breadth-first walk over the node's egress-guarded page fetcher (defaults depth 1, breadth 20, limit 50; caps 3/100/200) honoring select/exclude path and domain regex patterns and allow_external (www-insensitive same-site scope otherwise); /crawl returns Tavily's base_url+results envelope (markdown format and favicons on request, raw-scope auth like /extract), /map returns discovered URLs only; the natural-language instructions field is accepted and ignored (no model interprets it), and nodes with fetch-on-extract disabled answer 503. tavily-python client.crawl()/map() now work against this node. | Not applicable. | Supplies extracted document content and image metadata that can satisfy `raw_content` and image response fields after ingest. | Not applicable. | `yagonode/internal/tavilyapi/*_test.go`, `yagonode/internal/yagonode/node_public_search_test.go`, `yagonode/internal/compatibility/*_test.go` | Basic, fast, and ultra-fast depths use local search; advanced depth includes DHT-selected reachable-peer search. Local answer generation, image ranking/search, real usage accounting, the `search:admin` scope, a strict public-mode rate limit, and readability-grade extraction (boilerplate removal and images) for fetched pages remain planned. `POST /search` stays a drop-in Tavily surface: when the DDGS fallback lands, its results are returned here unmarked, while the human search surfaces (the public search portal, the admin search UI, and `/yacysearch.*`) label them with a `web` provenance badge instead. Audited against docs.tavily.com (2026-07): remaining known deltas — `time_range`/`start_date`/`end_date`/`topic`/`country` are accepted but do not steer results, `include_answer` returns an empty string (no local answer synthesis), `include_raw_content: "markdown"` returns plain text, multi-domain `include_domains` post-filters after retrieval, and `POST /crawl` / `POST /map` are not mounted. |
| FTR-033 | Peer news | YaCy news pool and seed attachment | Partial: a vault-backed peer news pool keeps the YaCy incoming, processed, outgoing, and published queues; own publications carry the upstream record wire form (`{key=value,...}` with `ori`, `cat`, `cre`, `rec`, `dis`), the id offset attribute `#`, and duplicate suppression; each publication is offered thirty times and then rests in the published queue; incoming records are validated against the upstream category whitelist. Each announcement cycle rotates the next publication into the advertised seed `news` attribute in the YaCy `b\|` wire form, and news attachments arriving on same-network hello callers and on seeds returned by greeted peers are decoded and enqueued. | Not applicable. | Not applicable. | Provides the seed `news` attribute and the base64 wire form codec. | `yagonode/internal/peernews/*_test.go`, `yagonode/internal/peeradmission/hello_news_test.go`, `yagonode/internal/peerannouncement/announce_news_test.go`, `yagonode/internal/nodestatus/node_report_test.go` | Automatic processing of incoming news categories (profile updates, crawl starts) into local state remains planned. |
| FTR-018 | Security | Admin authentication | Implemented: gates every operations endpoint except `/health` and `/ready` behind an admin session. The single administrator is provisioned by `YAGO_ADMIN_USER`/`YAGO_ADMIN_PASSWORD` at start or by `POST /api/admin/v1/auth/setup` on first run (allowed only while no admin exists), and the password is stored as an Argon2id hash (ADR-0015). `POST /api/admin/v1/auth/login` verifies the password with a constant-time compare and timing equalized against a missing account, then returns an `HttpOnly` `SameSite=Strict` session cookie plus a CSRF token; login is rate limited per client. `logout` invalidates the durable server-side session, `GET /api/admin/v1/auth/session` returns the current administrator, and unsafe methods require the `X-CSRF-Token` header to match the session. There is no default password and a failed login does not disclose whether the account exists. | Not applicable. | Not applicable. | Not applicable. | `yagonode/internal/adminauth/*_test.go`, `yagonode/internal/yagonode/node_admin_auth_test.go`, `yagonode/internal/compatibility/*_test.go`, `yagonode/doc/adr/0015-argon2id-admin-password-hashing.md` | Server-rendered login and first-run setup pages ship in FTR-030, in-console password change and API-key management in FTR-041, and the default-deny CORS policy in FTR-020. Automatic session-key rotation remains planned. |
| FTR-019 | Security | Admin API keys and scopes | Implemented: an `Authorization: Bearer` API key authenticates operations requests as an alternative to the session cookie. `POST /api/admin/v1/auth/api-keys` mints a high-entropy key with an explicit scope set, returns the secret exactly once, and stores only its SHA-256 hash beside a public identifier; `GET` lists key metadata and last-used time without the secret; `DELETE /api/admin/v1/auth/api-keys/{id}` revokes by identifier. The guard resolves the scope a path needs (`admin:read` for safe methods, `admin:write` for unsafe methods, `crawl:write` for `POST /crawl`), grants every scope to a cookie session, checks the required scope for a key, rate limits requests per key, and requires no CSRF token on the Bearer path. Scopes are `admin:read`, `admin:write`, `crawl:write`, and the reserved `search:read` and `search:raw`. | Not applicable. | Not applicable. | Not applicable. | `yagonode/internal/adminauth/api_key_store_test.go`, `yagonode/internal/adminauth/api_key_endpoints_test.go`, `yagonode/internal/adminauth/guard_apikey_test.go`, `yagonode/internal/compatibility/*_test.go` | Per-key scope enforcement now backs the public `POST /search` and `POST /extract` surfaces when `YAGO_SEARCH_REQUIRE_API_KEY` is set (FTR-017), sharing this hashed key store, per-key rate limiter, and audit observer; in-console key management ships in FTR-041. |
| FTR-020 | Security | Cross-origin request policy | Implemented: a default-deny CORS policy fronts each listener. A request with no `Origin` header — every `/yacy/*` peer call and any same-origin request — passes through untouched, so enabling CORS never changes peer behavior. The operations surface uses a credentialed policy from `YAGO_ADMIN_CORS_ORIGINS` that echoes an allowlisted origin and sets `Access-Control-Allow-Credentials` (a literal `*` is never sent with credentials); the public search endpoints use an uncredentialed policy from `YAGO_SEARCH_CORS_ORIGINS` that may use `*`. Preflight `OPTIONS` requests are answered with `204` and the allowed methods, headers, and max-age when the origin is allowlisted, and without CORS headers otherwise. The peer, public search, and ops listeners bind separately (FTR-043), so the admin surface can sit on loopback behind a reverse proxy while the P2P protocol and the public search surface stay reachable. | Not applicable. | Not applicable. | Not applicable. | `yagonode/internal/crossorigin/*_test.go`, `yagonode/internal/yagonode/*_test.go` | Per-route CORS overrides and a configurable preflight max-age remain planned. |
| FTR-021 | Security | Egress private-CIDR allowlist | Implemented: the shared egress guard screens outbound connections by resolved IP at dial time and blocks private networks by default. Beyond the all-or-nothing `YAGO_EGRESS_ALLOW_PRIVATE_NETWORKS` / `YAGOCRAWLER_ALLOW_PRIVATE_NETWORKS` toggle, `YAGO_EGRESS_ALLOW_CIDRS` (node) and `YAGOCRAWLER_ALLOW_CIDRS` (crawler) name specific private CIDRs to admit, so intranet mode reaches only chosen ranges instead of all RFC 1918 and unique-local space. The allowlist relaxes only the private-range check: loopback, link-local including the cloud metadata range, carrier-grade NAT, multicast, and reserved ranges stay blocked, so a non-private allowlist entry never opens them. | Enforced identically for crawler page and sitemap fetches, including the headless browser proxy. | Not applicable. | Not applicable. | `yagoegress/*_test.go`, `yagonode/internal/yagonode/node_config_test.go`, `yagocrawler/cmd/yagocrawler/crawler_config_test.go` | Per-target DNS-rebinding revalidation windows and richer intranet-mode policy remain planned. |
| FTR-022 | Operations | Structured event log | Records recent node events in a bounded in-memory ring keyed by severity (debug, info, warn, error) and category (p2p, dht, search, crawl, storage, security, config), and serves them newest-first as JSON at `GET /api/admin/v1/events` with an optional `limit`; a node-started config event is recorded on boot and admin login and API key auth outcomes are recorded as security events through the shared auth observer without revealing credentials. | Not applicable. | Not applicable. | Not applicable. | `yagonode/internal/events/*_test.go`, `yagonode/internal/yagonode/events_endpoint_test.go`, `yagonode/internal/yagonode/node_auth_events_test.go` | The endpoint requires an admin session or an `admin:read` API key. Recent events now survive restarts through a bounded, vault-backed durable event store (FTR-034, OPS-02) that reseeds the in-memory ring at boot, storing only severity, category, name, message, and time — never secrets; broader p2p/dht/search/crawl/storage emitters remain planned. |
| FTR-023 | Search | Optional DDGS web-search fallback | Implemented, off by default: a `websearch.FallbackSearcher` decorates the federated searcher so the Tavily `POST /search` API and the human `/yacysearch.*` surfaces share one fallback. On a true local-plus-federated miss and only while enabled (`YAGO_WEB_FALLBACK_ENABLED`), it queries a keyless, structure-driven multi-engine metasearch provider whose default `auto` backend excludes DuckDuckGo (Mojeek then Bing; DuckDuckGo is opt-in because it hard-blocks automated queries), routes outbound through the egress guard, caches responses briefly, backs off on `202`/`429`, and degrades to an empty result on failure. Fallback hits are stamped with the `ddgs` result source: the human surfaces label them with a `web` provenance badge (alongside `local` and `peer`) while the Tavily drop-in returns the same results unmarked. When `YAGO_WEB_FALLBACK_SEED_CRAWL` is on and crawling is enabled, a `webCrawlSeeder` publishes one conservative, domain-scoped crawl order per newly discovered URL (idempotent by URL, skipping URLs already indexed) so the next identical query is answered locally. | Adds a conservative search-miss crawl seeding path guarded by `YAGO_WEB_FALLBACK_SEED_CRAWL`; nil-safe when crawling is disabled. | Adds the `ddgs` result source (`searchcore.SourceWeb`). | Not applicable. | `yagonode/internal/websearch/*_test.go`, `yagonode/internal/tavilyapi/web_fallback_marking_test.go`, `yagonode/internal/yagonode/node_web_fallback_test.go`, `yagonode/internal/yagonode/node_web_seed_test.go`, `yagonode/internal/yagonode/node_web_fallback_config_test.go` | Configured via `YAGO_WEB_FALLBACK_*`. The runtime admin toggle (flip without restart) and the SEC-05 tri-state privacy mode remain planned; see `yagonode/doc/adr/0019-ddgs-web-search-fallback.md` and `0021-in-house-metasearch-backend.md`. SEARCH-33: the auto backend adds the DuckDuckGo endpoints as a tail resort — reached only when Mojeek (little non-English coverage) and Bing (bot-walls datacenter addresses) both yielded nothing, which used to leave Cyrillic queries with an empty fallback; empty provider answers are no longer cached (an intermittent bot-wall pinned the miss for the whole TTL); each engine attempt is debug-logged. Each engine’s answer must also mention a content word of the query before it is accepted (per-engine acceptance hook): Bing’s bot-tier response answers only the first query word with plausible-looking dictionary pages, which used to satisfy the engine loop while post-hoc verification then emptied the SERP — an off-topic answer now counts as empty and the loop walks on to an engine that can actually answer. SEARCH-34 (operator request): the auto chain is reordered by answer quality — DuckDuckGo (html+lite) first as the only keyless engines with solid multilingual coverage, then a new Brave engine (its server-rendered page marks organic results with data-type="web"; parser verified against a live 20-result page), then Mojeek and Bing as the tail; backoff became per-engine, so a rate-limited DuckDuckGo pauses only itself while the rest of the chain keeps answering; the backend choice gains admin parity (`web.fallback.backend`: auto/ddg/brave/mojeek/bing). Google, Yandex, Startpage, Qwant, and Ecosia were probed live and are not keyless-viable (consent walls, SmartCaptcha, DataDome); they need API keys or a self-hosted SearXNG bridge — follow-up material. |
| FTR-024 | UI | Server-rendered admin console foundation | Implemented: mounts a server-rendered operator console at `/admin/` on the operations listener behind the existing admin session guard (ADR-0022). It renders a Carbon-styled app shell — dark header, section side navigation, content area, keyboard skip-link, and an AGPL source notice — for the Overview, Search, Crawler, Network, Index, Performance, Configuration, Security, and Logs sections, with `/admin/` redirecting to `/admin/overview`. Sections that are not yet wired render a controlled unavailable state so the shell renders without any backing API; the Overview section renders live node status — documents, RWI words, known peers, uptime, peer identity, and index-transfer totals — read directly from the node status report, with an htmx partial (`/admin/overview/metrics`) that auto-refreshes every fifteen seconds (the uptime is rendered at second granularity — the console reads a seconds-precise uptime rather than the minute-granularity value the YaCy seed carries — so it visibly advances on every refresh instead of appearing frozen between minute ticks) plus a manual refresh button, degrading to the same server-rendered tiles without JavaScript and to a controlled unavailable state when no status source is wired. Pages are styled with hand-authored CSS built on IBM Carbon design tokens (system-font IBM Plex fallback, no external fetch) and progressively enhanced with a single vendored, pinned `htmx.min.js` (2.0.4) via `hx-boost`; both are embedded with `go:embed` and every page works with JavaScript disabled. Responses set a strict `Content-Security-Policy`, `X-Content-Type-Options: nosniff`, an active-nav `aria-current`, and cacheable embedded assets. The header's Public search link resolves to the public portal — the configured public base URL (FTR-036) when set, otherwise the request's own host with the public listener's port — and is omitted entirely when the public surface is disabled, rather than pointing back at the admin origin; the resolved link is injected per request via a layout envelope so no page-data struct carries it. The keyboard focus ring uses `:focus-visible` so it no longer sticks on buttons and header links after a mouse click, and the Sign out button matches the sibling header links' size so the header row no longer jumps. The header wordmark renders `yago` as a two-tone logo — `ya` in red, `go` in white — that links to the project's GitHub repository in a new tab (`rel="noopener noreferrer"`), with a muted `admin` label beside it. The header keeps only two right-aligned controls, both icon-only (labelled for assistive tech): Public search (a globe linking to the public portal) and Sign out (a logout glyph); the redundant Security link is dropped (it already lives in the nav) and Restart moved into the section navigation. The graceful-restart interstitial is styled to match the Photon theme rather than the old Carbon card. The Restart section restarts the node and, when a crawl fleet is reachable, offers a separate "Restart crawlers" action that signals every connected crawler over the gRPC control plane and reports how many were signalled while the node keeps running (FTR-031). A `photon.css` layer, loaded after `carbon.css`, re-skins the console to resemble the QNX Neutrino Photon microGUI: a light-blue active titlebar, neutral-grey widgets with Motif-style raised/sunken bevels over a blue-grey desktop, and the section navigation moved to the right as a raised "shelf" panel with per-section QNX-flavoured inline-SVG icons (a sprite in the layout, referenced by `NavItem.Icon`). Every control shares one height and text size so the widgets stay proportional. The theme is a token-and-component override — `carbon.css` is untouched — and stays a single deliberate visual world (no dark variant). A dedicated Public portal nav section (`GET`/`POST /admin/portal`, between Configuration and Security) groups the portal-facing surface into three progressively-enhanced ARIA tabs: Configuration renders the portal-facing runtime settings (the `settingCategory` "Public portal" group — OpenSearch/public base URL, robots policy, HTTPS redirect, greeting — filtered from the catalog by category rather than a hardcoded key list and gated so only those keys write here), while Portal design and Results design are accessible tabpanels scaffolded ahead of the operator-editable Handlebars page editor approved in ADR-0033. | Not applicable. | Not applicable. | Not applicable. | `yagonode/internal/adminui/*_test.go` | Every console section is now wired to a live provider: search (FTR-025), index (FTR-026), network (FTR-027), logs (FTR-028), crawler (FTR-031), configuration (FTR-036), security (FTR-041), and performance (FTR-042); the login/first-run pages ship in FTR-030 and the public search portal (FTR-029) is a separate server-rendered surface. |
| FTR-025 | UI | Admin search console | Implemented: the console's Search section (`GET /admin/search`) runs queries against the node's shared search core — the same searcher that backs `/yacysearch.*` and the Tavily API, including the optional DDGS web-fallback. A server-rendered Carbon form submits a plain `GET` (query `q`, plus a `scope` selector that defaults to `global` — local index plus the peer network — with `scope=local` restricting the search to this node's own index) and renders the results list with no client JavaScript, while `hx-boost` enhances navigation. The shared searcher parses the raw query into word-hash terms, so the peer-network fan-out targets the right DHT positions from the admin console and public portal just as it does from `/yacysearch` and the Tavily API. DDGS web-fallback hits are labelled with a `web` source in the result's metadata line on this human surface (ADR-0019), whereas the Tavily `POST /search` API keeps returning the same results unmarked. Partial peer failures render as inline notifications, and a backend error renders a generic "Search failed." message without leaking detail. Results page with prev/next navigation over the core's offset window (`?p=`, 20 per page, scope preserved in pager links), snippets highlight query terms with `<mark>` (escaped before markup), and the meta line carries URL, date, page size, and result source. Result links open in the same tab by default (NN/G) with `rel="noreferrer nofollow"`; `YAGO_SEARCH_LINKS_NEW_TAB` switches to `target="_blank"` with `rel="noopener noreferrer nofollow"` and an accessible new-tab indicator. The query box offers the same keyboard-accessible autocomplete as the public surfaces (WAI-ARIA combobox over a listbox, debounced fetch, progressive enhancement) served CSP-compliantly as a static asset script — the console CSP forbids inline scripts — against GET /admin/search/suggest, an ops-listener endpoint backed by the same local-only, denylist-filtered index suggest source as /suggest.json (session/API-key guarded like every admin route). | Not applicable. | Not applicable. | Not applicable. | `yagonode/internal/adminui/*_test.go`, `yagonode/internal/yagonode/*_test.go` | Query explain and richer filter UI (content domain, language, site) remain planned. |
| FTR-026 | UI | Admin index section | Implemented: the console's Index section (`GET /admin/index`) renders search-index statistics — indexed document count, backend, last-updated time, the full-text index's current on-disk size, and the data vault's on-disk usage against its configured maximum (`YAGO_STORAGE_QUOTA`, shown as "unlimited" when unset) — plus four operator tools. A **term browser** looks up a word's RWI posting count, word hash, and a bounded sample of carrying documents (`?term=`). A read-only **schema view** lists the fields each index stores. A **document browser** searches stored documents by URL substring and/or domain (`?q=`/`?domain=`) into a bounded, newest-first table. **Delete-by-URL and delete-by-domain** (`POST /admin/index/delete`, CSRF + htmx confirm) retract a document from every store lineage it participates in — the Bleve index, the document store, and the RWI postings + URL metadata. A **blacklist manager** (`POST /admin/index/blacklist`) adds, lists, and removes URL/domain denylist entries (FTR-039). Each tool renders only when its backing store is wired; the section degrades to a controlled unavailable state when the index is absent. | Not applicable. | Not applicable. | Not applicable. | `yagonode/internal/adminui/index_test.go`, `yagonode/internal/yagonode/node_admin_index_delete_test.go`, `node_admin_blacklist_test.go`, `node_admin_documents_test.go`, `node_admin_terms_test.go` | Full-index rebuild controls and richer per-document inspection remain planned. |
| FTR-027 | UI | Admin network section | Implemented: the console's Network section (`GET /admin/network`) renders the peer network read directly from internal state — known and reachable peer counts, whether the DHT distribution gate is open (with the blocking reason when it is closed), an "Advertised to swarm" block listing every seed capability flag this node publishes in its own seed DNA with its on/off state (direct, remote-crawl, remote-index, root, ssl — read live from the advertised self seed, so the operator sees exactly what peers receive in hello/seedlist), a per-gate results table, and the peer roster rendered as a sortable, paginated table (name, type, address, flags, RWI count, last seen, health, age, hash): every column except flags and hash — peer, type, address, RWI count, last seen, health, age — is a server-side sort link that toggles ascending/descending (numeric and recency columns open descending) and is `aria-sort`-annotated, where the address column sorts by parsed IP (numeric order, not lexical, with hostnames last) and last seen by the real timestamp behind the humanized label; the roster pages fifty peers at a time through `psort`/`pdir`/`ppage` query parameters anchored back to the table, so a large swarm no longer renders as one unbounded wall; all of it works with JavaScript off (UI-31, UI-32). It degrades to a controlled unavailable state when no network source is wired. | Not applicable. | Not applicable. | Not applicable. | `yagonode/internal/adminui/*_test.go`, `yagonode/internal/yagonode/*_test.go` | A per-peer detail page (`GET /admin/network/peer`) shows a peer's published profile and transfer counters; CSRF-guarded block/unblock (`POST /admin/network/peer/block`, FTR-038) and operator seedlist refresh (`POST /admin/network/seedlist/refresh`) are wired, and a read-only peer-news console renders the YaCy news pool (FTR-033). The network self-test control remains planned. |
| FTR-028 | UI | Admin logs section | Implemented: the console's Logs section (`GET /admin/logs`) renders the node's recent structured events — time, severity, category, event name, and message — newest-first from the in-memory events recorder (FTR-022), as a Carbon table with severity tags. An htmx partial (`GET /admin/logs/events`) auto-refreshes every ten seconds with a manual refresh button, and the table works as plain server-rendered HTML without JavaScript. | Not applicable. | Not applicable. | Not applicable. | `yagonode/internal/adminui/*_test.go`, `yagonode/internal/yagonode/*_test.go` | The section now filters by severity and category (an exact, case-insensitive GET form whose selection rides the htmx auto-refresh URL), and the events it shows survive restarts through the durable event store (FTR-034). A client-side log viewer with time-range chips remains planned (UI-13). |
| FTR-029 | UI | Public search portal | Implemented, off by default: when `YAGO_PUBLIC_SEARCH_UI_ENABLED` is set, a server-rendered public search portal replaces the landing page at the public listener's root (`/`). It is a minimal early-2000s-style page — a centered wordmark, one search box and button, and a plain results list — that works without JavaScript and in legacy browsers, is responsive down to phone widths, and is self-contained (inline styles, no external assets or fonts). It runs queries against the node's shared search core (local plus peer network, DDGS fallback included) and labels fallback hits with a `web` provenance badge (ADR-0020, ADR-0019); it exposes only search and never admin APIs and does not log the query text. Results page with prev/next navigation (`?p=`, 10 per page, capped at 50 pages), snippets highlight query terms with `<mark>` (escaped before markup) and meta lines carry URL, date, and page size; a collapsible "Search operators" help block lists the working query operators. The search box is a WAI-ARIA combobox with keyboard-accessible autocomplete over `/suggest.json` as a progressive enhancement — the plain form still works without JavaScript. Autocomplete suggests whole, multi-word titles of matching locally-indexed pages (local-index-only, never the swarm or web fallback), so it works on a fresh node with no prior query history. Result links open in the same tab by default with `rel="noreferrer nofollow"`; `YAGO_SEARCH_LINKS_NEW_TAB` switches to new-tab links with `rel="noopener noreferrer nofollow"` and an accessible indicator. When the toggle is off the portal is not mounted and the landing page is served instead. After a query the portal links the machine-readable formats for the same search (RSS and JSON via /yacysearch.{rss,json}, plus the OpenSearch description) both as a visible footer line and as a <link rel="alternate"> feed hint, and each locally indexed result carries a "cached" link to GET /cached?u=<url> — a read-only, escaped rendering of the stored page text with the fetch date and a live-page link (peer and web-fallback results have no stored copy, so they carry no link). /yacysearch.html carries the same cached links and visible RSS/JSON format line. The results meta line shows the honest total result count together with the measured query time (Doherty-style latency feedback), and submitting a query swaps the results region for shimmer skeleton placeholders until the new page arrives (progressive enhancement — no-JS visitors just get the classic reload; the shimmer respects prefers-reduced-motion). /yacysearch.html shows the same count-plus-time line. P2P transparency: every portal result carries a colored provenance badge (local — this node's index, peer — another swarm node, web — the external fallback), the results meta line breaks the page down by source ('X from this node · Y from peers · Z from the web'), the footer explains that global searches send query terms to peers, and /yacysearch.html labels peer results with a [peer] marker. Provenance is classified through searchcore.Result.{FromPeer,FromWeb,StoredLocally} — the same predicate that gates cached-copy links, since local hits inside a global search carry the request's global source value. Accessibility: a skip-to-results link precedes the page (visually hidden until focused, targeting the focusable results region), the result count / searching / no-results states announce via role=status (errors via role=alert), results render as a semantic list, keyboard focus is marked with a visible :focus-visible outline, and pager links meet the 24px target-size minimum (WCAG 2.4.1/4.1.3/2.4.7/2.5.8); /yacysearch.html carries the same skip link and status count. Mobile-first behavior: under the 48rem breakpoint the results page pins the search bar to the top for thumb re-query (sticky, operator-help collapsed), search controls grow to the 44px touch minimum, and pager links spread to the screen edges with enlarged touch targets; the layout stays single-column from 320px up. Bottom-sheet filter controls arrive with the faceted sidebar. Each result shows its host's favicon to the left of the title, served through this node's favicon proxy (GET /favicon?host=): the icon is fetched over the guarded egress client with the branded User-Agent, size-bounded, sniffed to raster types only (SVG rejected), cached in an in-memory LRU bounded to 256 MiB of icon bodies with the bodies deduplicated by content digest (hosts sharing one icon — CDN defaults, subdomain families — hold references to a single stored copy; negative entries cost no bytes and a 64k-entry backstop bounds their bookkeeping) under positive/negative TTLs, and replaced by a neutral built-in placeholder on any failure — so result hosts never see the searcher's browser before a click (the SERP-03 favicon remainder; YaCy's ViewImage-proxy privacy property). Result dates render in canonical human form ('Wed, 20 May 2026') on the portal and /yacysearch.html via searchcore.Result.DisplayDate. Zero-result recovery (YaCy DidYouMean parity): when a query with parsed terms finds nothing, the shared search pipeline retries once with edit-distance-1 fuzzy matching against the local index (denylist still applied; remote fan-out unaffected); recovered pages render under a 'no exact matches — showing close matches instead' status line, and a 'did you mean' link offers the query respelled from the closest words (Levenshtein distance 1–2) of the recovered titles. An honest empty answer stays empty when even the fuzzy retry finds nothing. Applies to the portal and /yacysearch.html alike. Query operators now include author: (matches the page author metadata the crawler extracts from article:author / meta-author tags, case-insensitive substring) and near (all query words must fall inside one small token window of the page text — a post-retrieval proximity filter, since the vendored bleve match-phrase query carries no slop). Raw-query surfaces (the portal and the admin console) now apply every parsed operator to the request and strip operator tokens from the index query, closing the gap where operators only worked on the surfaces that parsed parameters themselves. Paging is session-stable (YaCy SearchEvent/SearchEventCache parity): page one runs one deep federated search (up to 200 assembled results) and caches the assembled list in a bounded per-query session (TTL five minutes, LRU across queries); deeper pages slice that session instead of re-running the fan-out — whose random DHT peer sample used to reshuffle every page — and the displayed total is the honest pageable count rather than the raw index-plus-peers estimate, so pager links never lead to an empty page. Re-running page one refreshes the session. The pager shows up to ten numbered page links around the current page (aria-current marked) on the portal and /yacysearch.html. Faceted navigation: the portal results page shows a filter sidebar (fieldset/legend per dimension inside a collapsible details block — the mobile bottom-sheet pattern; a two-column grid from the 48rem breakpoint) over backend facet counts computed by the local index across every matching document AFTER the request filters, so the numbers agree with what paging can reach: host, file type, language, author, protocol, and month (top 8 terms each). Clicking host/file type/language/author re-runs the query with the matching operator appended (site:/filetype:/language:/author:), reusing the operator filters; protocol and month render as plain counts. A filter-only query with no keyword (a bare `filetype:pdf`, `site:`, `tld:`, or `inurl:`) stays keyword-seeded as in YaCy — retrieval walks the postings by word hash, so it returns nothing rather than scanning the whole index — and the portal and admin console show a one-line hint to add a search word when such a query matches nothing (ADR-0042; facet clicks remain the zero-friction way to narrow a keyword query by type). Facets describe the local corpus only (peers report no counts) and ride the paging session so every page shows the same sidebar. Topics facet remains planned. Content verticals: tab links (All / Images / Audio / Video / Apps) above the results switch the search's content domain — the media domains filter by the document's file type — classified from its Content-Type with the URL extension as a fallback, so an extension-less media URL still classifies (ADR-0042) — and the image vertical keeps only pages with extracted images. The Images tab renders a responsive thumbnail grid (lazy-loaded) with a no-JS CSS lightbox (:target overlay showing the full image, its alt text, and a link to the source page); thumbnails and full views load through the node's bounded page-image proxy (GET /imgproxy?u=, guarded egress, 512 KiB cap, raster-only, its own 64 MiB LRU) so pictured hosts never see the searcher before a click. The same contentdom values now actually filter on /yacysearch.* and the Tavily API, which already transported them. | Not applicable. | Not applicable. | Not applicable. | `yagonode/internal/publicportal/*_test.go`, `yagonode/internal/yagonode/node_public_portal_test.go`, `yagonode/internal/cachedpage/*_test.go`, `yagonode/internal/faviconproxy/*_test.go`, `yagonode/internal/searchsession/*_test.go` | The portal's own OpenSearch suggest stub stays privacy-preserving and empty (SEC-05); the autocomplete uses the yacysearch suggestion endpoint on the same public listener. A per-result 'why this result' ranking explanation on the public surfaces remains planned (the admin explain endpoint already exists). |
| FTR-030 | UI | Admin auth pages | Implemented: server-rendered login (`GET`/`POST /admin/login`) and first-run administrator setup (`GET`/`POST /admin/setup`) pages, plus a CSRF-protected sign-out action (`POST /admin/logout`) surfaced in the console header. The pages are self-contained (inline styles, work without JavaScript, redirect on success) and reuse the existing auth service (Argon2id verify, per-caller login rate limiting, session issue, timing equalization), so the HTML flow carries the same guarantees as the JSON auth API (FTR-018). The session guard redirects an unauthenticated browser navigation to the login page — or, while no administrator account exists yet, to the first-run setup page (and the login page itself redirects there on a fresh node) so an operator is guided to create the first admin instead of being stranded on a login form for an account that does not exist — while still answering programmatic requests with 401, and accepts the session CSRF token from either the `X-CSRF-Token` header or a `csrf_token` form field so server-rendered write forms work without JavaScript; `adminauth.CSRFTokenFromContext` exposes that token to the console. The login and setup pages are guard-exempt; logout requires a valid session and CSRF token. The first-run setup page is a node-mode wizard: alongside the administrator account it offers three modes — local-only (default: private index, public portal off), public peer (join the swarm, portal off), and public search node (swarm plus the public portal) — with the critical settings prefilled (the current peer name — the friendly name advertised to the swarm, left blank to keep the node's generated name; autodetected advertise host, effective seedlist URLs — falling back to the canonical public seeds when the node has none configured, so a fresh bare-metal install is not left with an empty seed list and no peers to bootstrap from, DDGS web-fallback privacy). Choices persist through the same validated runtime-settings path the console uses (the peer name only when the operator actually names the node, so an empty field never overwrites the generated name); if applying them fails the administrator account still stands and the login page says the node-mode step needs redoing in the console. | Not applicable. | Not applicable. | Not applicable. | `yagonode/internal/adminauth/auth_html_test.go`, `yagonode/internal/adminauth/guard_html_test.go`, `yagonode/internal/adminui/admin_ui_test.go` | The in-console API-key management page and a password-change page remain planned; API keys are managed through the JSON auth API (FTR-019) and shown once at creation. |
| FTR-031 | UI | Admin crawler section | Implemented: the console's Crawler section (`GET`/`POST /admin/crawl`) lets an operator start a crawl from a server-rendered form — seed URLs (one per line), an optional name, start mode (single URL, sitemap, site list, robots.txt), scope (same domain, sub-path, wide), and a bounded maximum depth. The POST is a CSRF-protected form (via the `csrf_token` field, so it works without JavaScript) that reuses the same crawl dispatcher as the JSON `POST /crawl` endpoint (a new `crawldispatch.Dispatcher` seam shared by both), builds and validates a crawl profile, and enqueues the order on the durable crawl broker; the page then confirms acceptance (seed count and profile handle, noting a duplicate) or shows a failure notice. The section renders a controlled unavailable state when crawling is disabled on the node. The crawl form defaults to crawling query-parameter URLs and to the TLS-authority opt-out (both checkboxes pre-checked; mis-chained certificates and query-routed sites otherwise silently empty a crawl), and the monitor offers a Restart action on finished, failed, or cancelled runs that re-dispatches the run's profile as a fresh run. A 'Document formats' block (POST /admin/crawl/formats, CSRF form) manages the shared format-family toggles every crawl uses — text, XML+feeds, PDF/PS, Office, images, audio, vCard/torrent/APK, archives — persisted in the vault and stamped into each dispatched crawl profile (operator crawls, recrawls, and swarm seeds alike); defaults enable everything except archives. | Consumes the dispatched crawl orders. | Not applicable. | Not applicable. | `yagonode/internal/adminui/admin_ui_test.go`, `yagonode/internal/yagonode/node_admin_crawl_test.go`, `yagonode/internal/crawldispatch/*_test.go` | The section now offers expert profile options behind a simple/expert toggle (regex crawl/index must-match and must-not-match filters, per-host page cap, crawl delay, recrawl-if-older, allow-query-URL, follow-nofollow, and noindex-on-canonical-mismatch toggles), a live run monitor (`GET /admin/crawl/monitor`, htmx auto-refresh every 5s) showing running runs with per-outcome tallies, order-queue depth, and a results/rejections rollup, and per-run pause/resume/cancel and pages-per-minute controls (`POST /admin/crawl/control`) that the worker applies within a few seconds — directives ride the worker heartbeat, whose interval is kept short so a cancel takes visible effect promptly rather than after up to half a minute — plus a whole-worker restart directive (`CRAWL_CONTROL_KIND_RESTART`) the Restart section (FTR-024) broadcasts to every connected crawler: the node tracks live order-stream registrations, queues a one-shot restart per worker drained on its next heartbeat, and the crawler shuts down gracefully and exits with a non-zero restart code so its supervisor brings it back up (leased orders return to the durable queue and resume after reconnect) — laid out on a single control row (the rate field is compact, not stretched, and pre-fills with the run's last operator-applied rate — remembered node-side and preserved across worker progress reports — so the current throttle is visible and editable; a run with no operator rate paces at the crawler's polite 30-pages-per-minute default, and an explicit zero unleashes it) — natively covering YaCy's `/Crawler_p.html` function. A named crawl-profile CRUD editor and per-URL result inspection remain planned. |
| FTR-032 | UI | Admin configuration section | Implemented: the console's Configuration section (`GET /admin/configuration`) shows the node's effective configuration grouped into Node, Listeners, Storage, Search, Network policy, Crawler, and Administrator, as server-rendered tables. Values are snapshotted from the startup configuration; secrets are redacted before they reach the console — the search API key and the administrator password render only as `Configured` or `Not set`, never as their values, and an unset administrator username renders as `Not configured`. The same section also hosts the runtime write surface: an editable settings form and a listener-rebinding form (`POST /admin/configuration`, FTR-036). The section renders a controlled unavailable state when no configuration source is wired. | Not applicable. | Not applicable. | Not applicable. | `yagonode/internal/adminui/admin_ui_test.go`, `yagonode/internal/yagonode/node_config_view_test.go` | This row covers the read-only effective-config view; the runtime-editable settings store and listener rebinding are catalogued in FTR-036. |
| FTR-034 | Operations | Durable event persistence | Implemented: a vault-backed `eventstore` keeps a bounded, monotonic-sequence log of recent structured events (default cap 512, oldest pruned past the cap), storing only severity, category, name, message, and timestamp — never secrets. At startup `provisionObservability` opens the store, seeds the in-memory recorder ring (FTR-022) with the events that survived the last restart, and installs a best-effort write-through sink so each new event persists outside the recorder lock. | Not applicable. | Not applicable. | Not applicable. | `yagonode/internal/eventstore/*_test.go`, `yagonode/internal/events/recorder_test.go`, `yagonode/internal/yagonode/node_events_test.go`, `yagonode/internal/yagonode/events_endpoint_test.go` | The admin Logs section (FTR-028) shows recent important events across restarts, bounded on both memory and disk (OPS-02). Broader p2p/dht/search/crawl/storage emitters remain planned. |
| FTR-035 | Crawling | Recrawl scheduling and durable frontier | Implemented: a durable, node-side recrawl schedule (`recrawlfrontier`) records each crawled URL's profile cadence across three vault buckets (per-URL records, a chronological due index, and profiles), so pages are refetched on schedule and the plan survives node and crawler restarts. `Observe` schedules the next due time from the profile interval; a sweep loop's `ClaimDue` returns URLs due at or before now, soonest-first and bounded per batch, atomically advancing each next-due time forward so a URL is not re-dispatched every sweep, then republishes a fresh single-URL crawl order onto the durable order queue. A default recrawl cadence keeps this schedule from idling: crawls carry a recrawl-if-older interval of thirty days (`yagocrawlcontract.DefaultRecrawlInterval`) unless overridden, so indexed pages are re-fetched on schedule instead of living forever — previously the interval defaulted to zero everywhere, which made `Observe` drop the record and the frontier sit idle. The autocrawler stamps the default onto both its discovery paths (swarm greedy-learning and web-fallback seeding) through the operator-editable `autocrawler.crawl.recrawl_interval` setting on the Autocrawler page; the manual crawl form pre-fills the same default; and the interval accepts operator-friendly `30d`/`2w`/`720h`/`off` spellings (`yagocrawlcontract.ParseRecrawlInterval`, since Go durations cap at hours) bounded to between one hour and a year. A recrawl that finds a page permanently gone now removes it from the index instead of leaving a dead result forever (ADR-0034): when a fetch returns HTTP 404 or 410 the crawler returns a structured `pagefetch.GoneError` (which the browser-fallback source propagates rather than escalating to the headless browser, since the browser would only render the server's error page into a soft-404), the pipeline emits a tombstone — an `IngestBatch` carrying just the source URL with `Removed: true`, no proto change since the batch travels as opaque JSON — through the same durable at-least-once ingest channel, and the node routes the tombstone to an idempotent purge that drops the URL's postings and metadata in one capacity-exempt vault transaction (reusing the eviction purge primitive). Only 404/410 delete; every transient or ambiguous status (403, 429, 5xx, timeouts, network and read errors) is left as a plain failed fetch. The purge still enforces crawl-profile ownership, so a worker cannot delete URLs outside its profiles, and purging an unindexed URL is a harmless no-op, so a redelivered tombstone is safe. | Executes republished recrawl orders like any other order, and signals a permanently-gone page back to the node as a removal batch. | Carries the recrawl crawl order and profile handle, and the removal tombstone as an `IngestBatch` `Removed` flag. | Reuses YaCy URL values. | `yagonode/internal/recrawlfrontier/*_test.go`, `yagonode/internal/yagonode/recrawl_sweep_loop_test.go`, `yagonode/internal/yagonode/recrawl_capture_test.go`, `yagocrawler/internal/pagefetch/gone_error_test.go`, `yagocrawler/internal/pagefetch/fallback_page_source_test.go`, `yagocrawler/internal/pipeline/pipeline_removal_test.go`, `yagonode/internal/crawlresults/ingest_removal_test.go` | Sweep interval and batch are code constants; the per-URL cadence comes from each crawl profile's recrawl-if-older setting. The sweep loop runs only when the crawl runtime is live. |
| FTR-036 | Operations | Runtime-editable configuration and listener rebinding | Implemented: a vault-backed `settingsstore` holds admin-editable runtime overrides (string values; secrets are never stored) that layer onto the env-derived config at boot. Two settings are live-flippable without a restart — `portal.enabled` (public search portal, FTR-029) and `https.redirect` (FTR-037) — each applied through an atomic runtime toggle read on every request and recorded as a config event. Listener bind addresses (`bind.peer`, `bind.public`, `bind.ops`) are also editable and persisted as restart-required overrides, admitting only a host the machine actually has and guarding the ops bind against operator self-lockout; the public listener bind is optional (empty disables it). Changing the peer bind re-derives the advertised port and the local DHT self-test address from the new value so the node announces the port it actually listens on, unless `YAGO_ADVERTISE_PORT` / `YAGO_PUBLIC_SELF_TEST_URL` pinned them. The console Configuration section renders the editable settings and binding forms (FTR-032), grouped into per-category tabs (General, Public portal, Search, Swarm, Crawler, Web fallback, Extraction, Monitoring, Network & peers) alongside a Listen-addresses tab and an Effective-configuration tab, replacing the former single flat sheet; each setting is assigned its tab from its key prefix, tab order is fixed for a stable layout, and the tabs progressively enhance from a plain page that shows every section under its own heading when JavaScript is off (a vendored `tabs.js` wires the ARIA tablist). A 'Public base URL' setting (env YAGO_PUBLIC_BASE_URL, live-applied) pins the absolute public origin used in OpenSearch description documents, suggest templates, and machine-readable result links for reverse-proxy deployments; empty keeps deriving the origin from each request. The editable catalog now covers every non-secret environment knob: peer name, advertised host, seedlist URLs, result-link new-tab behavior, swarm-result caching, Prometheus metrics, query-log mode, the DDGS web fallback privacy toggle, greedy learning (enable), and fetch-on-extract — each validated, persisted in the vault, and layered over the environment at startup, with restart-required settings labeled as such in the console. The Autocrawler section (`/admin/autocrawler`) additionally exposes the manual crawler's per-crawl fetch policy for the two automatic discovery paths (swarm greedy-learning and web-fallback seeding), in the crawler's order: the shared document-format toggles (`POST /admin/autocrawler/formats`, saving the same shared setting the crawler page edits) plus five settings-only booleans — crawl query-parameter URLs (default on), ignore SSL certificate authority (default on), ignore robots.txt, disable browser rendering, and follow rel=nofollow links — that flow into every autocrawler-seeded crawl profile so both discovery paths obey one fetch policy. Secrets (admin credentials, API keys) deliberately stay environment-only. The console renders each Enabled/Disabled setting as a checkbox rather than a two-option dropdown and groups a section's settings inside a labelled fieldset (the Configuration tabs, the Autocrawler settings, and the public-portal Configuration tab); each setting is one compact aligned row and a tab (or a page without tabs) carries a single shared Save that batch-applies only the rows whose value changed, so an unchanged Save writes nothing and never spuriously marks a setting overridden, while an overridden row keeps a per-row Reset. Saving works without JavaScript: an unchecked checkbox submits no value, and a hidden per-row marker lets the batch read that absence as a disabled setting. | Not applicable. | Not applicable. | Not applicable. | `yagonode/internal/settingsstore/*_test.go`, `yagonode/internal/yagonode/node_settings_test.go`, `yagonode/internal/yagonode/node_admin_settings_test.go`, `yagonode/internal/yagonode/node_runtime_toggles_test.go`, `yagonode/internal/yagonode/node_bindings_test.go`, `yagonode/internal/yagonode/node_admin_bindings_test.go`, `yagonode/internal/adminui/settings_test.go` | Reset-to-default unsets an override and reapplies the env default. Search ranking weights are a separate runtime-editable persisted surface (FTR-040). |
| FTR-037 | Security | HTTP-to-HTTPS redirect | Implemented, off by default: when the `https.redirect` runtime toggle is on (FTR-036; env default `YAGO_HTTPS_REDIRECT=false`), a middleware answers a `308` redirect to the `https://` origin for a plain-HTTP, non-loopback request. A request counts as already-HTTPS when `r.TLS` is set or `X-Forwarded-Proto: https` (reverse-proxy-aware), and loopback is never redirected so the local admin console is never pushed to an unreachable origin. Session cookies set `Secure` from the request TLS state and are always `HttpOnly`, `SameSite=Strict`. | Not applicable. | Not applicable. | Not applicable. | `yagonode/internal/yagonode/node_redirect_test.go`, `yagonode/internal/yagonode/node_runtime_toggles_test.go` | TLS is terminated by a reverse proxy in front; the node has no in-process TLS listener or certificate loading. |
| FTR-038 | P2P policy | Peer block/unblock enforcement | Implemented: a vault-backed `peerblock` store keeps an operator blocklist of peer hashes that survives restart. A `blockingRoster` decorator filters blocked peers out of the reachable-peer set that feeds outbound DHT index fan-out and the peer lists this node advertises, while the admin peer table still lists a blocked peer so it can be unblocked. Blocking is CSRF-guarded (`POST /admin/network/peer/block`, FTR-027) and refuses to block the node's own hash. | Not applicable. | Not applicable. | Reuses the YaCy peer hash value. | `yagonode/internal/peerblock/*_test.go`, `yagonode/internal/yagonode/node_peer_block_test.go` | Blocked peers are excluded from fan-out and advertisement but stay visible to the operator for unblocking. |
| FTR-039 | Search policy | URL/domain blacklist and search enforcement | Implemented: a vault-backed `urldenylist` store keeps operator URL and whole-domain denylist entries that survive restart. A search decorator (`withDenylistFilter`) wraps the federated searcher so denylisted results are dropped from every source — local index, remote peers, and the web fallback — matching an exact URL or a host that equals or is a subdomain of a denylisted domain, loading one in-memory snapshot per search; it fails open (serves unfiltered) with a warning if the snapshot cannot be read. Entries are managed from the Index blacklist manager (FTR-026). | Not applicable. | Not applicable. | Not applicable. | `yagonode/internal/urldenylist/*_test.go`, `yagonode/internal/yagonode/node_denylist_filter_test.go` | Search-result filtering is the enforcement point; fresh-crawl admission enforcement (in the separate crawler worker) remains a documented follow-up. |
| FTR-040 | Search | Runtime-tunable search ranking | Implemented: a vault-backed `rankingprofile` persists the node's default search ranking weights and serves them live to the local searcher, so an operator can retune ranking without a restart or redeploy. An admin ranking-explain preview (FTR-008) scores a query under caller-supplied weights without saving them. Local and remote result lists now merge with Reciprocal Rank Fusion (Cormack et al., SIGIR 2009) at the single federation point: each source contributes 1/(k+rank) per result with k=60, so heterogeneous peer scores never need calibration, results confirmed by several sources rise, and the fused weight feeds the existing diversification, date ordering, and paging unchanged — this replaces the previous max-score calibration of remote results and is the merge primitive a future dense retrieval layer plugs into. On top of the SimHash near-duplicate collapse and the two-per-host crowding cap, the visible window (top 50) reorders with Maximal Marginal Relevance (Carbonell & Goldstein, SIGIR 1998; lambda 0.7, token-Jaccard similarity over title+snippet) so texts repeating an already-shown result yield to novel ones — skipped for site: and date-ordered queries. Snippets are query-biased (Tombros & Sanderson, SIGIR 1998): the excerpt centers on the densest query-term window with a leading ellipsis when it starts mid-text, falling back to the leading text when no term occurs. | Not applicable. | Not applicable. | Not applicable. | `yagonode/internal/rankingprofile/*_test.go`, `yagonode/internal/yagonode/search_ranking_endpoint_test.go` | Weights persist across restarts; the explain preview is non-persisting. |
| FTR-041 | UI | Admin security management | Implemented: the console Security section (`GET`/`POST /admin/security`) manages admin credentials and API keys in the browser. Password change verifies the current password with a constant-time compare and requires a signed-in session principal plus a matching new/confirm. API-key creation is split into two separate forms so the key families cannot be mixed by accident: a **Node API key** (scopes `admin:read`/`admin:write`/`crawl:write`, authenticating this node's own ops API — yago-specific, since Java YaCy has no API-key mechanism at all and protects its admin servlets with HTTP digest auth instead) and a **Search API key (Tavily-compatible)** (scopes `search:read`/`search:raw`, authenticating `POST /search`/`POST /extract` when `YAGO_SEARCH_REQUIRE_API_KEY` is on). Each form mints a key with a chosen label (returning the secret exactly once); the key list carries a type tag (node/search/mixed, classified from scopes), scope tags, and last-used time, and revokes by identifier — over the same hashed key store as the JSON auth API (FTR-018, FTR-019). | Not applicable. | Not applicable. | Not applicable. | `yagonode/internal/adminui/security_test.go`, `yagonode/internal/yagonode/node_admin_security_test.go` | The minted secret is shown once at creation and never again. Keys carrying both scope families remain mintable over the raw JSON API and list as `mixed`. |
| FTR-042 | UI | Admin performance and operations dashboard | Implemented: the console Performance section (`GET /admin/performance`) renders a read-only operational dashboard as Carbon metric tiles — crawl-queue depth, index-queue depth, connected-peer count, local RWI word count, and a storage-headroom flag — read live at request time from the DHT gate snapshot and the crawl-broker queue-depth probe (the same sources as the OPS-01 metrics). | Not applicable. | Not applicable. | Not applicable. | `yagonode/internal/adminui/performance_test.go`, `yagonode/internal/yagonode/node_admin_performance_test.go` | Snapshot per request. The deeper OPS-07..OPS-12 observability panels (RED/USE dashboards, profiling, crawl-health, tracing, SLOs, swarm health) remain planned. |
| FTR-043 | Operations | Separate peer, public search, and ops listeners | Implemented: the node runs three independent HTTP listeners, each with a distinct role. The **peer** listener (`YAGO_PEER_ADDR`, default `:8090`) carries only the YaCy peer-to-peer wire protocol (`/yacy/*`: remote search fan-out, RWI/URL transfer, seedlist, query, hello) and serves a static identity landing page at its root `/`. The **public search** listener (`YAGO_PUBLIC_ADDR`, default `:8080`, unprivileged so the `nonroot` container binds it) carries the client-facing surfaces — the Tavily-compatible API (`POST /search`, `POST /extract`), `/yacysearch.*`, OpenSearch description/suggestions, and the public search portal or landing page at its root; setting it to `off`/`none`/`disabled` suppresses the listener for a pure peer node. The **ops** listener (`YAGO_OPS_ADDR`, default `:9090`) carries `/health`, `/ready`, `/metrics`, the ops JSON endpoints, and the admin console, with its root `/` redirecting to `/admin/`. This matches YaCy's own servlet split — the `/yacy/*` namespace is the inter-peer protocol while `/yacysearch.*` is the client search frontend (verified against `net/yacy/peers/Protocol.java`, which posts remote searches only to `/yacy/search.html`) — so relocating the client search surfaces off the peer port keeps swarm interop intact. Each surface is independently bindable at runtime via the console (FTR-036). | Not applicable. | Not applicable. | Not applicable. | `yagonode/internal/yagonode/node_listener_split_test.go`, `yagonode/internal/yagonode/node_config_test.go`, `yagonode/internal/yagonode/node_bindings_test.go`, `yagonode/test/e2e/interop_matrix_e2e_test.go` | Admin-console search uses the in-process searcher, so it is unaffected when the public listener is off. An external/public base URL for OpenSearch behind a reverse proxy remains planned. |
| FTR-044 | Search | Greedy learning from swarm search | Implemented, off by default: with `YAGO_SWARM_SEED_CRAWL` set (YaCy `greedylearning.enabled` parity) and crawler integration configured, every swarm-search response feeds its remote-result URLs to a conservative crawl seeder off the request path — domain-scoped orders at depth 1 with up to 20 pages per host, idempotent by URL through the durable queue, skipping URLs already stored, robots and blacklist handling enforced by the crawler. Seeding has no document-count ceiling — YaCy's `greedylearning.limit.doccount` cutoff was deliberately dropped, because a node that has already grown a large index is exactly the one that should keep discovering resources neither it nor the swarm already holds; capping seeding at a document count switched greedy learning off just when the node had the most reach to keep growing. Complements the always-on remote-result metadata caching (`YAGO_INDEX_REMOTE_RESULTS`). | Executes the seeded crawl orders like any operator order. | Carries the seeded crawl orders. | Not applicable. | `yagonode/internal/yagonode/node_swarm_seed_test.go`, `yagonode/internal/yagonode/node_swarm_seed_config_test.go` | YaCy 1.5 greedy learning: a fresh peer grows a useful index from what the network already answers with. |
| FTR-045 | Search | Local host block-rank ranking signal | Implemented, off by default (YaCy Block Rank / YBR, local slice): a background loop recomputes a per-host authority table from the stored document citation graph (damped iterative host-level rank over incoming host links, normalized so the top host scores 1, unknown hosts score 0) and publishes it atomically for the search path to read lock-free. The `RankingWeights.HostRank` coefficient (default 0) folds it into local full-text results as a post-retrieval multiplier — a result's score is scaled by `1 + HostRank*rank(host)` and results are re-sorted — so authoritative hosts rise without penalizing hosts absent from the graph; at the default weight the rescore is a no-op. Operators enable and tune it through the existing `/api/admin/v1/search/ranking` profile (the JSON round-trips the new `hostRank` field). Distributed exchange and aggregation of peer rank tables is deferred (ADR-0024). | Not applicable. | Not applicable. | Not applicable. | `yagonode/internal/hostrank/*_test.go`, `yagonode/internal/searchlocal/host_rank_test.go`, `yagonode/internal/yagonode/host_rank_refresh_loop_test.go`, `yagonode/internal/searchindex/ranking_weights_test.go` | YaCy 1.0 YBR: this slice computes host authority from the node's own crawl graph; the peer-exchanged, network-aggregated table remains future work. |
| FTR-046 | Search | Ranking evaluation harness (NDCG@10) | Implemented (test/tooling): the `searcheval` package scores any `searchcore.Searcher` with NDCG@k — exponential-gain, log-discounted, normalized against the ideal ordering — over graded query→relevant-URL judgments, and `Evaluate` reports per-query and mean NDCG. `PseudoJudgments` turns corpus-derived labels (e.g. a document's own title as the query, that page as relevant) into a self-evaluation set needing no human assessments. It backs a non-regression gate: `searchlocal` runs a labelled multilingual corpus (Russian base-form/inflected, German, English) through the real in-memory index and asserts mean NDCG@10 stays perfect, so a ranking change that reintroduces the morphology flood or drops per-language stemming fails the build (SEARCH-16). | Not applicable. | Not applicable. | Not applicable. | `yagonode/internal/searcheval/searcheval_test.go`, `yagonode/internal/searchlocal/morphology_eval_test.go` | Depends only on `searchcore`, so it can gate local or federated searchers. NDCG uses graded relevance (2^grade − 1 gain); pseudo-qrels default to grade 1. |
| FTR-047 | Search | Lexical proximity and coverage evidence | Implemented: the merged top 50 receives a learning-free content-term coverage and proximity signal. Local results use whole-document matched positions; peer results use fetched snippets when available and otherwise title/snippet metadata. The signal contributes 25% of a near-tie reorder key and leaves single-term queries, short lists, and the tail unchanged. The same immutable values feed learned ranking and `/api/admin/v1/search/explain`; normal serving computes field evidence even when diagnostic explanation text is disabled. | Not applicable. | Not applicable. | No wire change. | `yagonode/internal/searchcore/lexical_rerank_test.go`, `yagonode/internal/searchlocal/field_features_test.go`, `yagonode/internal/searchindex/hit_features_test.go`, `yagonode/internal/yagonode/search_explain_endpoint_test.go` | Explain, training, and serving use the same evidence boundary and denylist policy. |
| FTR-048 | Search | Pseudo-relevance feedback (RM3) | Implemented (always on, gated): a `searchcore` decorator wrapping the local searcher expands a recall-poor query with terms mined from its own top results (Lavrenko & Croft RM3, SIGIR 2001). When the first local pass returns some but fewer than 50 results, it treats the top 5 as pseudo-relevant, selects up to 3 content-bearing expansion terms (absent from the query and a small multilingual stoplist, ≥4 characters, appearing in ≥2 feedback documents, ranked by document then total frequency), reissues the expanded query, and fuses the two passes by reciprocal rank so the added terms widen recall and lift on-topic pages without displacing the original matches. A full first page or an empty one skips expansion, and an expanded-pass failure keeps the original answer (SEARCH-14). | Not applicable. | Not applicable. | Not applicable. | `yagonode/internal/searchcore/pseudo_relevance_test.go`, `yagonode/internal/yagonode/node_public_search.go` | Wraps only the local searcher (peers run their own retrieval); benefits from the term-vectored per-language index. SymSpell dictionary spelling correction ships alongside it (FTR-049). |
| FTR-049 | Search | SymSpell query spelling correction | Implemented: a `spellcheck` package corrects query spelling against the node's own indexed vocabulary with the Symmetric Delete algorithm — the dictionary precomputes delete-variants so a lookup only deletes characters from the query term (never the dictionary), fast enough to run on every zero-result query. A background sweep rebuilds the corrector from the stored documents' titles and body text on the same cadence as host-rank, pruning singletons so corrections target words the corpus uses repeatedly, and publishes it lock-free through an atomic holder. The zero-result recovery consults it: a fuzzy retry that surfaces pages gets a frequency-ranked "did you mean" from the whole vocabulary (preferred over the recovered-title sample), and even a total miss — where the fuzzy retry is also empty — now carries a spelling suggestion so the user can reach the intended query (SEARCH-14). | Not applicable. | Not applicable. | Not applicable. | `yagonode/internal/spellcheck/symspell_test.go`, `yagonode/internal/yagonode/spell_refresh_loop_test.go`, `yagonode/internal/yagonode/node_search_recovery_test.go` | Edit distance up to 2, minimum term length 4, corrections ranked by edit distance then corpus frequency. Complements the existing recovered-title "did you mean" (FTR: zero-result recovery). |
| FTR-050 | Search | Swarm morphological query expansion | Implemented (opt-in, `YAGO_SWARM_MORPHOLOGY`, admin `swarm.morphology.enabled`, default off): a single-word remote search is expanded into the inflected surface forms the node has actually indexed, so the swarm also reaches peers holding a document under an inflection of the query word. Because a YaCy DHT word hash encodes the exact lowercased word and peers do not stem, the expansion is done client-side before hashing and never touches the wire contract — each variant is an ordinary exact-word hash a stock peer answers normally. The forms are not hardcoded: a background sweep runs the node's stored vocabulary through the same per-language Snowball stemmers the index uses (ADR-0026, via `searchindex.StemWord`), groups terms by shared stem, and publishes a `stem → surface forms` expander through an atomic holder for lock-free reads; a query word expands to the other observed forms of its stem, capped. The remote searcher runs the exact conjunctive search once per variant and fuses the passes by reciprocal rank so a document indexed under any inflection contributes while the base form keeps top rank, and the fused list is capped to the requested limit. Multi-word queries keep the exact search to avoid multiplying peer fan-out; the sweep runs only when the feature is enabled (ADR-0027, SEARCH-22). | Not applicable. | Not applicable. | Defines the DHT word-hash addressing the expansion preserves. | `yagonode/internal/wordforms/*_test.go`, `yagonode/internal/searchindex/word_stem_test.go`, `yagonode/internal/searchremote/searchremote_test.go`, `yagonode/internal/yagonode/word_forms_refresh_loop_test.go` | Generalizes to every language with a Snowball stemmer with no per-language tables; forms are corpus-grounded so only words the swarm plausibly holds are searched. Opt-in because expansion multiplies per-word peer fan-out. |
| FTR-051 | Search | All-words retrieval and peer evidence verification | Implemented: precise local retrieval requires every content term across title, headings, trusted anchors, body, or URL; analyzer-elided terms are exempt and all-stopword queries retain their full term list. RM3 expansion terms can reorder only documents that already satisfy the required query. Peer rows are capped to the requested count, deduplicated, and counted from rows actually received. YaCy wire rows intentionally omit body snippets, so metadata-only rows are retained. When verification is enabled, the bounded first-page snippet pass checks reachable page text, supplies a query-biased snippet for a match, demotes a confirmed no-match without deleting it, and keeps an unreachable page eligible; `verify=false` skips that pass. | Not applicable. | Not applicable. | YaCy search row vocabulary is unchanged. | `yagonode/internal/searchindex/required_terms_query_test.go`, `yagonode/internal/searchremote/peer_row_limit_test.go`, `yagonode/internal/snippetfetch/snippetfetch_test.go`, `yagonode/internal/searchcore/term_mentions_test.go` | Web-provider metadata verification is a separate boundary in FTR-052. |
| FTR-052 | Search | Web-fallback hygiene: redirect decoding and metadata verification | Implemented when the optional fallback is enabled: provider redirect URLs are decoded to their real targets, and an unrecoverable redirect is dropped. Provider title, snippet, and URL metadata must mention a content term unless `verify=false`; consent pages, anti-bot filler, and unrelated redirects therefore cannot reach the SERP or the optional crawl seeder. This metadata gate is provider-specific. YaCy peer rows instead use fetched page-text evidence when reachable because their wire format omits body snippets. | Not applicable. | Not applicable. | No YaCy wire change. | `yagonode/internal/websearch/bing_redirect_test.go`, `yagonode/internal/websearch/web_result_verification_test.go`, `yagonode/internal/websearch/ddgs_parse_test.go` | The fallback remains admin-opt-in and is never a YagoRank runtime dependency. |
| FTR-053 | Search | Stopword-aware evidence and index lineage repair | Implemented: compact multilingual function-word lists keep duplicate fingerprints, fetched peer-page checks, web metadata checks, snippets, highlighting, lexical proximity, and RM3 focused on content terms; an all-stopword query falls back to its complete term list. The crawler mirrors the list and omits stopword RWI postings. Peer wire metadata is never rejected merely for lacking a content word because it carries no body; reachable page text supplies that evidence in FTR-061. Document deletion now clears search entries, outbound-anchor contributions, and persistent cluster membership while refreshing surviving representatives. Disk search also removes legacy orphaned index entries when encountered. | Not applicable. | Not applicable. | Ingest and local policy only; no wire change. | `yagonode/internal/stopwords/stopwords_test.go`, `yagonode/internal/contentcluster/content_fingerprint_test.go`, `yagonode/internal/snippetfetch/snippetfetch_test.go`, `yagonode/internal/snippetmark/snippetmark_test.go`, `yagonode/internal/searchindex/bleve_disk_index_test.go`, `yagonode/internal/yagonode/document_lineage_eviction_test.go` | Function-word lists remain data-driven ranking support, not query-specific semantic rules. |
| FTR-054 | Crawler | URL canonicalization | Implemented (always on): `weburl.Normalize` — the one spelling the frontier visited-set, link admission, document keys, and the recrawl schedule all share — applies full RFC 3986 semantics-preserving normalization (lowercased host, default ports elided, dot-segments removed, empty path spelled `/`, fragments stripped, percent-encoding recanonicalized) plus production-crawler parameter hygiene: query parameters are sorted (order never aliases one page into many) and unconditional tracking identifiers are stripped (`utm_*`, gclid/dclid/gbraid/wbraid, fbclid, msclkid, yclid/ysclid/ymclid/_openstat, twclid, ttclid, irclickid, s_cid, mc_eid, _ga/_gl, oly_*, vero_*, HubSpot `__hs*`) along with session ids (phpsessid, jsessionid, sessionid, aspsessionid*, cfid/cftoken). The ambiguous `sid` falls only under Heritrix’s guard (value must look like a 32-character token), so a forum thread crawled under dozens of `sid=` spellings burns one visited-set slot and one index entry instead of dozens. Semantic parameters (`ref`, story ids) are never touched, path/query case and trailing slashes are preserved, and an unparsable query is kept verbatim. Node ingest stores every accepted normalized URL; persistent exact and near-duplicate clustering belongs to FTR-121 and consolidates representatives only in final ranking. | Not applicable. | Not applicable. | No wire change. | `yagocrawler/internal/weburl/canonicalize_test.go`, `yagonode/internal/crawlresults/ingest_microbatch_group_test.go`, `yagonode/internal/contentcluster/*_test.go` | `rel=canonical` is parsed and stored as ranking evidence; it does not delete a distinct accepted URL. |
| FTR-055 | Crawler | Per-profile robots.txt opt-out | Implemented (per-crawl, default off — robots is always enforced unless the operator explicitly opts out): the crawl profile carries a new `IgnoreRobots` flag, exposed as an explicit confirmation checkbox on the admin crawl form («Ignore robots.txt (I confirm I may crawl these hosts)», never pre-checked) and as `ignoreRobots` on the dispatch API. The flag rides the profile JSON to the worker (no wire/proto change), the frontier stamps it onto each job, and the pipeline routes such jobs through a fetch-chain variant that skips only the robots.txt layer — botwall screening, browser fallback, and the public-web egress admission stay identical, and both TLS variants (verifying and IgnoreTLSAuthority) have a robots-skipping twin. An unwired variant falls back to the robots-obeying chain, so the safe default cannot be lost by misassembly. Closes the last CRAWL-04 task («enforce robots.txt unless profile disables it explicitly and admin confirms»). A robots.txt that is fetched but cannot be parsed is handled robustly rather than re-fetched forever: a strict-parser rejection — a common real-world quirk is a directive such as Crawl-delay placed before the first User-agent line — is retried once on a sanitized body that drops the stray pre-group lines so the host's actual Disallow rules are still honored, and the outcome of any successfully fetched body (parsed rules, or an allow-all fallback when even the sanitized body will not parse) is cached; only a genuinely transient fetch or read failure stays uncached (PORT-02), so a malformed file is fetched and logged once instead of on every page fetch. | Not applicable. | Not applicable. | Profile JSON only; no protocol change. | `yagocrawler/cmd/yagocrawler/robots_bypass_chain_test.go`, `yagocrawler/internal/robots/robots_admission_fetcher_test.go`, `yagocrawler/internal/pipeline/robots_fetcher_test.go`, `yagonode/internal/crawldispatch/ignore_robots_test.go`, `yagonode/internal/adminui/crawl_ignore_robots_test.go` | Intended for archiving one’s own sites or hosts whose robots file blocks all crawlers by mistake; robots-denied tallies keep counting for obeying profiles. Fixed (PORT-02, from upstream `nikitakarpei/yacy-rwi-node` commit a99b9aae): the robots fetcher used to cache the *fail-open* (allow-all) verdict it returns on a transient robots.txt fetch/read/parse error, so a single network blip made an obeying crawl ignore a host's robots.txt until the LRU evicted it; the transient allow-all now applies to the current request only and is not cached, so the next request re-fetches robots and honors the real rules (a real HTTP outcome — including a 4xx "no robots" — is still cached). |
| FTR-056 | Crawler | Adaptive server-load-aware politeness | Implemented (always on): the fixed per-host crawl delay is now wrapped by an adaptive pace that hears every fetch outcome. A 429 Too Many Requests or 503 Service Unavailable becomes a typed throttle signal carrying the server’s Retry-After wish (RFC 9110 — both delay-seconds and HTTP-date forms parse; absent, unreadable, or past values fold to zero): the host’s penalty doubles per consecutive signal starting from twice the base delay, honors a larger Retry-After, is clamped to 15 minutes (Heritrix’s retryDelaySeconds default) so a hostile header cannot park a host for days, and five consecutive signals park the host at the full cap (Nutch’s exception threshold). Every served page halves the penalty back toward the base delay and resets the failure count — AIMD-style slow-start recovery. The browser fallback no longer fires on a throttled fetch: retrying an overloaded host through a full browser would hit it harder, so the throttle propagates to the politeness layer instead. Backoffs are counted by the new `yacy_crawler_host_backoffs_total` metric (CRAWL-11; complements the operator-set PPM budget with automatic, server-signalled restraint). | Not applicable. | Not applicable. | Worker-local; no wire change. | `yagocrawler/internal/crawldelay/adaptive_pace_test.go`, `yagocrawler/internal/pagefetch/throttle_error_test.go`, `yagocrawler/internal/httpfetch/page_fetcher_test.go`, `yagocrawler/internal/pipeline/host_load_feedback_test.go` | The frontier consults the same CrawlPace interface, so the adaptive pace dropped in without touching the dispatch loop; per-profile CrawlDelay still sets each host’s base rhythm. |
| FTR-057 | Crawler | Main-content extraction on the FineWeb recipe | Implemented (always on): the trafilatura extractor the HTML parser already runs now uses the exact settings the FineWeb corpus pipeline validated at web scale (arXiv:2406.17557): precision-favoring focus, duplicate-segment removal, comments excluded — so navigation chrome, subscription asides, and footer boilerplate stay out of the RWI postings and snippets, lifting both posting quality and snippet quality with one change. The readability/dom-distiller fallback stays enabled as the recall floor, and a page where extraction still yields nothing falls back to the full-DOM text walk, so no page loses its only text (CRAWL-13). | Not applicable. | Not applicable. | Worker-local; no wire change. | `yagocrawler/internal/pageparse/boilerplate_extraction_test.go`, `yagocrawler/internal/pageparse/html_parser_test.go` | A boilerplate regression fixture (nav/aside/footer chrome around a Russian article) pins the behavior: article text indexed, «Корзина покупателя»-grade chrome not. |
| FTR-058 | Crawler | Pre-index content-quality gate | Implemented (default on; env `YAGO_INGEST_QUALITY_GATE`, admin `crawl.ingest.quality_gate`): before a crawled page is stored or indexed, its extracted text passes the deterministic token-based subset of the Gopher (Rae et al., arXiv:2112.11446) and C4 (arXiv:1910.10683) quality rules FineWeb validated at web scale — word-count bounds [50, 100k], mean word length [3, 10], symbol-to-word ratio ≤0.1 (#/ellipsis), ≥80% alphabetic words, ≥2 function words (widened from Gopher’s English-only set to the node’s multilingual stopword list), Gopher repetition caps (top 2/3/4-gram character share ≤0.20/0.18/0.16; duplicated 5..10-gram share ≤0.15..0.10), and C4’s lorem-ipsum marker. A failing batch is dropped whole — its postings never pollute the shared RWI — with the rejection counted (`crawl_ingest_low_quality_total`) and the violated rule named in the debug log. Documented deviations: line-based rules are absent (extracted text is whitespace-collapsed, it has no lines); C4’s curly-brace/javascript rules are dropped (a web engine indexes technical pages); unsegmented scripts (CJK, Thai…) skip the gate, since word statistics need word boundaries (CRAWL-14). | Not applicable. | Not applicable. | Ingest-side; deterministic rules keep peers reproducible. | `yagonode/internal/contentquality/contentquality_test.go`, `yagonode/internal/crawlresults/ingest_quality_gate_test.go`, `yagonode/internal/yagonode/node_crawl_config_test.go` | Every rejection names its rule, so an operator can see per-rule kill patterns in the logs before deciding to disable the gate for an unusual corpus. |
| FTR-059 | Crawler | Value-ordered frontier dispatch | Implemented (always on, pluggable): the frontier no longer dispatches ready jobs first-in-first-out — among the jobs that are unpaused, under their host’s in-flight cap, and past their politeness due time, the one a value scorer ranks best goes first (ties keep submission order), so a limited page budget is spent on high-value pages (Craw4LLM, arXiv:2502.13347, motivates value-ordered frontiers). The built-in model-free heuristic scores shallow link-distance first (the classic quality signal behind breadth-first ordering), rewards hosts that contributed few pages to the run so crawls stay broad instead of drilling one site to its depth limit, and penalizes deep paths and parameter-heavy URLs (disproportionately pagination and faceted listings). An alternative scorer plugs in through `frontier.WithValueScorer` (CRAWL-12). | Not applicable. | Not applicable. | Worker-local ordering; no wire change. | `yagocrawler/internal/frontier/value_scorer_test.go` | PLAN’s optional gRPC scoring endpoint is deliberately deferred: the ValueScorer function seam is the plug point, and a remote scorer can implement it without touching the dispatch loop. |
| FTR-060 | Crawler | Per-profile browser-rendering opt-out | Implemented (per-crawl, default off — the browser fallback stays available unless the profile opts out): the crawl profile carries a `DisableBrowser` flag (admin checkbox «Disable browser rendering (fast HTTP fetch only)», `disableBrowser` on the dispatch API) that keeps every job of the crawl on the fast HTTP path — a rejected fetch is not retried through the headless browser. The opt-out travels as a context marker inside the pagefetch package (set per job by the pipeline, read by the fallback source), deliberately avoiding a third doubling of the fetch-chain matrix (TLS × robots × browser would be eight chains). PLAN’s Wave-C framing wanted rendering opt-IN and disabled by default; this node deliberately keeps the existing default — the browser fallback fires only when the fast path is rejected, its per-host cost is already bounded by politeness, and live deployments rely on it for bot-walled hosts — so the profile knob is an opt-OUT (CRAWL-15, adapted). When the browser fallback does render a page, it reads the result markup with a single `document.documentElement.outerHTML` script evaluation over Marionette rather than resolving a node handle and reading its outer HTML in a second call: a page that navigates between those two steps (a redirect, meta refresh, or client-side navigation) would leave the handle stale, so a single live-DOM read makes the slow path resilient to in-flight navigation. | Not applicable. | Not applicable. | Profile JSON only; no protocol change. | `yagocrawler/internal/pagefetch/fallback_page_source_test.go`, `yagocrawler/internal/firefoxfetch/browser_page_fetcher_internal_test.go`, `yagocrawler/internal/pipeline/host_load_feedback_test.go`, `yagonode/internal/crawldispatch/ignore_robots_test.go`, `yagonode/internal/adminui/crawl_ignore_robots_test.go` | Composable with IgnoreRobots and IgnoreTLSAuthority — all three are independent per-profile fetch-path axes. |
| FTR-061 | Search | Peer-result snippet fetch & verification | Implemented (default on; env `YAGO_PEER_SNIPPET_FETCH`, admin `search.peer.snippet_fetch`; `verify=false` requests skip it): a peer sends only a result’s title, so the SERP previously showed title-as-snippet and the query words the peer matched in the page body stayed invisible (live: «что такое осень ддт» returned Wikipedia’s ДДТ band pages with no «осень» anywhere). YaCy parity (SearchEvent.getSnippet/TextSnippet): the first page’s peer rows now fetch their pages — through the node’s egress-guarded client, size-bounded, up to 8 concurrent under a 2.5 s pass budget, with a TTL’d text cache in YaCy’s snippetsCache role — and each fetched text is checked for **every** content word of the query: a verified page trades the bare title for a query-biased excerpt centered on the earliest content term (Tombros & Sanderson, SIGIR 1998), a page missing the words is demoted behind every other row (SEARCH-32: YaCy sorts such results out of a stream of hundreds, but dropping from a ten-row window emptied the SERP above the web-fallback and zero-result layers — «0 result(s)» with no DDGS trigger — so verification may reorder but never removes), and an unreachable page keeps its row unchanged, exactly as YaCy keeps remote rows whose snippet fetch fails. Enrichment sits directly on the federated merge — below the web fallback (an emptied window still triggers DDGS), below the rerank (ranking sees fetched-page evidence instead of bare titles; the rerank may lift a demoted row whose title carries the query words — demotion is best-effort, non-removal is the invariant), and below zero-result recovery (SEARCH-31, SEARCH-32). | Not applicable. | Not applicable. | Fetching a result URL sends no query text anywhere; egress-guarded. | `yagonode/internal/snippetfetch/snippetfetch_test.go`, `yagonode/internal/yagonode/node_snippet_fetch_test.go`, `yagonode/internal/searchcore/term_mentions_test.go` | Sits below the paging session cache, so one query enriches once; deeper pages keep peer titles. Compared with other engines: SearXNG/Whoogle never load result pages — YaCy is the reference behavior for a P2P engine whose peers cannot be trusted for snippets. |
| FTR-062 | Crawler | Autocrawler — tunable seed-crawl profile | Implemented (env `YAGO_SWARM_SEED_DEPTH` default 1 / 0–8, `YAGO_SWARM_SEED_MAX_PAGES` default 20 / ≥1; admin `swarm.seed.depth` and `swarm.seed.max_pages`): the greedy-learning seeder that crawls URLs surfaced by swarm search previously used a hardcoded depth-1 / 20-pages profile, so operators could enable greedy learning but not tune how aggressively it grew the index. The swarm-seed config now carries the seed-crawl depth and per-host page cap — mirroring the web-fallback seed knobs so both discovery paths are equally tunable — and the public-search assembly threads them into the conservative, domain-scoped crawl order instead of the fixed constants. A runtime edit is bounds-checked to the same 0–8 depth / ≥1 pages the env loader enforces, so the admin console cannot set a profile startup would reject (CRAWL-16, UI-14). | Not applicable. | Not applicable. | Crawl-order profile only; conservative ScopeDomain/MatchAll orders unchanged. | `yagonode/internal/yagonode/node_swarm_seed_config_test.go`, `yagonode/internal/yagonode/node_swarm_seed_test.go`, `yagonode/internal/yagonode/node_settings_catalog_test.go` | Shares the seedProfile spine with the web-fallback seeder; greedy learning now seeds for the life of the node with no document-count ceiling (the `YAGO_SWARM_SEED_LIMIT_DOCS` / `swarm.seed.limit` knob was removed so a large index keeps discovering what it and the swarm still lack). UI-14 follow-up (operator report: настройки жили только в общей Configuration): a dedicated «Autocrawler» admin section now sits in the navigation between Search and Crawler, editing exactly the discovery subset — swarm greedy learning (toggle, depth, pages per host) and web-discovery seeding (toggle, depth, pages per host; these three env-only knobs `YAGO_WEB_FALLBACK_SEED_{CRAWL,DEPTH,MAX_PAGES}` gained admin parity here) — through the same validated runtime-settings path; foreign keys 404. |
| FTR-063 | Admin | Mandatory node restart after the setup wizard | Implemented: several first-run wizard choices (public-portal listener, seedlist bootstrap, web-fallback assembly) are persisted runtime overrides that only take effect at boot, so completing the wizard used to leave the node half-configured until someone restarted it by hand. The setup form now ends in a mandatory graceful restart: after the choices are saved the operator sees a restarting notice (auto-redirect to the login page after ~10 s), the serve context is cancelled so in-flight responses finish under the shutdown timeout, and the process exits with the dedicated restart code 3 — non-zero so every supervisor policy (docker restart: always/unless-stopped, systemd Restart=on-failure) brings the node back up with the settings applied. A failed wizard apply skips the restart and surfaces the partial success honestly. | Not applicable. | Not applicable. | No protocol change. | `yagonode/internal/adminauth/setup_wizard_test.go`, `yagonode/internal/yagonode/node_restart_test.go`, `yagonode/internal/yagonode/node_setup_wizard_test.go` | The restart controller is reusable groundwork for the planned admin-nav Restart Node action (UI-02). |
| FTR-064 | Admin | Restart Node action in the console header | Implemented: a «Restart Node» header link (between Security and Sign out) opens a confirmation page explaining that in-flight crawls and searches are interrupted while durable queues resume; the confirmed CSRF-protected form renders a restarting notice (auto-return to the console in ~10 s) and fires the same graceful restart controller the setup wizard uses — the node exits with the dedicated restart code 3 and the supervisor brings it back with persisted settings applied (UI-02). Deployments without the trigger wired show an honest unavailable page and 404 the action. | Not applicable. | Not applicable. | No protocol change. | `yagonode/internal/adminui/restart_test.go` | Reuses the FTR-063 restart controller; CSRF enforcement comes from the admin guard shared by every console POST. |
| FTR-065 | Security | Agent search API: no key, no access | Implemented (breaking, by operator directive): the Tavily-compatible agent surfaces (`/search`, `/extract`, `/crawl`, `/map`) previously fell back to public access when neither a static token nor the scoped key store was configured. The fallback is removed: an unconfigured access policy now answers 401 — the search API returns raw page content and can seed the crawler, so exposure must be an explicit opt-in (mint scoped keys in Admin → Security, or set `YAGO_SEARCH_API_KEY`). The YaCy-compatible public surfaces (`/yacysearch.*`, suggest, OpenSearch, the portal) are unaffected and stay public with rate limiting (SEC-02). | Not applicable. | Not applicable. | YaCy wire surfaces untouched. | `yagonode/internal/tavilyapi/search_access_read_test.go`, `yagonode/internal/tavilyapi/extract_endpoint_test.go`, `yagonode/internal/yagonode/node_public_search_test.go` | Operators upgrading past this change must configure a key before agent API clients work again. |
| FTR-066 | Search | Parallel federated fan-out | Implemented: the local index query and the peer fan-out previously ran in sequence, so every federated query paid the local latency on top of the seconds the remote branch spends waiting on peers. The two branches now run concurrently — the remote branch starts first on its own goroutine (buffered handoff, no leak on the local error path), the local branch runs inline, and the Reciprocal Rank Fusion merge point is unchanged. A local failure still fails the query; a remote failure still degrades to a partial-failure annotation (PERF-02). | Not applicable. | Not applicable. | No protocol change. | `yagonode/internal/searchcore/federated_test.go` (barrier test deadlocks under sequential execution) | Wall-clock for federated queries drops to max(local, remote) instead of local+remote. |
| FTR-067 | Search | Zero-result recovery retries the local index only | Implemented: the fuzzy retry used to re-run the whole federated pipeline — a second multi-second peer fan-out and potentially a second web-fallback query — even though only the local bleve index understands edit-distance-tolerant matching. The recovery searcher now takes a dedicated retry target and the assembly passes the denylist-filtered local index, so a recovery costs one cheap local query; the honest-empty, did-you-mean, and recovered-label semantics are unchanged (PERF-04). | Not applicable. | Not applicable. | No protocol change. | `yagonode/internal/yagonode/node_search_recovery_test.go` (dedicated-retry guard: full pipeline retried 0 times) | Composes with PERF-02: a federated miss now costs one fan-out, not two. |
| FTR-068 | Admin | Bounded document browse on the Index page | Implemented: every visited document in the admin Index browser is a vault read plus a zstd+JSON decode of the full page text, and the browse scanned the entire store on every page load — tens of seconds on a large index (PERF-01). The scan is now bounded: an unfiltered browse stops the moment the 50-row page is full (visits exactly 50 documents), and a filtered browse gives up after a 20 000-document budget; both cases mark the page as sampled and the template says the scan stopped early instead of implying full coverage. | Not applicable. | Not applicable. | No protocol change. | `yagonode/internal/yagonode/node_admin_documents_test.go` (visit-count guards) | The index document total on the same page still comes from the cheap index Stats call, so the operator keeps an exact corpus size. |
| FTR-069 | Crawler | Micro-batched ingest: one vault write, one bleve batch | Implemented: ingest absorbed one crawl delivery at a time — every page cost its own vault Receive and its own bleve segment write, which made indexing the crawl bottleneck (PERF-05). The ingest loop now drains whatever deliveries are already waiting on the stream (up to 16), runs the admission gates per delivery, and lands the surviving documents through one vault Receive and one bleve batch per shard (a new optional BatchIndexer bulk path on the search index, with a per-document fallback for backends without one). The grouped tail persists the same way (IO-AGG-01): the whole group's URL metadata lands through one Receive, its RWI-01 stale sweeps through one transaction, its postings through one Receive, and its recrawl schedule through one transaction — one durable commit per touched shard instead of one per page, which is what fsync-bound (ZFS) storage feels. Store backpressure or an index failure redelivers every carrying delivery so the store and the index stay in step; a lone delivery takes the unchanged single path. | Not applicable. | Not applicable. | No protocol change. | `yagonode/internal/crawlresults/ingest_microbatch_test.go`, `yagonode/internal/searchindex/batch_indexer_test.go` | Group size follows crawl pressure: an idle stream yields groups of one, a busy crawl fills the 16-slot batch. |
| FTR-070 | Search | Bounded hit hydration in local search | Implemented: the disk index hydrated every bleve hit — up to four times the requested window, ~800 vault reads with zstd+JSON decodes of full page text on a page-one query — even when nothing filtered on document fields (PERF-03). Hydration now stops at a full result page when the request carries no post-filters and no facet ask, and the total comes from bleve’s own match count (minus discovered orphans); requests with language/domain/date/author/near/vertical filters or facets still scan the full hit window because only the stored document can answer them. | Not applicable. | Not applicable. | No protocol change. | `yagonode/internal/searchindex/batch_indexer_test.go` (hydration-count guard: exactly one page loaded) | The full fix — indexing filterable fields into bleve and storing display fields — needs an index-format migration and stays a follow-up; this bound removes the common-path cost today. |
| FTR-071 | Storage | Concurrent shard-vault writers | Implemented: the shard vault serialized every writer behind one store-wide mutex — needed because lazily opening several bbolt shards from concurrent updates in data-driven order can deadlock. Writers now take per-shard locks on first touch, so updates landing on disjoint shards commit concurrently; a writer that meets a locked shard rolls back, upgrades to an exclusive gate, and retries alone — no failure surfaces, progress is guaranteed. Known bound: inserts into one collection still serialize on its length-counter shard (the counter key is the collection name), so the win applies between different collections — documents, URL metadata, postings, DHT intake — and to update-in-place writes (PERF-06). | Not applicable. | Not applicable. | On-disk format unchanged. | `yagonode/internal/shardvault/shardvault_test.go` (barrier overlap test on verified-disjoint shards + contention-retry test, both race-clean) | Sharding the per-collection length counters would extend the win within a collection and is follow-up material. |
| FTR-072 | Search | Realistic swarm fan-out budgets, tunable with peer-failure transparency | Implemented (env `YAGO_SEARCH_REMOTE_PEER_TIMEOUT` / `YAGO_SEARCH_REMOTE_TIMEOUT`, admin `search.remote.peer_timeout` / `search.remote.timeout`): the swarm is always queried in parallel with the local index, but the per-peer budget was one second and the whole fan-out two — real YaCy peers answer a remote search in one to five seconds, so most honest answers were discarded and the SERP showed «0 from peers» at exactly the two-second deadline (SEARCH-35, operator report on «cisco 2801»). Defaults are now 3 s per peer / 4 s overall — concurrent with the local query since PERF-02, so the longer budget costs latency only when peers actually take that long — and the portal meta line says «N peer(s) unreachable or timed out» when the fan-out lost peers, so an empty peer column is distinguishable from peers having nothing. | Not applicable. | Not applicable. | Peer wire protocol unchanged; only client-side budgets. | `yagonode/internal/yagonode/node_swarm_seed_config_test.go`, `yagonode/internal/yagonode/node_settings_catalog_test.go` | Runtime edits are bounds-checked to 100ms–2m. |
| FTR-073 | Network | LAN peer discovery beacon | Implemented (env `YAGO_LAN_DISCOVERY` default on, admin `network.lan_discovery`): a Syncthing-style UDP beacon on port 21777 broadcasts this node’s hash and peer port every ~45 s and listens for neighbors; a heard announcement hands the packet’s **source address** (never the payload’s) to the ordinary verified hello exchange, so a forged beacon can at most trigger one authenticated greet, and a verified LAN peer lands in the roster like any greeted peer. Repeat announcements from one endpoint are suppressed for ten minutes. Two nodes behind the same NAT now find each other with no seedlist or public reachability at all (NET-05). | Not applicable. | Not applicable. | Discovery only; the peer wire protocol and roster semantics are unchanged. | `yagonode/internal/landiscovery/landiscovery_test.go`, `yagonode/internal/yagonode/node_lan_discovery_test.go` | The greeter is the announcer’s new GreetDiscovered path — the beacon itself is never trusted for identity. |
| FTR-074 | Search | Global search by default + facets on peer/web answers | Implemented (SEARCH-36, two operator reports): (1) the public request normalizer defaulted an absent `resource=` to **local**, so every /yacysearch.* query without the parameter silently skipped the swarm — YaCy defaults resource to global, and so do we now (an unreachable swarm still degrades to local results via the partial-failure path); a nil peer roster no longer panics the fan-out. (2) The facet sidebar only ever carried local-corpus counts, so a page answered by peers or the web fallback rendered with no facets at all; when corpus counts are absent and rows exist, the sidebar now derives host and language groups from the visible rows (capped at 8 terms, counts honestly describe the window, not a corpus). | Not applicable. | Not applicable. | resource semantics now match YaCy. | `yagonode/internal/searchcore/searchcore_test.go`, `yagonode/internal/yagonode/node_public_portal_test.go` | Composes with SEARCH-35: swarm consulted by default AND given a realistic budget. |
| FTR-075 | Search | Whole-roster concurrent peer fan-out | Implemented (SEARCH-37, operator report «13 peer(s) unreachable or timed out» at exactly the overall budget): the swarm fan-out ran through a four-worker pool, so thirteen targets took four sequential waves of up to three seconds each and the overall budget expired before the later waves started — every peer looked timed out even when it answered. YaCy queries every selected target at once (RemoteSearch.java starts one thread per peer); the worker cap now exists only to bound a pathological roster (32). | Not applicable. | Not applicable. | Wire requests unchanged; only client-side scheduling. | `yagonode/internal/searchremote/searchremote_test.go` (thirteen 400 ms peers must all be in flight together and answer inside a budget four waves would blow) | With SEARCH-35 budgets, a 13-peer fan-out now completes in one wave ≤3 s. |
| FTR-076 | Ops | Runtime profiling endpoints on the ops listener | Implemented (OPS-08): Go’s pprof surface (`/debug/pprof/` index, CPU profile, heap, goroutine, cmdline, symbol, trace) mounts on the ops mux, which the admin guard already wraps — profiles are operator-only, and continuous-profiling collectors (Parca, Pyroscope) scrape with an authenticated session or a reverse-proxy exemption. No restart, flag, or separate listener needed. | Not applicable. | Not applicable. | No protocol change. | `yagonode/internal/yagonode/node_profiling_test.go` | Complements the RED/USE metrics of FTR-... (OPS-07); doc/observability.md covers both. |
| FTR-077 | Search | Research-backed ranking signals: SDM proximity, URL and freshness priors, host authority on | Implemented (SEARCH-38, operator request, research brief in doc/research/ranking-2026-07.md): the pipeline already scored with BM25, field boosts, RRF fusion, and MMR — but three signals were missing and one was off. (1) Sequential Dependence Model proximity (Metzler & Croft 2005): adjacent query-word pairs now add phrase boosts over title and body at 0.12 of the field weight, so «alpha beta» as a phrase outranks the same words scattered. (2) Entry-page URL prior (Kraaij et al. 2002 / TREC-13 saturating feature): root-like URLs gain up to +10 % — deep leaf paths almost nothing. (3) Freshness prior (Li & Croft 2003): dated documents gain Freshness·2^(−age/180 d); undated ones are not punished; new `freshness` weight in the ranking profile (default 0.2). (4) The YBR host-authority multiplier existed but defaulted to 0 — defaults now enable it at 0.3, and field weights move to the TREC-13 practical range (title 6, anchors 4, url 2). | Not applicable. | Not applicable. | No protocol change; peer results still fuse by rank. | `yagonode/internal/searchindex/sdm_bigrams_test.go` (phrase document beats scattered words end-to-end), `yagonode/internal/searchlocal/ranking_priors_test.go` | True pre-saturation BM25F needs an index migration and stays a documented follow-up. |
| FTR-078 | Admin | Live free-text log filtering | Implemented (UI-13): the Logs section gains a server-side «Contains» filter matching the message, event name, and category case-insensitively, composing with the existing severity and category filters over the durable event log; the filter form applies live through the existing htmx partial (300 ms debounce on typing, instant on select change) and the auto-refreshing table carries the text filter across its 10-second updates. | Not applicable. | Not applicable. | No protocol change. | `yagonode/internal/adminui/logs_filter_test.go` | Extends the UI-09 severity/category filters; no client-side filtering — the durable log is filtered where it lives. |
| FTR-079 | Admin | Full env-to-admin settings parity (CFG-02 review) | Implemented: an operator review walked all 67 YAGO_* environment variables against the runtime settings catalog and closed every gap — 25 new settings: `storage.quota` (human-readable sizes, 100MB floor), `search.api.scoped_access`, swarm presence (`network.peer.https_preferred`, `network.announce.interval`, `network.announce.greets_per_cycle`), the whole `dht.*` group (participation, distribution, busy-gates, interval, redundancy, minimum peer age/connected peers/indexed words), web-fallback tuning (`max_results`, `timeout`, `cache_ttl`, `safesearch`), extract-fetch tuning (`timeout`, `max_bytes`), and the perimeter (`security.egress.allow_private`, `security.egress.allow_cidrs`, `security.trusted_proxies`, `security.cors.admin`, `security.cors.search`). doc/configuration-parity.md records what stays env-only and why: identity/boot facts, listener addresses (own UI), secrets (Security section), and the DHT partition exponent (changing it live would misroute transfers). | Not applicable. | Not applicable. | No protocol change. | `yagonode/internal/yagonode/node_settings_catalog_gaps_test.go` (round-trip for every new setting + normalizer rejections) | The catalog secret-guard test forbids key/password/token-looking setting names — the scoped-access toggle is named accordingly. |
| FTR-080 | Ops | Crawl-health tiles and spider-trap warnings | Implemented (OPS-09, Olston & Najork FnTIR 2010): the live crawl monitor derives harvest rate (indexed/fetched), link redundancy, and failure rate once a sample reaches 100 fetched pages — small samples hide the tiles rather than mislead. The spider-trap signal is a *low harvest rate*, not raw duplicate volume: the frontier counts a duplicate every time a discovered link re-points at an already-queued URL, which any densely intra-linked site does many times per fetched page, so that count is unbounded against fetched pages (it produced nonsensical >100 % "duplicate" alerts on healthy crawls) and is now surfaced only as a bounded "link redundancy" share of all links the frontier resolved (duplicates / duplicates+fetched+failed+robots-denied). Per running run, a tally where fewer than 20 % of fetched pages produced an index entry raises a named «possible spider trap or junk pages» warning and one where over 50 % of fetches fail a «host may be blocking» warning (failure checked first as the more specific diagnosis), refreshed with the monitor every 5 s. A densely-linked but healthy run — high duplicate volume yet a high harvest rate — is deliberately not flagged. | Not applicable. | Not applicable. | No protocol change. | `yagonode/internal/adminui/crawl_health_test.go` | Content-level near-duplicate detection at crawl time (Charikar/Manku SimHash, WWW 2007) to catch traps serving distinct URLs with near-identical bodies, and structural URL-trap heuristics (parameter-count/entropy caps, path-segment repetition), remain a documented follow-up. Freshness reads as an indexing-rate query in Prometheus; a true age-of-index gauge needs a corpus scan and stays a documented follow-up. |
| FTR-081 | Ops | W3C trace context + latency exemplars | Implemented (OPS-10): a dependency-free tracectx package implements the W3C traceparent header — every instrumented HTTP request adopts a valid inbound trace or roots a fresh one (sampled 1-in-256 by the random trace ID’s low byte), the trace rides the request context, and the peer fan-out stamps a child traceparent on each outbound peer search so one public query correlates across its legs. Sampled traces attach their ID as an exemplar on the latency histogram (native prometheus ExemplarObserver — no new dependencies); the exemplar path counts the request and records a single latency observation carrying the exemplar, exactly like the plain path, so a sampled request never double-counts the latency histogram. | Not applicable. | Not applicable. | Outbound peer requests gain a standard traceparent header; YaCy peers ignore unknown headers. | `yagonode/internal/tracectx/tracectx_test.go`, `yagonode/internal/yagonode/trace_middleware_test.go`, `yagonode/internal/yagonode/response_metrics_middleware_test.go` (one request → one latency observation and one request count on both the sampled and unsampled path), propagation test in searchremote | Full OpenTelemetry export would need a collector dependency (ADR) — documented as out of scope. |
| FTR-082 | Ops | SLOs with multiwindow burn-rate alerts | Implemented (OPS-11, Google SRE Workbook ch. 5): `deploy/prometheus-rules.yml` (promtool-validated, 14 rules) records the search error and slow-request ratios over 5m/1h/6h/3d and alerts on the canonical burn-rate pairs — fast burn (14.4× over 1h∧5m) pages, slow burn (3× over 6h∧3d) opens a ticket. Objectives in doc/slo.md: search availability 99.5%, search latency 95% ≤ 2.5s (matching a native histogram bucket), plus ticket-worthy crawl-stall (runs active, nothing absorbed 30m) and intake-shedding hints. | Not applicable. | Not applicable. | No protocol change. | promtool check rules (14 rules SUCCESS) | Requires Prometheus scraping the guarded /metrics; single-node operators can drop the page rules and keep tickets. |
| FTR-083 | Ops | Per-peer swarm health score in the Network section | Implemented (OPS-12): each peer row now carries a 0–100 availability score derived purely from passively-gossiped seed facts — recency of last contact (50 pts, 24 h half-life; churn studies make last-contact the dominant availability predictor), longevity (25 pts, mature at 30 days), and self-reported uptime (25 pts, full at 24 h) — banded as healthy/aging/stale tags. No extra probing beyond normal protocol traffic, and the map is only visible to the authenticated local operator (the privacy caveat from the backlog). | Not applicable. | Not applicable. | No protocol change; purely derived display. | `yagonode/internal/adminui/swarm_health_test.go` | Complements the Known/Reachable rollup; the peer detail page shows the underlying facts. |
| FTR-084 | Ops | Offline backup and restore with maintenance mode | Implemented (OPS-03): deploy/backup.sh and deploy/restore.sh cover Docker-Compose and systemd deployments — the node is stopped for the copy (the shard trees are only consistent quiescent) and restarted even on failure via shell traps; restore wipes the target first (mixing shard epochs corrupts both) and the node’s own STOR-04 integrity checks plus SEARCH-30 orphan healing serve as restore verification. Archives hold the vault (identity, settings, key hashes, profiles, events, corpus) and the bleve index; volatile queues are memory-only by design; no plaintext secrets — but the private identity is inside, so archives are sensitive. The OPS-03 acceptance E2E is automated: yagonode/test/e2e/backup_restore_e2e_test.go (build tag e2e) seeds a small RWI index into a compose-run node on the /opt/yago layout, executes the real deploy/backup.sh (docker mode, offline copy with automatic service stop/start), wipes the data volume, executes deploy/restore.sh, and asserts the restarted node reports the exact seeded posting count — so the operator scripts themselves stay covered, not a reimplementation. | Not applicable. | Not applicable. | No protocol change. | Live E2E: backup of a running node, volume wipe, restore, restart — health 200, local search serves restored documents, no integrity findings (doc/backup-restore.md). | Scheduled backups are a cron line away; the scripts are single-purpose on purpose. |
| FTR-085 | Ops | Bare-metal installer for the /opt/yago layout | Implemented (OPS-04 remainder): `deploy/install.sh` creates the yago system user, the /opt/yago/{bin,etc,data} tree with correct ownership (data 750 yago:yago, env files 640 root:yago), copies binaries and env examples without overwriting edited configs, installs and enables the systemd units. Idempotent — re-run for upgrades. Validated in a clean Debian 12 container, including the second run. The packaged `yagocrawler.service` bounds the headless-Firefox fetcher with cgroup controls — `MemoryHigh=60%` (throttle) and `MemoryMax=85%` (a hard ceiling whose OOM stays confined to the crawler cgroup so the node survives), `TasksMax=4096`, and `CPUWeight=50` so the node wins the CPU under contention — all relative to physical RAM and overridable per host with a systemd drop-in, so a runaway render cannot starve the co-located node (PERF-IO-02, ADR-0041); on top of the cgroup weights the crawler process runs at the lowest scheduling priority — `Nice=19` and `IOSchedulingClass=idle` with a low `IOWeight` (the container image applies the cap-free equivalent, `ionice` best-effort/7 plus `nice 19`, in its entrypoint) — so a crawl yields CPU and disk to the node's latency-sensitive search and admin work (PERF-PRIO-01). | Not applicable. | Not applicable. | No protocol change. | Live container E2E (layout, ownership, idempotence). | The .deb postinst performs the same steps. |
| FTR-086 | Ops | Debian package | Implemented (OPS-05): `deploy/debian/build-deb.sh` builds yago_<ver>_<arch>.deb installing into /opt/yago — Depends ca-certificates+adduser, Recommends firefox-esr | firefox (the crawler's browser resolves on both Debian, which ships `firefox-esr`, and Ubuntu, whose `firefox` is a snap; the crawler still runs without it on the HTTP fast path so it is a recommend, not a hard depend that Ubuntu's snap-only browser would make uninstallable), postinst creates the yago user and seeds env files, prerm stops/disables units, postrm on purge removes edited env files but **keeps /opt/yago/data** (the operator’s index and identity outlive the package). The CGO-off binaries are static (no glibc coupling), so one package installs across Debian 12/13 and Ubuntu 24.04 and the latest LTS; validated E2E on all four: install (deps resolve, both binaries report the stamped `--version`), remove (env survives), purge (data survives, env gone). | Not applicable. | Not applicable. | No protocol change. | Live dpkg install/remove/purge matrix across Debian 12/13 + Ubuntu 24.04 and the latest LTS containers in the release workflow smoke test. | Built per-tag for amd64+arm64 by the release workflow. |
| FTR-087 | Ops | Tag-driven release automation | Implemented (OPS-06): `.github/workflows/release.yml` on v* tags — make verify as the required gate, CGO-off trimmed builds for amd64+arm64 with the tag stamped as the build version (`-ldflags -X` into `yagonode.buildVersion` and the crawler's `main.version`, so `yago-node --version` / `yagocrawler --version` report it), per-arch tarballs (binaries + install.sh + systemd units + backup doc) and .deb + .rpm packages (one release matrix drives both builders — the deb via dpkg-deb, the rpm via rpmbuild with debuginfo/strip disabled so the prebuilt static cross-arch binaries package cleanly), an amd64 smoke-install of the .deb across Debian 12/13 and Ubuntu 24.04 and the latest LTS and of the .rpm on Fedora and Rocky 9 (declared deps resolve, both binaries report the stamped version, package removal keeps data), release notes generated from commit titles since the previous tag (the first release lists up to 500), and a GitHub Release with all assets. | Not applicable. | Not applicable. | No protocol change. | YAML validated; tarball recipe, .deb + .rpm builders, static-binary cross-build, and per-distro install + `--version` exercised locally (the .rpm live-installed on Fedora and Rocky 9, amd64 + arm64 packages built); the workflow itself runs on the first v* tag. | Actions pinned by commit SHA like the existing workflow. |
| FTR-088 | Search | Boot-time purge of indexed click-tracking redirects | Implemented (SEARCH-29): SEARCH-28 stopped new bing.com/ck/ redirect targets at ingest, but documents indexed before that fix kept polluting results. A background sweep now runs once per boot: it scans the stored corpus, condemns documents whose normalized or canonical URL is a search-engine click-tracking address (bing.com//*.bing.com with a /ck/ path — junk regardless of whether the destination decodes), and deletes them from the search index first and the vault second (an index failure skips the vault delete so the pair stays consistent). Per-document failures are logged and retried by the next boot’s sweep; shutdown never waits for the scan. | Not applicable. | Not applicable. | No protocol change. | `yagonode/internal/redirectpurge/redirectpurge_test.go` (removal, canonical-URL condemnation, per-document failure resilience, detector table) | Generalizing the detector to other engines’ tracking hosts is a one-line-per-engine extension. |
| FTR-089 | Portal | robots.txt policy for foreign crawlers | Implemented (UI-15, YaCy ConfigRobotsTxt_p parity; first slice of the UI-GAP admin review, matrix in doc/research/admin-gap-2026-07.md): the public listener now serves /robots.txt. Default policy hides the infinite query-addressed surfaces (/yacysearch.*, /suggest.json, /api/, /*?q=) while keeping the landing page indexable; the operator can open everything or close the whole site via the new `web.robots.policy` setting, applied live per request — no restart. Unknown stored values fall back to the safe default. | Not applicable. | Not applicable. | No wire change. | `yagonode/internal/publicrobots/publicrobots_test.go`, robots-policy round-trip in the settings tests | The gap matrix drives the next slices: UI-16 search activity, UI-17 denylist test/import/export, UI-18 index export, UI-19 crawl schedules, UI-20 rate-limit settings, UI-21 portal branding. |
| FTR-090 | Admin | Search-activity section with privacy modes | Implemented (UI-16, YaCy AccessTracker_p parity): a new Activity section shows searches since start, zero-result count, and the recent-searches table from a bounded in-memory ring (200 entries, wiped on restart, never persisted or shared). What is recorded follows the `search.query.log` privacy mode: off renders the section unavailable, aggregate records only shapes (length, term count, results, latency, scope — never text), full also records the query text and enables the top-query-words table. The ring feeds off the existing query-logging decorator, so both sinks honor one mode. | Not applicable. | Not applicable. | No protocol change. | `yagonode/internal/searchactivity/searchactivity_test.go` (modes, ring order/overflow, top words, concurrent recording), adapter + page tests | The section sits between Search and Autocrawler in the nav. |
| FTR-091 | Admin | Denylist probe, export, and import | Implemented (UI-17, YaCy BlacklistTest_p + BlacklistImpExp_p parity): the Index blacklist manager gains a «Test a URL» probe answering through the same snapshot matcher the search filter uses (so the verdict is exactly what a search would do), a plaintext export (one `kind value` line per entry, served as a download), and a paste-to-import form — explicit «domain X» / «url Y» lines, or bare tokens classified by the presence of a scheme, with comments and blanks skipped; a malformed line aborts with its line number instead of half-importing silently. | Not applicable. | Not applicable. | The export format round-trips through the import. | `yagonode/internal/yagonode/node_admin_blacklist_porter_test.go` (import/probe/export round-trip on a real vault-backed store), `yagonode/internal/adminui/blacklist_porter_test.go` (probe verdicts, attachment export, import note, 404 without source) | Optional interfaces keep the base BlacklistSource unchanged (OCP). |
| FTR-092 | Admin | Index export as URL list, CSV, or JSON-lines | Implemented (UI-18, YaCy IndexExport_p parity): the Index section exports the corpus filtered by domain (including subdomains) and/or URL substring — text (one URL per line), CSV (url/title/content-type/language/indexed-at with a header), or JSONL. The export walks the stored corpus directly rather than the sampled admin browser, so it is complete, and streams row by row without buffering; export buttons on the document browser inherit its current filters. | Not applicable. | Not applicable. | The text format re-imports as a crawl seed list or an external tool’s input. | `yagonode/internal/yagonode/node_admin_index_export_test.go` (formats, filters, quoting, unknown format), `adminui` route test (headers, filter plumbing, 400/404) | Complements OPS-03: backup preserves the node, export feeds other systems. |
| FTR-093 | Admin | Public search rate limits as settings | Implemented (UI-20, YaCy SearchAccessRate_p parity): the anonymous /yacysearch.* limits (10 per 3 s, 60 per minute, 300 per 10 minutes, from the YaCy-parity rate-limit work) were hard-coded — they are now `search.rate.burst`, `search.rate.minute`, and `search.rate.ten_minutes` in the catalog, each tier editable independently while untouched tiers keep the shipped defaults; authenticated callers keep their 10× multiplier on top. | Not applicable. | Not applicable. | No wire change. | round-trip + independence + rejection tests in the catalog gap tests | Applied at restart like other perimeter settings. |
| FTR-094 | Portal | Operator-named portal | Implemented (UI-21, ConfigPortal_p greeting subset): `portal.greeting` names the public portal (title and header) live — no restart — through a provider the portal resolves per render; empty keeps the default brand; names are one plain-text line (≤60 chars, no angle brackets). | Not applicable. | Not applicable. | No wire change. | portal brand provider tests + setting normalize/apply/live tests | Logo upload deliberately deferred — a name covers the common «brand my portal» ask without asset management. |
| FTR-095 | Crawler | Recurring crawl schedules | Implemented (UI-19, YaCy Automation_p parity — recorded actions with a recurring schedule, for the one action worth repeating: re-crawling a site): the Crawler section gains a Schedules block — name, seed URLs, scope, depth, and an interval (hour/6h/day/week; the store enforces an hour floor for politeness). Schedules persist in the vault, IDs derive from names so re-creating replaces rather than duplicates, and a background loop (minute tick) re-dispatches due schedules through the very path the crawl-start form uses — same validation, profiles, and format settings. A dispatch failure (e.g. duplicate still running) defers the schedule a full interval instead of hammering the dispatcher. Pause/Resume/Delete per row. | Not applicable. | Not applicable. | No wire change. | `yagonode/internal/crawlschedule/crawlschedule_test.go` (lifecycle, due math, validation, ID stability), adapter/loop tests, console route tests | The loop runs detached from the shutdown group like the other housekeeping loops. |
| FTR-096 | Crawler | Non-HTML formats actually crawlable | Implemented (CRAWL-17, operator report: every PDF rejected with «unsupported content type»): the HTTP fetcher hard-filtered everything but text/html — the body was already downloaded at that point, so the filter saved nothing and made every FMT parser family unreachable from a crawl. The fetcher now passes every content type (and sniffs a type when the header is missing); the pipeline decides per job against the format-parser registry — accepted when the type or URL extension matches the HTML core or an enabled family (extension rescues application/octet-stream), honestly rejected when the family is off or the binary type is unknown. The browser fetcher keeps its HTML-only check: a headless browser produces HTML, and a PDF still never wastes a tab. | Not applicable. | Not applicable. | Matches YaCy: TextParser routes by type+extension after fetch. | Family-matrix test (all 8 families × mime/extension × toggle), boundary tests, rewritten fetcher tests, pipeline gate | Verified live: mafet.org PDFs fetch and index. |
| FTR-097 | Storage | Contended writes retry instead of dropping rows | Implemented (STOR-05, operator report: «url row discarded: shard contended»): the sharded engine retries a contended update under its exclusive gate only when the update callback RETURNS the contention error — the URL-metadata intake swallowed it and logged the row as discarded, turning a retryable abort into data loss. The contention abort now carries an exported vault sentinel, and the intake propagates it so the engine’s second pass lands the row. | Not applicable. | Not applicable. | No wire change. | Sentinel test + an engine double proving the row lands on the retry pass and is never reported rejected | The sentinel’s godoc states the contract: callbacks must propagate it. |
| FTR-098 | Crawler | HTTP/1.1 fallback for HTTP/2-hostile hosts | Implemented (CRAWL-18, operator report: microsoft.com fetches dying with «stream error … INTERNAL_ERROR; received from peer»): bot-protection layers fingerprint Go’s TLS/h2 handshake and reset streams while serving the same page over HTTP/1.1 — YaCy never hits this because its Java client speaks h1. On an h2 stream failure (stream reset, GOAWAY, h2 header timeout) the fetcher retries the request once through an h1-only client (ForceAttemptHTTP2 off, ALPN pinned to http/1.1, same egress guard/timeouts/redirect policy) and remembers the host for an hour in a bounded table (1024 hosts, expired entries swept at capacity), so subsequent fetches skip the doomed h2 attempt. Both the verifying and the IgnoreTLSAuthority chains are armed. | Not applicable. | Not applicable. | Behavior converges with YaCy’s h1-only client while keeping h2 for well-behaved hosts. | `yagocrawler/internal/httpfetch/h1_fallback_test.go` (error-signature table, fallback + host-memory flow, unarmed propagation, TTL/cap semantics) | The downgrade is per-host and expires — a host that fixes its edge returns to h2. |
| FTR-099 | Crawler | Legacy PDF filter chains decode (ASCII85+LZW, ASCIIHex) | Implemented (CRAWL-17 follow-up, found by the live PDF re-crawl: the fetched mafet.org file parsed to nothing): the PDF text extractor only inflated FlateDecode streams, but PDF 1.0-era archives encode content as [/ASCII85Decode /LZWDecode] — the stream dictionary’s /Filter chain is now read and applied in order via stdlib decoders (ascii85, compress/lzw MSB, hex), with image filters (DCTDecode/JPXDecode) skipped instead of emitting garbage. The real 1999 PDF now yields its title and text. | Not applicable. | Not applicable. | Matches YaCy’s pdfbox coverage for these filters. | Synthetic legacy-chain tests (ASCII85+LZW round-trip built with the same stdlib encoders, ASCIIHex, image-only stays unparsed) + probe against the real file | Fetched-but-unparsed documents remain a silent DEBUG — candidate for a parser-error counter. |
| FTR-100 | Crawler | End-to-end format matrix: gate → parser → extraction | Implemented (operator demand after the PDF fix): one table-driven test drives EVERY parser family — HTML, text, CSV, RSS/XML, PDF (legacy-encoded), OOXML Word, PNG, MP3/ID3, vCard, ZIP archive — through the full path: the fetch gate admits the document under all-on toggles, the parser produces an indexable page, and the extraction carries the expected text and title; the SAME sample is then re-probed with the family toggled off and must be rejected at the gate, proving gate and parser consult one registry. A pipeline-level test additionally pins the process gate: a PDF job with the toggle off never reaches the emitter. | Not applicable. | Not applicable. | No wire change. | `yagocrawler/internal/formatparse/e2e_gate_extract_test.go`, pipeline gate test | Samples are built with the same fixtures the per-family unit tests use — no binary blobs in the repo. |
| FTR-101 | Crawler | Quality gate judges web pages only | Implemented (CRAWL-17 wrap-up, found tracing the live PDF end to end: fetched, parsed — then silently dropped at ingest): the Gopher/C4 spam heuristics (50-word floor, boilerplate ratios) model WEB PAGES, but they were applied to every ingested document — a parsed PDF bulletin, vCard, or image/audio metadata extraction legitimately yields a handful of words and was rejected as «too short». The gate now applies only to web-page content types (text/html, xhtml, text/plain, empty); parsed document formats pass, since their parser already required a non-empty extraction. | Not applicable. | Not applicable. | Consistent with the FMT wave: metadata-style documents are first-class. | `yagonode/internal/crawlresults/quality_gate_scope_test.go` (short PDF passes, same short text on HTML still rejected, all web types still gated) | Verified live: the 1999 PDF is now searchable. |
| FTR-102 | Crawler | Legacy binary Office extracted from its own streams, not scanned raw | Implemented (FMT-10, found by running real .doc/.xls/.ppt/.odt/.rtf downloads through the parser as the operator asked): the legacy binary Office formats are OLE2 compound files, and the previous best-effort scan of the whole container leaked OLE directory names («Root Entry», «Calc»), Word stream artefacts («bjbj»), and even the raw bytes of embedded images (PNG chunk tags out of a .ppt) into the index. A new stdlib compound-file reader now isolates each text stream and format-specific extractors decode them: Word through the FIB piece table (compressed cp1252 and UTF-16 pieces, field-code and control-character handling), Excel through the BIFF shared-string table (with CONTINUE-record spanning) and inline label cells, PowerPoint through its TextBytes/TextChars atoms; Visio and any other compound file fall back to readable runs over content streams only, skipping the metadata and OLE machinery. RTF now drops ignorable `{\*\...}` destination groups (no more embedded WordML schema URLs as the title), and OOXML/ODF metadata parts no longer index the generator string or timestamps. | Not applicable. | Not applicable. | No new third-party dependency (stdlib only); no wire change. | `yagocrawler/internal/formatparse/ole2.go`, `legacy_office.go`, `sst_reader.go`, `cp1252.go` + tests building valid compound-file fixtures | Verified live against real downloaded .doc/.xls/.ppt/.odt/.rtf files: clean text and titles, no OLE noise. |
| FTR-103 | Crawler | Structural crawler-trap defence at link admission | Implemented (CRAWL-27): discovered links whose structure marks them as a likely crawler trap — calendar, faceted-navigation, or path-recursion loops that mint an unbounded space of distinct URLs and flood the frontier — are refused at the link-admission gate (`crawladmission.admit`, alongside the scope, query-URL, and regex rules) before they are ever fetched, so a trap can no longer explode the pending queue. A new pure `urltrap` package applies conservative structural heuristics (Olston & Najork, "Web Crawling", FnTIR 2010): a URL is refused when it exceeds 2048 bytes, nests beyond 24 path segments, repeats any single path segment more than three times (the signature of a `/a/b/a/b/a/b` link cycle), or carries more than 16 query parameters. Session-id and tracking-parameter variants are already collapsed upstream by `weburl.Normalize`; these heuristics are the complementary defence against distinct-URL traps that normalization cannot fold. The thresholds are deliberately conservative so a genuinely deep or faceted site is admitted; only operator seed URLs bypass the gate (the check runs on discovered links, where traps propagate), and a link that fails to parse is refused rather than fetched. A complementary whole-run page budget (PORT-03, from upstream `nikitakarpei/yacy-rwi-node` commit 728b3738) caps how many pages one crawl run may admit in total across all hosts — `YAGOCRAWLER_MAX_PAGES_PER_RUN`, default 50 000, 0 disables it — enforced in the frontier's admission above the per-host cap and per-crawl depth limit, so a trap that mints distinct URLs faster than the structural heuristics catch them still cannot make one run crawl unbounded; the frontier logs once when a run reaches its budget. | Consumes fewer wasted crawl orders. | Not applicable. | No new third-party dependency (stdlib only); no wire change. | `yagocrawler/internal/urltrap/urltrap_test.go`, `yagocrawler/internal/crawladmission/url_admission_test.go`, `yagocrawler/internal/frontier/url_frontier_test.go`, `yagocrawler/cmd/yagocrawler/crawler_config_test.go` | Content-level near-duplicate detection at crawl time (Charikar/Manku SimHash, WWW 2007), to catch traps that serve distinct URLs with near-identical bodies and to stop expanding such a host, is the planned complementary slice (FTR-080 follow-up). |
| FTR-104 | Portal | Operator-editable portal theme (server-side Handlebars) | Implemented (ADR-0033 slice 2 of 3): the public portal render path first consults an operator theme — Handlebars templates for the search page and the results page plus a shared CSS styles block — stored in the durable vault by the new `internal/portaltheme` store and rendered server-side with `mailgun/raymond` v2.0.48 (pure Go, MIT; the ADR is the dependency gate). The built-in Go template remains the permanent fallback: a theme that is disabled, missing, unparseable, or failing at render time (raymond helper panics are recovered) degrades to the default render, so a broken operator template can never blank the public surface, and a persistent render failure logs once per saved body rather than per request. Templates interpolate a fixed, documented view model (lowerCamel keys mirroring the Go portal data: query, results with escaped fields, pagination with a derived `show`, verticals, `imageVertical`, format links, elapsed); crawled-content fields auto-escape through `{{...}}`, only the pre-escaped highlighted snippet and the operator styles block render raw. A curated helper allowlist (urlencode, truncate, pluralize, formatNumber) touches no filesystem, network, environment, or process. Documents are capped at 256 KiB, record their parse status for the editor, and every save/reset/toggle is a config event. The built-in default bodies are a faithful Handlebars port of the current portal design (logo, autocomplete, facets, image grid with lightbox, pager, provenance badges), so the design editors seed from the page visitors already see and "Default design" restores it. The theme starts disabled; no public behavior changes until the operator opts in. | Not applicable. | Not applicable. | No protocol change; public listener only. | `yagonode/internal/portaltheme/*_test.go`, `yagonode/internal/publicportal/theme_test.go`, `yagonode/internal/yagonode/node_portal_theme_test.go` | Slice 3 (ADR-0033) shipped: the admin Public portal section (nav between Activity and Autocrawler) hosts the Configuration tab (the portal-facing settings, removed from the flat Configuration sheet so no second competing form exists) and two design tabs — Portal design and Results design — each a no-JS-capable form (plain textareas submit as-is) progressively enhanced by vendored, self-hosted editors: CodeMirror 5.65.21 (Handlebars + CSS modes) and a GrapesJS 0.21.13 visual canvas with the webpage preset, synced through a first-party portal_designer.js (full-page templates split around the body for the canvas; visually-added CSS merged into the shared styles between grapes markers). Each tab saves its template plus the shared styles and the theme toggle in one submit, surfaces the stored parse status, and offers Default design / Default styles buttons that restore the built-in bodies. Saves are CSRF-protected admin POSTs to /admin/portal/design, gated to the two page documents. The Public portal admin page (only) serves a page-scoped CSP with style-src 'unsafe-inline' for the GrapesJS canvas iframe; script-src stays 'self' everywhere and all other pages keep the strict policy (amended into ADR-0033). Vendored assets carry pinned version + license headers and are catalogued in deploy/THIRD_PARTY_ASSETS.md; assets/vendor and the vendored htmx bundle are excluded from Semgrep as non-actionable foreign minified code. |
| FTR-105 | UI | Performance history and Backup & restore console pages | Implemented (closes UI-09): a new `internal/metrichistory` sampler snapshots the node's shared Prometheus registry every ten seconds into a bounded ninety-point ring (about fifteen minutes) and derives operator-facing series — HTTP request rate, 5xx rate, interval-average request latency (histogram delta), DHT postings-sent rate, and the crawl/index queue depth gauges; counter resets clamp to zero, the first gathering only seeds the baseline, and a failing gather records nothing. The Performance page renders each series as a current value plus a server-rendered SVG sparkline (pure numeric markup, no charting JavaScript, flat series draw a midline) with peak/window/sample metadata, and links the new Backup & restore page. That page (nav entry Backup) shows the data directory and the vault's used/quota bytes and arms the operator with the exact offline `deploy/backup.sh` / `deploy/restore.sh` invocations for docker and systemd deployments — a consistent backup requires the node stopped by design (OPS-03), so the console deliberately does not offer an online copy. The restart controls gained the UI-09 acceptance switch: `YAGO_ADMIN_RESTART_ENABLED=false` strips both the node and crawler restart actions and the Restart page renders as unavailable. | Not applicable. | Not applicable. | No protocol change; ops listener only. | `yagonode/internal/metrichistory/metrichistory_test.go`, `yagonode/internal/adminui/performance_history_test.go`, `yagonode/internal/adminui/backup_test.go`, `yagonode/internal/yagonode/node_ops_pages_test.go` | History is volatile by design — durable history belongs to a Prometheus server scraping /metrics (doc/metrics.md). |
| FTR-106 | Search | Federated hygiene: vertical-safe fallback, peer language filter, stale-posting sweep | Implemented (gap-analysis follow-up): (1) the optional web fallback no longer fires for non-text verticals — an empty image/audio/video/app search stays empty instead of silently returning unfiltered text links (SEARCH-40). (2) Remote peer results now carry the peer-reported document language (the URL metadata row's plain `lang` property) instead of echoing the query filter, and a `language:` query drops remote rows whose reported language contradicts it — rows without a reported language are kept, and languages compare by two-letter prefix so ISO 639-1 and 639-2 spellings reconcile (SEARCH-41). (3) RWI re-ingest hygiene (RWI-01): before storing a fresh crawl batch's postings, the node purges the source URL's existing postings for words absent from the new set (`eviction.PurgeStalePostings`, diffed cheaply through the URL-reference projection), so a changed page stops answering DHT and remote searches for words it no longer contains — posting-free batches and the DHT intake are untouched, so partial transfers can never wipe a URL. Java YaCy accumulates such stale postings indefinitely. | Not applicable. | Not applicable. | No wire change. | `yagonode/internal/websearch/fallback_searcher_test.go`, `yagonode/internal/searchremote/language_filter_test.go`, `yagonode/internal/eviction/stale_postings_test.go`, `yagonode/internal/crawlresults/ingest_removal_internal_test.go` | Complements the crawler-side parity slice (meta robots, canonical policy, redirect dedup, stopword postings). |
| FTR-107 | Peer protocol | Bootstrap seed endpoint + crawlReceipt delay parity + compatibility-matrix truth-up (PROTO-FULL slice 1) | Implemented: (1) the `/p2p/seeds` and `/p2p/seeds.json` bootstrap paths are now mounted — upstream's unauthenticated seed-list mirror that principal peers upload to a bootstrap position — reusing the existing seedlist backend, so third-party peers and tools that bootstrap from the `/p2p/` shape resolve this node; both honour the shared seedlist filters (`maxcount` capped at 1000, `minversion`, `node`, `me`, `address`, `my`, `id`, `name`, `peername`, JSONP `callback`). (2) The remote crawl-receipt endpoint now answers a network-authentication failure with YaCy's retry delay of 3600 instead of an empty response — a re-verified upstream behaviour (`crawlReceipt.java` sets `delay=3600` before the auth return), removing a silent wire divergence. (3) The compatibility matrix and its machine-readable catalog were trued up against upstream master (2026-07): GSA search compatibility is re-marked unsupported (upstream removed GSA in 2020, so there is no live surface), the suggest rows record the index-vs-recent-queries source divergence, the Solr row names the exact upstream mounts it declines, and a new row records MCP/OpenAI-compatible AI surfaces as a deliberate non-goal — this node's agent surface is the Tavily-compatible API, kept deliberately simple. | Not applicable. | Not applicable. | `/p2p/seeds`, `/p2p/seeds.json` added; `/yacy/crawlReceipt.html` delay-on-auth-failure corrected to match YaCy. | `yagonode/internal/seedlist/seedlist_endpoint_test.go`, `yagonode/internal/crawling/crawl_receipt_endpoint_test.go`, `yagonode/internal/compatibility/catalog_test.go` | First slice of the PROTO-FULL parity pass; informed by the upstream-source matrix re-audit. |
| FTR-108 | Search surfaces | Full OpenSearch suggestion contract on /suggest.json and /suggest.xml (PROTO-FULL slice 2) | Implemented: the suggestion endpoints now honour upstream YaCy's full request contract — `count` clamped to 30, `timeout` (default 300 ms) bounding the index lookup via a request-scoped deadline, a JSONP `callback` (validated to a bare JavaScript identifier so a reflected callback cannot inject script), and the open `Access-Control-Allow-Origin: *` header upstream sends so cross-origin search boxes can read the suggestions. The suggestion source was already index-backed — real matching document titles merged with recorded recent queries — which is a deliberate, wire-identical difference from upstream's term-dictionary `DidYouMean`: the OpenSearch array shape a client consumes cannot distinguish the two, and indexed titles are the more useful completion for a content portal. Both matrix rows flip from partial to implemented. | Not applicable. | Not applicable. | `/suggest.json` and `/suggest.xml` now accept `count`, `timeout`, and (JSON) `callback`, and answer with open CORS. | `yagonode/internal/yacysearch/suggest_params_test.go` | Second slice of the PROTO-FULL parity pass. |
| FTR-109 | Search ranking | Curated relevance judgments | Implemented: the vault-backed qrels store is node-wired and exposed through `/api/admin/v1/search/judgments`. A judgment carries graded URLs plus optional query cluster, observation time, intents, navigational intent, and slice labels for held-out evaluation. | Not applicable. | Not applicable. | No YaCy wire change. | `yagonode/internal/judgments/*_test.go`, `yagonode/internal/yagonode/search_judgments_endpoint_test.go` | Human labels are authoritative over click-derived evidence for the same query. |
| FTR-110 | Search ranking | Coordinate-ascent profile learner | Implemented: a pure-Go coordinate-ascent preview fits the visible field boosts and priors to mean NDCG@10 on the current judgment set. It remains the small-data fallback and never changes the live profile until the operator saves it. | Not applicable. | Not applicable. | No YaCy wire change. | `yagonode/internal/rankfit/*_test.go`, `yagonode/internal/yagonode/search_ranking_tune_test.go` | The learned document rerankers are tracked in FTR-111. |
| FTR-111 | Search ranking | Pure-Go learned ranking and held-out promotion | Implemented (ADR-0043): signed linear LambdaRank and bounded histogram LambdaMART train in-process from the lexical-evidence boundary. Serving reranks only locally stored candidates until representative federated judgments exist, and equal scores preserve input order. Query-clustered train/development/test partitions include chronological clusters when timestamps exist. Lexical baseline, active incumbent, and proposal run on one frozen pool; promotion requires a gain over both, at least 20 independent held-out clusters, and a non-negative cluster-level paired-bootstrap lower bound. Recall, NDCG, ERR, MRR, alpha-NDCG, intent, duplicate, domain, discounted top-10 safety/spam exposure, latency, peer-byte, and timeout reports guard promotion. Training is capped at 1,000 queries, 200,000 pooled results, 100,000 examples, and 1,000,000 pairs with inner-loop cancellation. Approved snapshots use atomic catalog reads and compare-and-swap activation; the vault restores the active revision and eight rollback revisions. Admin endpoints expose status, train, rollback, and explanations. | Not applicable. | Not applicable. | No YaCy wire change. | `yagonode/internal/rankfit/*_test.go`, `yagonode/internal/rankingtrain/*_test.go`, `yagonode/internal/learnedrank/*_test.go`, `yagonode/internal/rankingmodel/*_test.go`, `yagonode/internal/yagonode/search_ranking_*_test.go` | CPU-only and pure Go; no external API, sidecar, separate trainer, native plugin, or dynamic model runtime. |
| FTR-112 | Search ranking | Immutable document and federation evidence | Implemented: each candidate carries a versioned 33-signal vector covering retrieval branches, field scores, coverage/proximity, quality/spam/text shape, date confidence/freshness, domain authority, URL prior, local/remote rank, source count, peer support, and peer reputation. Missing evidence is explicit and neutral. Explain output uses the active profile, local RM3 stage, active learned-model candidate window, learned contributions, tree paths, model revision/kind, and final result ranks. Index-cache and paging-session identities structurally cover every result- or policy-affecting request field except pagination and deep-clone nested data. Disk post-filters and facets traverse query matches with a bounded `_id` cursor, bounded score top-k, five-second deadline, and 100,000-hit cap; deadline, cap, and partial-shard failures return errors instead of publishing incomplete counts. | Not applicable. | Not applicable. | No YaCy wire change. | `yagonode/internal/searchcore/ranking_evidence_test.go`, `yagonode/internal/learnedrank/*_test.go`, `yagonode/internal/searchindex/cached_search_correctness_test.go`, `yagonode/internal/searchindex/bleve_disk_index_test.go`, `yagonode/internal/searchsession/searchsession_test.go`, `yagonode/internal/yagonode/search_explain_endpoint_test.go` | Evidence is computed before learned ranking and is immutable between stages. |
| FTR-113 | Search ranking | Persistent quality, temporal, anchor, authority, safety, and cluster evidence | Implemented: document storage persists signed quality with neutral missingness, lifecycle publication/modification/first-seen/content-change dates and confidence, trusted inbound anchors with atomic recrawl replacement, domain authority/confidence, structured and character-model safety evidence, and exact/near-duplicate cluster identity. Publisher-declared adult/RTA ratings and `isFamilyFriendly=false` remain blocking explicit evidence; `isFamilyFriendly=true` is not trusted as general evidence and cannot bypass the content classifier. Tavily safe-search image output requires `SafetyGeneral` evidence. Future or contradictory dates are rejected and unchanged recrawls cannot refresh rank. | Emits structured dates and safety labels, normalized outbound anchors with `nofollow`/`ugc`/`sponsored`, and content identity inputs. | Carries lifecycle dates, safety evidence, and outbound anchor relations. | No YaCy wire change. | `yagocrawler/internal/pageparse/*_test.go`, `yagocrawler/internal/pageindex/*_test.go`, `yagocrawlcontract/*_test.go`, `yagonode/internal/documentstore/*_test.go`, `yagonode/internal/crawlresults/*_test.go`, `yagonode/internal/contentcluster/*_test.go`, `yagonode/internal/contentsafety/*_test.go`, `yagonode/internal/hostrank/*_test.go`, `yagonode/internal/tavilyapi/*_test.go` | Fetch/index time is never substituted for publication time; unsafe or rel-qualified links do not add ranking trust. |
| FTR-114 | Search ranking | Bounded lexical candidate union and term dependence | Implemented: strict all-term fielded BM25 and a relaxed 60%-match branch are fused deterministically. Phrase, ordered SDM, unordered and global proximity evidence, and bounded RM3 improve recall and ordering while required query terms remain mandatory. RM3 uses five distinct feedback documents, 256 tokens per document, at most three terms, and a two-document support floor. | Not applicable. | Not applicable. | No YaCy wire change. | `yagonode/internal/searchindex/minimum_terms_query_test.go`, `yagonode/internal/searchindex/sdm_ordered_test.go`, `yagonode/internal/searchlocal/candidate_union_test.go`, `yagonode/internal/searchcore/pseudo_relevance_model_test.go` | Candidate generation is bounded to 200 for training and 100 for learned serving. |
| FTR-117 | Admin console | Photon settings-grid + widgets restyle | Reworked the Photon skin (`assets/photon.css`) and two nav glyphs (`templates/layout.tmpl`) so the console reads as a compact QNX Photon dialog, verified by headless render in both Firefox and Chromium. Settings grid: the label is a fixed 10rem column and the field fills the rest of the row right after it (was label `flex:1 1 auto` + a right-justified 18rem control, which flung the field to the far edge with a dead gap); the group `<fieldset>` is a plain bounded block — deliberately NOT `width:fit-content`, which collapses a fieldset in Firefox into stub separators and overflowing fields. Group frames are a thin etched groove whose legend interrupts the top line (was a heavy raised bevel that printed over the legend text); fields and data-table bodies are white sunken wells; tables size to content. Icon fixes: the Backup glyph rendered solid black (a `<path>` with no `fill`) — redrawn as a line-art archive box using `currentColor`; the Restart glyph was the same circular-refresh arrow as Autocrawler — redrawn as a power symbol so the two read distinctly. | Not applicable. | Not applicable. | No behavior or wire change; admin console CSS + SVG sprite only. | Rendered-page structure unchanged (adminui tests green); layout verified by headless Firefox + Chromium screenshot. | Addresses operator feedback: the console diverged from the QNX Photon reference, inputs spread apart, the layout broke in Firefox, and two nav icons were wrong (one black, two identical). |
| FTR-116 | Search surfaces | yacysearch navigation facets + author/count param parity | Implemented (PROTO-FULL slice 3): the public `/yacysearch.json` and `/yacysearch.rss` surfaces now render the local facet groups as YaCy navigation. A `nav=` request turns on facet counting and the response carries every group (hosts/authors/filetypes/languages/protocols/dates) with per-value counts and, for dimensions with a query operator, a refine `modifier` (`site:`/`author:`/`filetype:`/`language:`) plus a ready-to-use `url`; the JSON `navigation` array and the RSS `yacy:navigation` element share one surface-neutral builder. The parser also (a) honors `count` as the OpenSearch alias for `maximumRecords` via a multi-key lookup and (b) wires the `author:` operator and the `author=` param into the request — previously `ParseTextQuery` extracted the author but the JSON request builder dropped it, so author filtering silently never fired from the public surfaces even though the whole searchcore→searchlocal→searchindex chain honors it. | Not applicable. | Not applicable. | Wire-compatible: the navigation shape mirrors YaCy's navigator ids and modifiers; the facet scan runs only when `nav=` is present, so the default path stays cheap. The public per-page cap (10) is left unchanged by design (resource-bound public surface), a deliberate deviation noted for the eventual status flip. | `yagonode/internal/yacysearch/navigation_test.go` | Advances the `/yacysearch.json` and `/yacysearch.rss` rows in compatibility.md (notes updated; status stays `partial` pending the HTML-surface and selective-nav sub-behaviors, to be flipped in the PROTO-FULL closing audit). |
| FTR-115 | Admin console | YagoRank operator console | Implemented: the Carbon YagoRank section edits lexical profile weights, previews coordinate-ascent tuning, shows active learned revision/kind and rollback availability, trains linear LambdaRank or histogram LambdaMART, reports held-out NDCG gain, confidence, promotion reasons, and split sizes, and rolls back a model revision. | Not applicable. | Not applicable. | No YaCy wire change; admin ops surface only. | `yagonode/internal/adminui/yagorank*_test.go`, `yagonode/internal/yagonode/node_ranking_console*_test.go` | Training can finish without promotion; rejected models never replace the active snapshot. |
| FTR-118 | Search ranking | Authenticated and debiased click evidence | Implemented: optional click capture issues short-lived HMAC-SHA256 impressions bound to query, model assignment, ordered URL/cluster identities, positions, propensities, expiry, and nonce. Adjacent FairPairs exposure is randomized at propensity 0.5; team-draft interleaving is available for model comparisons. Replay and click counts are bounded. Only aggregate query/model/cluster evidence is stored, and implicit judgments combine clipped IPS and SNIPS after minimum randomized exposure. | Not applicable. | Not applicable. | `/searchclick` is yago-local and does not change YaCy protocol responses. | `yagonode/internal/clickcapture/*_test.go`, `yagonode/internal/yacysearch/click_endpoint_test.go`, `yagonode/internal/yagonode/node_click_capture_test.go` | Capture is off by default; curated qrels replace click evidence for the same query. |
| FTR-119 | Engineering | Repository-wide 100% test-coverage gate | At 100.0% gated statement coverage; every statement-bearing function is exercised, including the hard clusters — shardvault split/compaction crash-recovery and manifest paths, landiscovery UDP broadcast/receive loops, crawlresults ingest micro-batching, and the admin/search/schedule surfaces. | At 100.0%, including the firefoxfetch Marionette browser layer (protocol/profile/render driven by in-memory fakes; real process launch reached through injected func-var seams). | At 100.0% (the generated `crawlrpc` gRPC glue stays in `COVER_EXCLUDE`). | Both at 100.0%. | `Makefile` (`COVERAGE_MIN ?= 100`, `cover-check` per module, `COVER_EXCLUDE` = `vaulttest` / `test/e2e` / `crawlrpc`); campaign commits TEST-COVER-01/02/03. | Implemented (TEST-COVER-01..03): `make verify`'s `cover-check` now enforces a per-module gated total of 100.0% across all six modules — yagonode, yagocrawler, yagocrawlcontract, yagomodel, yagoproto, yagoegress — raised from 80%. Empty-body 0-statement no-op stub methods are inherently uncoverable and do not move the statement-weighted total, so they are exempt. A handful of genuinely-unreachable defensive branches are made reachable by minimal, behavior-preserving func-var seams (e.g. `spawnFirefox`/`listenLoopback` process launch, `marshalAnnouncement`, `randRead`, `newScheduleTicks`, `cloneBaseTransport`) rather than coverage carve-outs. |
| FTR-120 | Crawler | PDF extraction reads real-world PDFs (filter-first dictionaries, TJ kerns, ToUnicode) | Not applicable. | Implemented (SEARCH-PDF-01, operator report: the live node's cached copy of an arXiv PDF held three garbage lines): four compounding extractor defects fixed. (1) The /Filter value parser read names from the whole remaining dictionary, so `/Filter /FlateDecode /Length 99` — the order pdfTeX writes — yielded a bogus `Length` "filter" and the stream was dropped: 56 of the test file's 60 streams vanished, leaving only figure labels. Only the entry's own value (bare name or bracketed array) is read now. (2) TJ-array kerns split words: `[(h)31(ydrogen-ric)31(h)]` indexed as "h ydrogen-ric h"; fragments now rejoin, with a space only at inter-word kerns (≥180/1000 em). (3) Octal escapes passed through as digit text (`\015ux` → "015ux"); they decode to their byte, TeX's OT1 (0x0B–0x0F) and T1/Cork (0x1B–0x1F) ligature codes expand to ff/fi/fl/ffi/ffl, high bytes read as Latin-1, and `<hex>` show-strings decode at all (they previously never indexed). (4) Subset fonts move glyphs to arbitrary codes (modern pdfTeX shows "firmly" as `\002rmly`), which no byte heuristic can recover: the embedded ToUnicode CMaps now parse (bfchar/bfrange, capped; fonts resolved by scanning `/Fn N 0 R` references to their objects, conflicting names dropped) and Tf font switches select the table per text block. Document-info titles additionally read the UTF-16BE `<FEFF..>` hex form. | Not applicable. | Extraction-quality parity step toward YaCy's pdfbox: embedded-text only, no OCR, CID fonts without a usable ToUnicode still honestly unparsed. | `yagocrawler/internal/formatparse/pdf_test.go` (pdfTeX-shaped regression fixture), `yagocrawler/internal/formatparse/pdf_tounicode_test.go`; live-verified against arXiv 2412.07792 (61 garbage bytes → 45.8 KB full text), arXiv 1706.03762 ("firmly" recovered via ToUnicode), and IRS fw4.pdf (Adobe-generated, unaffected) | The three real files are verification-only downloads, not repo fixtures (synthetic fixtures mirror their shapes). Re-crawling already-indexed PDFs refreshes their stored text through the normal recrawl lifecycle. |
| FTR-121 | Search ranking | Persistent duplicate clusters and peer reputation | Implemented: exact content identity precedes bounded SimHash-LSH and Jaccard near-duplicate assignment. Every accepted URL remains stored and final ranking consolidates a deterministic representative while aggregating source evidence. Remote search uses decayed Beta success/failure/timeout/invalid evidence and deterministic reputation-weighted RRF. A startup snapshot keeps vault scans off the request path; a bounded asynchronous queue serializes observations, retries the same monotonic batch with capped backoff until persistence or shutdown, reconciles a superseded sequence, refreshes the immutable snapshot after writes and every five minutes, keeps the last good value on refresh failure, resumes after restart, stops admission before draining, and gives every concurrent closer the same bounded shutdown completion. Aggregate IPv4 `/24` and IPv6 `/48` influence is capped; unresolved hostname peers share one conservative group rather than bypassing the cap with distinct names. | Emits content and canonical evidence used for cluster assignment. | Carries content identity inputs. | YaCy peers remain wire-compatible; reputation is local policy only. | `yagonode/internal/contentcluster/*_test.go`, `yagonode/internal/peerreputation/*_test.go`, `yagonode/internal/searchremote/peer_*_test.go`, `yagonode/internal/yagonode/node_peer_reputation_test.go` | Unclustered textual similarity is handled by MMR and never causes a volatile hard deletion. |
